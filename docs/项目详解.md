# PIKS 项目详解

> 全局背景、数据流、技术栈、边界。权威设计见根目录 `PIKS架构设计文档.md`(v1.0,已定稿;顶部含「现状偏差」注记,记录落地演进)。
> **以代码为准的完整现状架构见 `docs/架构总览.md`**。本文只记录**架构文档之外的执行决策**,并作为开发期唯一的决策登记处。

## 一句话定位

PIKS 是 **Knowledge System,不是 Trading System**。核心价值 = "我终于能解释为什么市场发生了这件事",而不是"明天买什么"。

## 三条冻结骨架(不可违背)

1. **存储分层**:`PostgreSQL = Source of Truth`;界面层在迭代 5 由「Markdown 投影 + Obsidian」演进为 **Web(PG 直渲)**,Obsidian/GitHub 于 5-2 下线(`PIKS-Vault/` 仅存档)。原 v1.0 蓝图的 `Markdown = Knowledge Projection` / `Obsidian = Knowledge Interface` 已被 `docs/phase2/design/web-app.md` 取代。
2. **三层模型**:`Fact ≠ Inference ≠ Belief`,禁止把 AI 推测写成事实
3. **AI 边界**:`AI → Structured Output → Schema Validation → Business Validation → Knowledge Core`,AI 不直接写库

## 执行决策(已定稿)

| 决策点 | 结论 | 定稿日期 |
|---|---|---|
| 开发顺序 | 先跑通**最小闭环**(新闻→Event→Markdown→Obsidian),再逐阶段补全,不按架构文档 33 节线性 Phase | 2026-08-26 |
| 个人知识回写 | **分域双源(已演进)**:生成层→PostgreSQL 唯一可信源;个人层原本计划 Obsidian 唯一可信源 + 单向 `harvest` 收割器,迭代 5 改由 **Web 内编辑 `personal_notes`** 直接落 PG(替代 Obsidian 编辑与收割器,5-2 落地;iter4 的 `cmd/harvest` 废弃) | 2026-08-26(演进 2026-08-27) |
| 数据源契约 | **事件类多源(2026-09-20 起)**:快讯接 **6 个独立机构源**(东方财富/金十数据/财联社/新浪财经/同花顺/富途资讯),每源落各自 `sources` 机构名行,上游原始字段落 `raw_documents.extra`(见 issue #43)。**注意:快讯品类无官方公开 API,各源皆为网页内部接口、无 SLA**,由源健康监控(连续失败自动暂停)+ 限频退避兜底;⚠️ 同机构不同端点不算独立源(Go 东财 = akshare 东财)。**明确数值**(行情/财务/估值/宏观)单源即可;**公告独立源(巨潮)已于 2026-09-21 接入**(issue #50,`status='collected'` 不进 LLM,只存标题 + 原文 PDF 外链)。行情/涨停等 Phase 后补 | 2026-08-26(演进 2026-09-21) |
| 数据库与 steady | steady 将上实盘 → **独立 Postgres 实例**(DB 级零耦合,同 host 双容器亦可);备份策略统一走晚间窗口;DSN 配置化 | 2026-08-26 |
| 因果链 | **模板,非约束**。Relationship 存通用有向边;因果链退化为分析模板 + 查询视图 | 2026-08-26 |
| 技术选型 | Go + PostgreSQL + AI Provider 适配器 + 界面层(原 Markdown/Git/Obsidian,迭代 5 演进为 Web)。V1 不上队列框架/ES/Neo4j/K8s(任务用 cron + pg 任务表) | 2026-08-26(演进 2026-08-27) |
| AI 接入与成本 | 首个适配器 = **OpenAI 兼容**(覆盖 DeepSeek 官方 + OpenAI 兼容订阅源,换源只改 base_url/model)。**模型分层**:高频机械抽取走便宜档(extract),深度复盘/周报综述走高智档(reasoning),截图走视觉档(vision,空=回退文本模型)。去重先于调 LLM + task_runs 记 token + 日预算护栏。**无本地模型,仅用厂商模型**。详见 `docs/phase1/design/iter0-min-loop.md` §5 | 2026-08-26 |
| 大模型配置入库 | **`app_config` 表为 AI 配置权威源**(代码不再读 `PIKS_AI_*` 环境变量);`/settings` 页面可编辑,密钥只存库、页面显示掩码、留空不修改;deploy 顺序改 postgres→migrate→web(web 启动读表) | 2026-08-27 |
| 界面层演进 | **Web 平台替换 Obsidian/GitHub**(迭代 5):PG 直渲(看板/事件/实体/图谱/复盘/对账/笔记/周报/交易/AI 对话);5-2 下线 vault/GitHub(pipeline 去 publisher、跳过写盘+git),`PIKS-Vault/` 存档 | 2026-08-27 |
| 周报聚合与 AI 综述 | iter4 的 `cmd/weekly-report` 由 `/weekly` Web 聚合取代;**AI 综述**按 ISO 周缓存(`weekly_summaries`),手动按钮触发、GET 永不调 LLM,预算护栏 + task_runs 记账,未配置/预算/无数据/失败如实降级 | 2026-08-28 |
| 交易功能 + 交易闭环 | 每日自交易入知识库(同花顺截图 → vision 抽取 → 预览确认入库 `trades`/`positions`,手动兜底,交易股票自动补实体)+ AI 带引用解读(防未来函数截断)+ **持仓 AI 诊断**(`position_reviews` 按快照日缓存,数字 Go 算 + KB 带引用 + risks 存笔记)+ 本周交易/持仓进周报;**只做 dev 验证不部署 lab** | 2026-08-28 |
| 语义检索(G8) | 探针 Zen 无 embeddings 端点(404)→ 否决方案 A(Ollama),**选方案 B:LLM 同义扩展**并入检索(原文权重 1.0/扩展 0.5),失败降级纯关键词 + 标注;**dev-only 不升级生产** | 2026-08-28 |
| 聚类质量 | 跨簇重复盲区修复:重审视 Pass 追加到 `cmd/cluster`(候选池扩为活跃簇 canonical ∪ 未聚类,复用迭代 1 阈值 + LLM 确认,不降门槛),**dev-only 验证不部署 lab** | 2026-08-28 |
| 跨源同事件判定 | 事件多源后(T1 六机构源)同一事件被各家分别报道、措辞各异 → `NormalizeTitle` 抽取 `【】` 内嵌标题前缀还原相似度(**不降阈值 0.7**);簇内**含 merged 成员**读回各源来源,`/api/v1/events` 仅跨源簇(≥2 机构)下发 `cluster_sources`,前端展示「N 家印证」。**零 schema**,dev-only | 2026-09-20 |
| 漏报 / 冲突检测 | 单源基准 = **机构级**(同机构不同端点不算独立源);新增 `source_count` 让单源可见(标「单一来源」,措辞中性 —— **独家报道常见,≠消息不实**,绝不丢弃事件);跨源数字冲突走**确定性数值比对**(不用 LLM:要可复现;`internal/cluster/conflicts.go`,单位白名单 + 骨架 Jaccard 门控 0.6),**留双方原文、禁止静默择一**(Fact ≠ Inference)。门控按生产真实事实句标定;**零 schema**,dev-only。顺修 `deploy.sh` 的 lab 侧 `scripts/` 同步(编排漂移根因) | 2026-09-20 |
| 公告分级(数据源分层 S1) | 公告 ~1200 条/日全平铺「看不过来」→ **纯规则标题分级**(`internal/announce`:`must`/`important`/`routine`/`noise`),采集时落 `raw_documents.grade`(迁移 `0017`),前端公告 tab **默认只显必读+重要**、其余一键展开。**只落标签、不进 LLM**(公告 `status='collected'` 本就零 token 成本,分级不产生成本收益,价值纯展示层);分级是**机器判定(Inference)非事实**且**只折叠、不隐藏**(`grade=NULL` 按常规显示)。巨潮 `announcementType` 不可解,故只能靠标题规则。见 issue #68 | 2026-09-21 |
| 快讯提频 + 反封禁护栏(数据分层 C 层) | 快讯从「每交易日 1 次(16:10 后)」提频到**盘中每 3 分钟**(交易日 09:15–15:05)。**硬前置 = per-host 三护栏**(`internal/collector/limiter.go`:令牌桶 2req/s·突发3 / 空响应哨兵连续3次降速封顶×1/8 / 熔断连续4失败开路60s)——现状只有 per-source `minGap`,对同 host 突发无约束;实证依据:东财 push2 频繁请求 SSL RST、正文接口 300 连发 209 失败。⚠️ **哨兵与熔断靠跨轮累积** → 采集**必须常驻**(仿 `research-worker`)而非 cron 每 3 分钟拉起,否则护栏形同虚设;新 `collector` 服务**复用 `piks-tools` 镜像**(仍四镜像)。**S2 新浪去留**:以生产聚类同尺(@0.7)实测新浪↔金十近同对仅 11.2%/11.5% 且对称 → **保留新浪**,「转载≠独立源」交 #45 P2 剥转载修根因(**不得**以「数据无用」删源)。连带:worker `-limit 300→800`;去掉自动 `PauseSource`(3 分钟就停源 + 无人读取)→ 熔断承担 + **尊重 `sources.status='paused'`**。**零 schema**,dev-only | 2026-09-21 |
| 热榜(数据分层 D 层) | 热榜**从零新建**(前置核查:热榜源与 `morning-brief` 在代码库中**均不存在**)。两源:同花顺话题榜(15 条)+ 财联社首页热文(13 条,SSR 解析);常驻 `hot-topic` 服务盘中**每 30 分钟**采(护栏同 C 层),日管线补一发收盘态。🔴 **方案 A:两源各出各的、不合并、不加权、不排名** —— 实测 615 对标题**同题对 = 0**(粒度不同:题材/事件 vs 文章/复盘),混排必然误导。🔴 **不进 `raw_documents`/聚类/`events`**:另立 `HotTopicSource` 接口 + 独立表 `hot_topic_items`(迁移 `0018`)+ 独立页 `/hot-topics` —— 把「可被操纵的热度」隔离成**结构性约束**(红线:只作展示,不作重要性判定,不与印证度合并)。⚠️ `rank` 取**上游数组下标 + 1**、跳行留空号、**不得重排**(实测财联社数组顺序 ≠ `readNum` 降序);SSR 漂移**报错不空成功**(#64 教训);`hot_value` `NULL ≠ 0`。见 issue #68 | 2026-09-22 |

## 数据流(最终形态)

```
新闻/公告(6 机构源 → raw_documents.extra) → Collector → raw_documents → LLM 结构化抽取
  公告另路:巨潮 → raw_documents(status='collected' + 标题分级 grade,不进 LLM)
  热榜另路:同花顺话题榜 + 财联社热文 → hot_topic_items(独立表,不进 LLM/聚类)
  → Schema/Business Validation → Knowledge Core(PostgreSQL)
  → Web 直渲(看板/事件/实体/图谱/复盘/对账/周报/交易/AI 对话/热榜)
  → 你(Web 内编辑 personal_notes:Belief/Case/Mistake/我的理解 + 交易截图录入)
  → 落回 PostgreSQL(权威源唯一,无 vault/git 中间层)
```

## 技术栈

| 模块 | 技术 | 备注 |
|---|---|---|
| Backend | Go 1.26 | 10 个管线命令 + `cmd/web`(JSON API)+ `research-run`(深研 CLI)+ `research-worker`(深研队列 worker)+ `probe` 探针(不进镜像) |
| Database | PostgreSQL(Docker) | 唯一 Source of Truth;**18 个前向迁移(0001~0018)**,22 张表 + `schema_migrations` 记账表(0016 是去重键索引、0017 是 grade 列、0018 是热榜独立表) |
| AI | AIProvider 接口 + OpenAI 兼容适配器(覆盖 DeepSeek/订阅源) | 配置存 `app_config` 表,模型分层 extract/reasoning/vision,预算护栏 |
| Interface | **React SPA**(Vite 5 + React 18 + TS + React Router v6) | nginx 单入口 :8090 服务静态 + 反代 `/api/*`;**Go 只提供 JSON API,不渲染 HTML**;Obsidian/vault 已下线(存档) |
| 部署 | Docker Compose | dev 单机 + 生产 lab(`piks-gateway`/`web`/`research` 常驻 + `tools` profile=run)**四镜像**(2026-09-20 拆分,issue #47) |

## 边界(明确不做)

- 自动交易 / 荐股 / 买卖信号 / 真实资金 / 高频行情;交易功能只做「记录 + 解读 + 诊断」,不连券商、不下单、不做信号
- 与 steady **领域耦合**(不共享表、不互相调用);DB 级完全隔离,独立 Postgres 实例
- 已做 Web 平台(React SPA + Go JSON API);仍不做:Neo4j、Elasticsearch、Kubernetes、微服务、多 AI Agent、十几个数据源
