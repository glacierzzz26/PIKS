# PIKS 架构总览

> **本文是 PIKS 的「一份完整架构」——以代码为准的现状快照。**
> 定稿蓝图见根目录 `PIKS架构设计文档.md`(v1.0,冻结);执行决策登记见 `docs/项目详解.md`;
> 阶段进度与遗留见 `docs/进度总表.md`。**当三者与代码冲突时,以代码与本文为准**(本文由通读全部代码整理)。
>
> 最后核对:2026-09-20(dev `84b3b49` / master `b996864`)。

---

## 0. 一句话定位

PIKS 是 **Knowledge System,不是 Trading System**。

核心价值 = 「**我终于能解释为什么市场发生了这件事**」,而不是「明天买什么」。

形态:A 股个人投研平台。把公开市场信息(快讯、涨停池、行情、龙虎榜、财务、行业指数、宏观指标)
自动加工为**结构化事件与实体**,沉淀进 PostgreSQL;再经 React 界面完成
**研究 → 决策 → 持仓 → 复盘**的个人闭环。

---

## 1. 三条冻结骨架(不可违背)

贯穿全部代码的三条硬约束。任何新功能都不得绕过。

| # | 骨架 | 在代码中的落点 |
|---|---|---|
| 1 | **PostgreSQL = Source of Truth** | 16 个前向迁移(0001~0016)建 21 张表 + 运行时 `schema_migrations` 记账表;Markdown/Obsidian 投影层已于迭代 5-2 下线,`PIKS-Vault/` 仅存档 |
| 2 | **Fact ≠ Inference ≠ Belief** | 研报三域标记 `fact 数据 / calc 计算 / opinion 研判`(`lib/constants.ts` `DOMAIN_LABEL`);`metrics` 是**数字唯一源(Fact)**,LLM 只写 `opinion` |
| 3 | **AI 不直接写库** | `AI → Structured Output → Schema Validation → Business Validation → Knowledge Core`。AI 输出先落 `raw_documents`/task 中间态,经校验才进知识核心;research 侧另有 Number Lint + Quality Gate 两道机检 |

> 骨架 3 的具体体现:深研 LLM **从不触碰数值**——所有数字由 Python 确定性计算出,LLM 只写三段定性叙述,
> 写完后由 Number Lint 反向扫描报告正文,把「不是程序算出来的数字」全部报为 issue。

---

## 2. 系统全景

PIKS 是**同仓三运行时、四镜像交付**的个人系统(2026-09-20 容器拆分,issue #47)。

```
                    ┌───────────── piks-gateway(nginx:alpine)──────────────┐
   浏览器  ────────▶│  nginx :80(发布 :8090)                             │
   (lab 局域网)     │    ├─ /        → React SPA 静态(SPA fallback)       │
                    │    └─ /api/*   → proxy_pass http://web:8090          │
                    └───────────────────────┬──────────────────────────────┘
                                            │ Docker 私网 piks
                    ┌───────────────────────▼──────────────────────────────┐
                    │  piks-web(alpine,纯 Go JSON API,不发布宿主端口)      │
                    │    └─ pgx ──▶ PostgreSQL 16 (:5433 loopback)         │
                    └──────────────────────────────────────────────────────┘
                                            ▲
                    深研触发:web 建 pending 行 + NOTIFY │ worker 认领
                    ┌───────────────────────────────────┴──────────────────┐
                    │  piks-research(python:3.12-slim,常驻队列 worker)     │
                    │    LISTEN piks_research_pending → ClaimPending →      │
                    │    os/exec python3 research/src/cli.py(同容器有解释器)│
                    └──────────────────────────────────────────────────────┘

   管线(cron 每 15min 自判) ──▶ docker compose run --rm tools ./bin/<9 个管线命令>
                               (profile=run,跑一次即退,非常驻)
```

四个镜像各自独立版本、独立升级半径(改前端只重建 gateway;改 Go 只重建 web/tools/research;改 Python 只重建 research)。实测 `docker save` 体积:gateway 25MB / web 8MB / tools 42MB / research 139MB。

| 镜像 | 底座 | 内容 | 职责 |
|---|---|---|---|
| `piks-gateway` | `nginx:alpine` | `nginx.conf` + SPA dist | **唯一对外**入口 :8090;静态 + 反代 `/api/*` |
| `piks-web` | `alpine:3.20` | `bin/web` | 全部 HTTP JSON API;深研只**入队**(写 pending 行 + NOTIFY) |
| `piks-research` | `python:3.12-slim` | `research/` + `bin/research-run` + `bin/research-worker` | Python 运行时 + 深研队列 worker(认领 pending 执行) |
| `piks-tools` | `alpine:3.20` | 9 个管线命令 + `migrations/` + `prompts/` | `profile=run`,跑一次即退 |

> ⚠️ **深研不再在 web 进程内跑**:web 容器已无 python3(拆分后若仍在 web 里 `os/exec python3`
> 就是 2026-09-12 的生产事故复现:`exec: python3: not found`,UI 深研必 failed)。故触发改为
> DB 队列:`migrations/0015` + `cmd/research-worker` + `internal/store/research_queue.go`(LISTEN/NOTIFY)。

三个运行时:

| 运行时 | 位置 | 代码量 | 职责 |
|---|---|---|---|
| **Go** | `cmd/` + `internal/` | 19,687 行(110 个 `.go`:84 生产 + 26 测试) | 采集 / 抽取 / 聚类 / 市场 & 实体 / 对账 / 全部 HTTP API / 深研编排 |
| **Python research** | `research/src/` | 8,353 行(16 个测试文件) | 确定性分析 + 报告装配 + 防幻觉机检(不写库、不调 LLM) |
| **React SPA** | `frontend/src/` | 12,444 行(134 个文件) | 全部 27 条页面路由的呈现层(只读投影 + 交互写) |

> 三者**代码级解耦**:Go 与前端不依赖 research 源码,只经「CLI 参数 + 产物文件契约」交互,
> 由 `scripts/check-research-isolation.sh` 在 CI 校验(见 §5.2);镜像边界由
> `scripts/check-image-topology.sh` 校验(见 §9.1)。
> ⚠️ 拆分未收窄这层解耦:**research 镜像仍由 Go 编译阶段派生**(worker/research-run 是 Go
> 二进制),故改 `internal/research` 会重建 research 镜像 —— 这是 D-11 隔离的一处已知收窄。

---

## 3. 技术栈

| 层 | 选型 | 关键事实 |
|---|---|---|
| 语言 | **Go 1.26** | 唯一直接依赖 `github.com/jackc/pgx/v5` v5.10.0;**无 vendor 目录**(2026-09-12 移除,改 `go.sum` + 模块代理 `goproxy.cn`) |
| 数据库 | **PostgreSQL 16** | 唯一 Source of Truth;16 前向迁移(无 down);表清单见 §4 |
| 前端构建 | **Vite 5 + React 18 + TypeScript**(strict) | 纯客户端 SPA,产物 `frontend/dist/`;**无代码分割**(全静态 import);**无 SSR/Next.js** |
| 路由 | React Router v6 | 单 `BrowserRouter`,27 条路由;筛选态一律写 URL query |
| 样式 | Tailwind(**只做布局**)+ `globals.css`(**唯一视觉源**,2,091 行语义类) | 3 档圆角 token;A 股涨红跌绿 |
| 图表 | ECharts 5(`echarts/core` 按需注册:`BarChart/LineChart/Grid/Tooltip/LabelLayout/CanvasRenderer`) | 禁全量 `import * as echarts` |
| 图标 | lucide-react | 线性图标;禁 emoji |
| 图谱 | 自绘 SVG 力导(`useForceSim.ts`,全对 O(n²),无 Barnes–Hut) | 无第三方图库 |
| Markdown | react-markdown + remark-gfm | **无 heading id**(故页面自己按 `##` 切章并注入锚点) |
| 后端网关 | nginx(`piks-gateway` 独立容器) | 单入口 :8090;SPA 静态 + 反代 `/api/*` → `web:8090`(Go 不发布宿主端口) |
| 服务端依赖 | 系统 Python 3.12 + requirements.txt(不入 Go 依赖链) | akshare / pandas 等;**仅在 `piks-research` 镜像内** |
| AI | OpenAI 兼容适配器 | 配置存 `app_config` 表(**非环境变量**),模型分三层 |

---

## 4. 数据层(PostgreSQL)

### 4.1 表清单(21 张 + 记账表)

按领域分组。每张表由哪个迁移建立亦列出。

| 领域 | 表 | 迁移 |
|---|---|---|
| **知识核心** | `sources` `raw_documents` `events` `evidences` `observations` `relationships` `task_runs` | 0001 |
| | `event_clusters`(+ `events` ALTER) | 0002 |
| **市场情报** | `market_snapshots` | 0003 |
| **实体** | `entities`(单表,`type` 判别 + `detail` JSONB) | 0004 |
| **配置** | `app_config`(AI 配置权威源) | 0005 |
| | (`ai_model_vision` 键种子) | 0007 |
| **个人知识** | `personal_notes` | 0006 |
| **AI 对话** | `chat_sessions` `chat_messages` `attachments` | 0008 |
| **周报** | `weekly_summaries` | 0009 |
| **交易** | `trades` `positions` | 0010 |
| | `position_reviews` | 0011 |
| | `account_snapshots` | 0013 |
| **深研** | `research_runs` | 0012(+0015 队列:`quick`/`days` 两列 + 两个偏索引) |
| **多源采集** | `raw_documents.extra` | 0014(issue #43 T1:上游原始字段原样留存) |
| | `raw_documents` 去重键 | 0016(issue #50:按源是否带 `external_id` 分派为两条 partial unique index;`status` 新增枚举值 `collected`) |

`schema_migrations` 由 `internal/store/migrate.go` 在首次运行时**自动创建**,记录已应用版本。
迁移文件经 `os.ReadDir` 从**文件系统**读取(不是 `embed.FS`),故运行目录下必须有 `migrations/`。

### 4.2 零 schema 扩展模式

后期功能**刻意不建表**,复用两种已有结构。这是本项目的稳定习惯,新增能力应优先照此办理。

| 模式 | 载体 | 实例 |
|---|---|---|
| **多态边** | `relationships(from_type, from_id, to_type, to_id, rel_type, properties)` | P6-4 决策记录:`(trade, <trades.id UUID>, research_run\|event\|personal_note, <UUID>, 'based_on')` |
| **JSONB 指标** | `research_runs.metrics` / `entities.detail` | P7 量价逐日序列、P9 章节清单 `metrics.meta.section_manifest`、风险项 `metrics.risk.items` |

> ⚠️ **`to_id` 必须是各表主键 UUID**(研报用 `research_runs.id`,**绝不用** `run_id` TEXT)。
> 无 FK 约束,接错**静默悬空**——这是本项目已踩过的地雷。

### 4.3 图谱隔离(硬约束)

`relationships` 承载业务边与决策边两类。**决策边不得漏进 `/graph`**,否则成为无名节点。

- 过滤点在 `internal/store/relationships.go` 的 `graphRelFilter`:
  `rel_type NOT IN ('decided_by', 'based_on')`
- **仅 `ListAllRelationships` 使用该过滤**;`graph.go` 自己按 rel_type 显式筛选。
- **新增决策边类型必须同步列入 `graphRelFilter`。**

---

## 5. Python research 子系统

### 5.1 独立性契约(D-11)

| 约束 | 落地 |
|---|---|
| **零共享状态** | `research/` 不 import `internal/` 任何代码 |
| **产物契约版本化** | `run_meta.json` 的 `contract` 字段(当前 `1`);Go 侧遇不支持的高版本**如实失败** |
| **依赖独立** | `requirements.txt` 不进 Go/前端依赖链 |
| **接口冻结** | CLI 三命令 + 产物文件名冻结;加能力可加命令,**不改既有语义** |
| **独立测试** | `pytest tests/`,纯 Python + provider mock,不依赖 PIKS 与数据库 |

### 5.2 CI 守卫

`scripts/check-research-isolation.sh` 校验:
1. Go 侧无 `go:embed.*research`;
2. Go 侧无 `.py"` 引用(白名单 `"cli.py"`);
3. `frontend/src` 无 `research/src` / `research/requirements` / `.venv` 引用;
4. 契约表里写到的产物文件名确实存在于 `research/README.md`;
5. **`exec.Command` 只允许出现在白名单** `internal/research/runner.go`、`cmd/daily-review/main.go`、
   `internal/publish/publish.go`(拆分后 web 容器无 python3,任何**新增**进程执行点都是回退信号)。

`scripts/check-image-topology.sh` 校验镜像边界(见 §9.1):四 target 存在、底座正确、
只有 research 装 Python、tools 逐条 9 命令(拒绝 bulk COPY)、dist 只进 gateway、web 不含 nginx/dist。

### 5.3 CLI 契约(三命令,冻结)

```bash
python3 -m src.cli research   <主体码> [--days N] [--profile P] [--out-dir D] [--run-id ID]
python3 -m src.cli synthesize <产物目录> <代码> --synthesis-file <LLM.json> [--prior-metrics <历史metrics.json>]
python3 -m src.cli gate       <产物目录> <代码> [--json]
```

**退出码语义**(非「执行失败」而是「未通过机检」):
`research` 正常出骨架;`synthesize` 在 Number Lint 有 issue 时退 **2**;`gate` 机检未过退 **3**。
两者**都已把结果写进产物文件**,Go 侧据此区分处置(markdown 回落骨架,lint/gate 原样落库)。

**主体码三类**(P9 泛化):公司 `600519` / 行业 `sw801010` / 宏观 `macro:cn_cpi`。
含冒号时 CLI 需加引号;Go 侧走 `exec.Command` **argv 数组**(不经 shell),改 runner 时**不得**改成 `sh -c`。

### 5.4 产物契约

| 文件 | 产出者 | 内容 |
|---|---|---|
| `run_meta.json` | research | `run_id`/`symbol`/`profile`/`mode`/`as_of`/`sections`/`provider_calls`/**`contract`** |
| `{code}_metrics.json` | research | 指标卡(**数字唯一源**)+ Evidence 链;`meta.section_manifest` = 章节清单 |
| `{code}_skeleton.md` | research | 骨架报告(模板槽位,数字已渲染) |
| `{code}_synthesis_prompt.txt` | research | 给 LLM 的提示(含「只能引用指标卡数字」硬约束) |
| `{code}_final.md` | synthesize | 渲染三段定性后的最终报告 |
| `{code}_lint.json` | synthesize | Number Lint `{scanned,matched,ignored,passed,issues[]}` |
| `{code}_synthesis.json` | synthesize | LLM 三段定性(= Opinion) |
| `{code}_prior_metrics.json` | **Go 编排** | 既往 done 研报 metrics 数组(Go 写、synthesize 读) |
| `{code}_gate.json` | gate | 六项机检结果 |

> 契约变更规则:新增字段/新 section **不升** contract(Go 忽略未知键);**改名/删除/改语义必须升**。

### 5.5 研究档案(7 个 profile)

| profile | 模式 | 章节 | 用途 |
|---|---|---|---|
| `complete-stock` | full | 11 节(含 `industry`,最重) | 全面深研(旧,保留兼容) |
| `company` | full | 8 节(财务/估值/行业/风险) | **公司研报**(P9-4 拆分后) |
| `stock` | express | 7 节(行情/量价/换手/形态) | **个股分析**(P9-4 拆分后) |
| `short-term` | express | 7 节(仅 2 维评分,不含 risk) | 短线视角 |
| `prebuy` | express | 去 industry、保留全 6 维评分卡 | **买入前速评**(P7) |
| `industry` | — | 申万行业 | 行业研报(P9-3) |
| `macro` | — | 宏观四维 | 宏观研报(P9-5) |

> ⚠️ **行为由 `sections` 驱动,`mode` 字段不参与分支。**

### 5.6 分析层模块(`research/src/analysis/`)

| 模块 | 职责 |
|---|---|
| `price.py` / `volume.py` | 行情、量价指标(`limit_up_days`/`limit_down_days` 仅计数无日期数组) |
| `patterns.py` | 量价形态规则判定(地量启动/换手峰值见顶/价跌量未缩…),每条带 evidence |
| `financial.py` | 财务快照 |
| `capital.py` | 资金面(龙虎榜) |
| `industry.py` | 申万行业分位/回撤/横截面 |
| `macro.py` | CPI/PPI/M2/GDP 四维(`level_percentile_meaningful` 门控) |
| `risk.py` | 风险规则;**P9-4a 起量价规则可选化**(显式传 `symbol`/`as_of`,无行情时规则 4–7 纯基本面独立运行,规则 1–3 整条跳过不猜不补) |
| `scorecard.py` | 6 维评分卡 + `overall_label` |
| `number_lint.py` | 防幻觉数字机检(见下) |

### 5.7 两层防幻觉

1. **Number Lint**(`number_lint.py`):扫描报告正文,把「不是程序算出来的数字」报为 issue。
   - `(?<!\d)\d{6}(?!\d)(?!\.\d)` 掩码(**6 位股票代码不误判**);
   - `UNIT_SCALES` = 1 / 1e4 / 1e8 单位换算容差;
   - `TEMPLATE_CONSTANTS` 精确匹配;
   - 既往研报数字经 `--prior-metrics` 并入 known 集(让「较上次 +12%」不被误判为编造)。
2. **Quality Gate**(`quality_gate.py`):6 项机检,`gate` 命令输出。
   > 代码实际发 6 项;docstring 曾写 7 项(第 7 项 `no_hallucination` 已由 Number Lint 覆盖)——以**代码为准 = 6 项**。

> **已知技术债**:`_save_artifacts` 从不写 `report_fingerprint`,导致 `run_gate` 的可复现性检查恒短路通过。

---

## 6. Go 后端

### 6.1 包结构(`internal/`,12 个包)

| 包 | 文件数 | 职责 |
|---|---|---|
| `store/` | 32 | **最大包**:全部 PostgreSQL 存取(表→文件近乎一一对应)+ 迁移 + 事务 |
| `web/` | 21 | HTTP 层:路由表 + 全部 handler(`api_*.go`)+ AI 对话 + 截图导入 |
| `research/` | 9 | 深研编排:状态机 + `os/exec` 调 Python CLI + 产物落库 |
| `collector/` | 14 | 数据源适配:**快讯 6 个独立机构源**(东财/金十/财联社/新浪/同花顺/富途)+ **公告源**(巨潮,issue #50)+ 涨停池 / 行情 |
| `ai/` | 4 | `Provider` 接口 + OpenAI 兼容适配器 + 预算护栏 + 记账 |
| `cluster/` | 4 | 事件去重聚类(含重审视 Pass);跨源判定见 `stripBracketWrap`(issue #48);数值冲突 `conflicts.go`(issue #49) |
| `publish/` | 3 | 渲染层:`daily-review`(市场复盘 md)、`reconcile`(对账 md)、`internal/web/entities.go`(实体卡)共用;vault 写盘已下线,渲染逻辑仍在用 |
| `entityextract/` | 2 | affected 词 → 实体(便宜档批量分类) |
| `marketstate/` | 2 | 市场状态 12 项 + 情绪模型 |
| `extract/` | 1 | LLM 结构化抽取(events) |
| `model/` | 1 | 领域模型 |
| `config/` | 1 | 配置加载 |

### 6.2 路由表(`internal/web/server.go`,28 个 handler)

Go **只提供 JSON API,不渲染任何 HTML**(全代码无 `html/template`/`text/template` import)。

**旧 API(4 条,`/api/*`,无 CORS 头)**:图谱点选、事件/实体卡片、附件回显。

**`/api/v1` 只读投影 + 写接口(24 条)**:

| 路径 | 方法 | 用途 |
|---|---|---|
| `/events` | GET | 事件流(`type`/`status`/`q`/`from`/`to`;`source_count` 机构计数 + 跨源簇附 `cluster_sources[]` + 冲突附 `event_conflicts[]`,issue #48/#49) |
| `/entities` | GET | 实体(`type`/`q`/`status`) |
| `/watchlist` | GET | 自选聚合(首页数据源,含覆盖率) |
| `/relationships` | GET | 关系 |
| `/market/snapshot` | GET | 市场状态快照(`date`) |
| `/flashes` | GET | 快讯(`q`/`source`) |
| `/notes`,`/notes/` | GET/PUT/DELETE/POST | 个人笔记 |
| `/dashboard` | GET | 看板 |
| `/recon` | GET | 对账 |
| `/reviews` | GET | 持仓诊断(`?snapshot=`) |
| `/account` | GET | 账户资金 |
| `/trades`,`/trades/` | GET/POST | 交易与持仓(含 `based_on` 决策边) |
| `/trades/import`,`/trades/confirm` | POST | 截图导入两段式 |
| `/stock/` | GET | 个股中心聚合(`?code`) |
| `/chat`,`/chat/clear` | GET/POST | AI 对话 |
| `/settings`,`/settings/form` | GET/POST | AI 配置 |
| `/weekly`,`/weekly/detail`,`/weekly/generate` | GET/POST | 周报 + AI 综述 |
| `/research-runs`,`/research-runs/` | GET/POST | 深研列表/触发/单份 |

**CORS**:仅对 `/api/v1/` 前缀加 `Access-Control-Allow-Origin: *`(无鉴权个人系统),`OPTIONS` 兜底。

**数组字段空态编码为 `[]`,永不 `null`**(issue #59):
Go 的零值 slice 经 `encoding/json` 会序列化成 `null`,而前端 `lib/types.ts` 把
`indices`/`ladder`/`snap_history`/`top_events`/`task_runs`/`facts`/`aliases` 等一律声明为
**非可选数组**并直接 `.map()` —— 契约不一致会在空库时整页白屏(issue #59 即此,
2026-09-20 清库后首页白屏)。故**每个返回数组的 handler 都必须显式初始化空 slice**
(如 `apiDashboard{SnapHistory: []apiSnapRow{}, …}`、`emptyMarketSnapshot()`),
`json.Unmarshal` 结果也要 `if x == nil { x = []T{} }` 兜底。
回归测试:`internal/web/api_dashboard_empty_test.go`(断言**真实 JSON 编码结果**,非 Go 层长度)
+ `api_dashboard_integration_test.go`(临时库跑真实 handler —— 缺陷在「`len(snaps)==0`
这条从未被走到的分支」上,只有真库能到达)。
⚠️ 唯一合法的 `null` 是**语义上真的缺席**的**指针**字段 —— 如 `apiTrades.Account`
(无账户快照时 `null`,表示「截图里没这个数」,区别于 `0`),以及 `apiStockHub.Entity`
等;这些在 `types.ts` 里本就是可空类型。

### 6.3 命令(`cmd/`,13 个)

| 类别 | 命令 |
|---|---|
| **管线(9)** | `migrate` `collector` `worker` `cluster` `quote-collector` `entity-build` `market-state` `daily-review` `reconcile`(`collector` 一次运行内跑**快讯 6 源 + 公告 1 源**,故命令数不变) |
| **常驻** | `web` |
| **运维** | `probe`(探针) |
| **深研** | `research-run`(CLI 编排入口) |

各命令**幂等、可单独重跑**;失败步骤记录不阻断(下次重试)。

### 6.4 配置面

**环境变量**(`internal/config/config.go` 只读 3 个):

| 变量 | 默认 | 说明 |
|---|---|---|
| `PIKS_DATABASE_URL` | `postgres://piks:piks_dev_password@localhost:5433/piks?sslmode=disable` | 数据库 DSN |
| `PIKS_VAULT_PATH` | `""` | 历史遗留 |
| `PIKS_UPLOAD_DIR` | `data/uploads` | 附件落盘目录 |

其余由各调用点直接读(`PIKS_LISTEN_ADDR` / `PIKS_RESEARCH_DIR` / `PIKS_RESEARCH_OUT_DIR` /
`PIKS_PYTHON_BIN` / `PIKS_AI_PROVIDER` / `PIKS_VAULT_REMOTE` / `PIKS_VAULT_GIT_NAME` /
`PIKS_VAULT_GIT_EMAIL` / `PIKS_GIT_SHORT`)。

> **AI 配置不读环境变量**:权威源 = `app_config` 表(`ai_service_base_url` / `ai_api_key` /
> `ai_model_extract` / `ai_model_reasoning` / `ai_model_vision` / `ai_daily_token_budget`),
> 启动时 `ApplyAppConfig` 合并,`/settings` 页面可编辑。
> `PIKS_VERSION` **不被任何 Go 代码读取**——只由 Dockerfile `ENV` 烘焙、`deploy.sh` 传入。

---

## 7. React 前端

### 7.1 路由表(27 条,`frontend/src/App.tsx`)

单 `BrowserRouter` → 一个 `<ShellLayout>`(渲染 `AppShell` + `<Outlet/>`)包住除 `/m/upload` 外全部页面。

| 路径 | 组件文件 | 说明 |
|---|---|---|
| `/` | `pages/home.tsx` | **今天** = 自选首页 |
| `/market` | `pages/dashboard.tsx` | 市场概况 |
| `/events` | `pages/events.tsx` | 消息(重要) |
| `/events/:id` | `pages/event/[id].tsx` | 事件详情 |
| `/flashes` | `pages/messages.tsx` | 消息(快讯 tab,旧深链) |
| `/ladder` | `pages/ladder.tsx` | 涨停股 |
| `/reports` | `pages/reports.tsx` | 研报列表 |
| `/reports/:runId` | `pages/report/[runId].tsx` | 研报阅读器 |
| `/research` | `pages/analyst.tsx` | 个股分析(触发入口)⚠️ 路由名 ≠ 文件名 |
| `/research/:runId` | `pages/research.tsx` | run 运维仪表盘(1500ms 轮询) |
| `/stock/:code` | `pages/stock/[code].tsx` | **个股中心**(聚合) |
| `/trades` | `pages/trades.tsx` | 交易与持仓 |
| `/reviews` | `pages/reviews.tsx` | 持仓诊断 |
| `/weekly` | `pages/weekly.tsx` | 周报 |
| `/notes`,`/notes/new`,`/notes/:id`,`/notes/:id/edit` | `pages/notes.tsx` 等 | 笔记 |
| `/chat` | `pages/chat.tsx` | 问 AI |
| `/settings` | `pages/settings.tsx` | 设置(仅 22 行,承载组件) |
| `/entities`,`/entities/:id` | `pages/entities.tsx` | 实体库(侧栏隐藏,重定向 `?id=`) |
| `/graph` | `pages/graph.tsx` | 图谱(侧栏隐藏) |
| `/recon` | `pages/recon.tsx` | 对账(侧栏隐藏) |
| `/help` | `pages/help.tsx` | 使用指南(纯静态) |
| `/m`,`/m/upload` | `pages/m/upload.tsx` | 手机投递页(layout route 外,不漏侧栏) |
| `*` | — | `Navigate to="/"` |

`/entities` `/graph` `/recon` 三条**移出侧栏**,唯一应用内入口是
`components/settings/OpsCard.tsx`(设置页「数据与运维」卡)。

### 7.2 导航骨架

`components/layout/navItems.ts`(单一真源,**12 项**):

- **今天** `/`(Sun,置顶不归组)
- **发现**:市场概况 `/market` · 涨停股 `/ladder` · 消息 `/events`
- **研究**:研报 `/reports` · 个股分析 `/research` · 问 AI `/chat`
- **交易**:交易与持仓 `/trades`
- **复盘**:持仓诊断 `/reviews` · 周报 `/weekly` · 笔记 `/notes`
- **系统**:设置 `/settings`

分组仅视觉分隔、不可折叠;标签一律白话。

### 7.3 数据获取层

- `lib/api.ts`:`API_BASE = import.meta.env.VITE_API_BASE_URL || "/api/v1"`(默认相对路径,走网关)。
  `request` 只做 `fetch`,失败尽力读 `body.error`;**无重试、无超时、无演示数据兜底**。
  **分页是纯客户端**(`usePagedQuery` 切数组),API 不收 `page`/`size`。
- `hooks/useData.ts`:唯一 fetch 原语(`AbortController` 清理,`AbortError` 静默)。
- `hooks/useResearchRun.ts`:`profile` **必填**——隐式默认会静默造出遗留 `complete-stock` run。
  状态标签与 Go 侧 `ActiveResearchStatuses` 镜像。
- `hooks/usePrebuy.ts`:`PROFILE="prebuy"`,触发 `{code, profile, quick:true}` 后 2000ms 轮询。
- 三态纪律:统一用 `components/ui/States.tsx` 的 `LoadingBlock`/`ErrorState`/`EmptyState`。
- **数组字段无防御性守卫**(issue #59):`types.ts` 把服务端数组一律声明为**非可选**,
  组件**直接 `.map()`/`.length`/`.filter`**(如 `WatchOverview.tsx:29` 的 `m.indices.map`),
  不写 `?? []`。这是**刻意的契约约定**:后端保证永不返回 `null`(见 §6.2),
  双方以「数组恒存在」为契约;代价是任何一处后端漏给空 slice 都会**整页白屏**。
  `?? []` 兜底被列为独立加固项,不在 issue #59 范围内。

### 7.4 样式系统

`frontend/src/globals.css`(2,091 行)是**唯一视觉源**;Tailwind 只用于布局(`flex`/`grid`/`gap`/`px`/断点)。

- **3 档圆角 token**:`--radius-card 16px` / `--radius-md 10px` / `--radius-sm 6px`;
  Tailwind `borderRadius: { sm:6, DEFAULT:10, lg:16 }`,**禁魔法值**。
- **A 股涨红跌绿**:`--red` 涨 / `--green` 跌;Tailwind 映射 `up: var(--red)` `down: var(--green)`。
- 暗色:`[data-theme="dark"]` 覆盖**每一个** token;`index.html` 内联脚本读 `localStorage["piks-theme"]`
  在首屏前应用,**无闪白**。
- 图表主题:`lib/chartTheme.ts` 用 `getComputedStyle` 读 token(canvas 读不到 CSS 变量),
  `MutationObserver` 监听 `data-theme` 变更重解析。
- 力导图节点色直接把 token 注入 SVG `fill`,切主题零 JS 重绘。

### 7.5 构建

- `vite.config.ts`:**dev 端口 3100**;代理单条 `"/api" → http://localhost:8090`;alias `@`。
  **无 `build.rollupOptions` / `manualChunks`**(即无分包,与部分文档所述不符)。
- `package.json`:`dev` = `vite --port 3100`;`lint` = `tsc --noEmit`;`e2e` = `node scripts/e2e_check.mjs`(Playwright 驱动)。
- `tsconfig.json`:strict,但**未开** `noUnusedLocals` / `noUncheckedIndexedAccess`。

---

## 8. 端到端数据流

### 8.1 日管线(9 命令,cron 驱动;`collector` 内含快讯 6 源 + 公告 1 源)

```
migrate → collector -driver all(快讯 6 机构源) → collector -driver cninfo-announce(巨潮公告) → worker -limit 300(AI 抽取 events) → cluster(语义去重 + 重审视 Pass,取全部未聚类事件)
→ quote-collector(涨停池,仅交易日) → entity-build(实体) → market-state(市场情绪)
→ daily-review(每日复盘) → reconcile(对账)
```

**公告(issue #50)**:`collector -driver cninfo-announce` 跑巨潮全市场个股公告(~1200 条/日),
落 `source_type='announcement'` / **`status='collected'`** —— **不进 LLM 抽取**
(worker 只取 `status='raw'`),也不报对账异常(`processed_no_event` 只看 `status='processed'`)。
前端在消息页「公告」tab 呈现(`GET /api/v1/announcements`);快讯投影已显式排除 announcement。
**只存标题 + 原文 PDF 外链,不存正文**(正文抓取实测被上游 IP 级限流,见
`docs/phase11/design/announcement-source.md` §3)。

**聚类候选池(issue #53)**:`cmd/cluster` 的 `-limit` 默认 **0 = 不限**(取全部未聚类事件)。
此前默认 `-limit 100` + `ORDER BY created_at ASC` 只取最旧 100 条,超出部分**静默丢弃**
(`task_runs` 仍记成功)→ 积压时新事件永远轮不到,并被 `source_count` 语义误标成「单一来源」。
显式传 `-limit>0` 时用 `store.UnclusteredEventsTruncated` 探测并把 `limit`/`truncated`
记进 `task_runs.meta` + 打 WARN。

**事件类多源(issue #43 T1)**:`collector -driver all` 依次跑 6 个**独立机构**源
(东方财富 / 金十数据 / 财联社 / 新浪财经 / 同花顺 / 富途资讯),每源落各自 `sources` 行
(name = **机构名**);单源失败**不阻断**其余源(免费源无 SLA)。各源上游原始字段
(`reading_num` / `important` / `data.source` / `like_nums` …)原样落 `raw_documents.extra`。
⚠️ 同机构不同端点不算独立源 —— Go 东财与 akshare `stock_info_global_em` 同为东财,只能算一个。

**跨源同事件判定(issue #43 T2 / #48)**:cluster 跑在 **`events`(抽取后)** 上,不是
`raw_documents` 上 —— 多源先在抽取层各自成 event,再由 cluster 合并。跨源标题写法分两类
(东财/新浪 = `【标题】`+公告正文;财联社/金十/同花顺/富途 = 裸标题),原句全文比对会把
**同一事件**的相似度稀释到阈值下(实测东财 vs 财联社同一天山生物减持 = 0.165)。
故 `NormalizeTitle` 前置 `stripBracketWrap` **抽取 `【】` 内嵌标题前缀**(≥6 字才当标题,
否则视为栏目标签)→ 相似度还原,**阈值 0.7 不动**(红线「扩候选池不降门槛」;实测降门槛
不增加专属召回)。簇内来源经 `store.ListClusterSources` 读回(**含 `merged` 成员**),
`/api/v1/events` **仅跨源簇(≥2 机构)** 下发 `cluster_sources`(机构名 + 原文链接 +
上游一级源 `extra->>'source'`),前端事件详情/表格展示「N 家印证」。**零 schema**。
详见 `docs/phase11/design/event-cross-source.md`。

**漏报 / 冲突检测(issue #43 T3 / #49)**:同一读路径再补两件事 ——

- **单源可见**:新增 `source_count`(**机构级**去重计数,未聚类 = 1)。⚠️ `cluster_sources` 是
  `omitempty`,**单源与未聚类都不下发**,光看它分不清「就 1 家」和「还没聚类」,故必须用显式计数。
  `=1` 前端标「**单一来源**」(中性措辞,**不说可疑** —— 独家报道很常见;绝不丢弃任何事件)。
- **冲突可见**:`internal/cluster/conflicts.go` 纯规则数值比对 —— 单位白名单锚定数字,
  骨架 Jaccard(只删数字**不删汉字**,否则「回购 vs 增持」误报)≥ **0.6** 才配同一句,
  两侧各恰好 1 个不同值才报。**留双方原文**(`sentence_a`/`sentence_b`),前端两条都渲染,
  **禁止静默择一**(Fact ≠ Inference)。门控按生产 1410 条真实事实句标定(随机句对误报 **0.0750%**,
  同标题重复组 **0 差异**,对抗集 **0/4**);远改写召回的已知边界如实登记,**留给 #45**。
  **零 schema**(读路径派生,与 `cluster_sources` 同范式)。

`scripts/pipeline.sh` 的**闸门**:
- **日期锚点取 GitHub `Date` 响应的 `Date` 头**(免疫宿主机时钟漂移),取不到才回退系统时钟并打显式 WARN;
- 周末跳过;早于 16:10(北京时间)跳过;当日 `pipeline-$TODAY.done` 已存在则跳过(防重跑);
- 任一步失败 → **记录不阻断**。

### 8.2 深研 run 状态机(2026-09-20 起为 DB 队列驱动)

```
pending → gathering → synthesizing → verifying → done | failed
```

- Go `internal/research/runner.go` 用 `os/exec python3` 调 CLI 三命令,**argv 数组不经 shell**;
- **触发入口两种**(UI 与 CLI):
  - **UI 按钮**(`POST /api/v1/research-runs`):web 只做三件事 —— 建 `pending` 行
    (`CreateResearchRun`)、`NOTIFY piks_research_pending`、返回 `{run_id,status}`
    (响应形状与拆分前逐字不变)。**执行在 research 容器的常驻 worker**,不在 web 进程内。
  - **CLI 旁路**(`docker compose exec research ./bin/research-run 000560`):绕过队列直接跑,
    供排障/回填。
- **队列实现**(issue #47 拆分引入,零 schema 变更语义 + 迁移 0015 两列):
  - `internal/store/research_runs.go` `ClaimPendingResearchRun`:`UPDATE … WHERE run_id =
    (SELECT … WHERE status='pending' ORDER BY created_at LIMIT 1 FOR UPDATE SKIP LOCKED)`
    —— 单条语句原子领取,`SKIP LOCKED` 天然支持多副本;**并发安全**。
  - `cmd/research-worker`:`LISTEN/NOTIFY` 加速(实测 `-poll 60s` 下通知同秒唤醒)+
    轮询兜底(通知丢失不影响正确性)。`SIGTERM` 只停**认领**,在跑的 run 用
    `context.Background()+TimeoutTotal` 跑完(compose `stop_grace_period 7m`)。
  - **孤儿回收**:`ReapStuckResearchRuns(grace)` 回收 `updated_at` 超期(grace =
    `TimeoutTotal + 2min` = 8min)的进行中 run → `failed`,诚实地登记中断而非永久卡住。
  - ⚠️ 旧 `researchRunStale` 启发式已删 —— web 不再判断谁在跑(它无从知道),收口归 worker 的 reaper。
- **运行参数随行落库**(迁移 0015 的 `quick`/`days` 两列):worker 只拿得到 `research_runs`
  这一行,不落库则认领后无从还原 `RequireSynthesis`(quick)与回溯天数。
- **危险点**:`runner.go` 的 argv 传参不得改成 `sh -c`,否则 `macro:cn_cpi` 的冒号有注入面。

### 8.3 截图导入两段式(交易 / 持仓 / 自选)

```
上传图 → /api/v1/trades/import(vision 抽取)→ 前端可编辑预览 → /api/v1/trades/confirm(确认入库)
```

- `kind ∈ {trade, position, watchlist}`;自选为**镜像语义**(服务端 diff `add`/`keep`/`remove`)。
- 预表格中**空字段 = 截图未拍到 → 确认时写 NULL,不写 0**(宁缺毋假)。
- 客户端降采样(`lib/image.ts`,长边 2000px,JPEG q0.85)以适配 10MB 上传上限。

### 8.4 个人回写(唯一写路径)

笔记 / 周报 / 交易 / 设置 / AI 对话全部经 `/api/v1` JSON 写接口
(`internal/web/api_write.go`);**无 vault、无 git 中间层**——落回 PostgreSQL 即为终点。

---

## 9. 部署拓扑

### 9.1 四镜像(2026-09-20 定,取代 2026-09-12 单镜像 D-2)

单 `Dockerfile`(多 target,而非四个文件 —— Go 与前端构建阶段在四镜像间完全共享):

| 阶段 | 底座 | 内容 |
|---|---|---|
| `build` | `golang:1.26-alpine` | `go build ./cmd/...` → `/out/bin/`(`-s -w` 去符号表,-trimpath) |
| `frontend` | `node:20-alpine` | `npm run build` → `dist/` |
| `gateway` | `nginx:alpine` | `nginx.conf` + `--from=frontend dist` |
| `web` | `alpine:3.20` | `--from=build bin/web` |
| `tools` | `alpine:3.20` | 9 个管线命令(逐条 COPY)+ `migrations/` + `prompts/` |
| `research` | `python:3.12-slim` | pip 装 `research/requirements.txt` + `research/` + `research-run` + `research-worker` |

```bash
docker build --target gateway  -t piks-gateway:latest  .
docker build --target web      -t piks-web:latest      .
docker build --target tools    -t piks-tools:latest    .
docker build --target research -t piks-research:latest .
```

- **cache 击穿已修**:`build` 阶段只 `COPY cmd/ internal/ go.mod go.sum`(**不含 `frontend/`**)。
  原先 `COPY . .` 让改一行 `frontend/src/main.tsx` 就击穿 Go 层、全部二进制重编(实测 27s)。
- **research 必须留 debian slim**:akshare/pandas 无法在 musl 上构建,反向「nginx:alpine 上
  apk add python3」不可行。
- **tools 逐条 COPY 9 个命令**,不用 `COPY --from=build /out/bin/ /app/bin/`:后者会把
  `web`/`research-run`/`research-worker`/`probe` 也塞进 tools,既白占体积又让误用成为可能。
- 构建参数:`GOPROXY` · `APT_MIRROR` · `PYPI_INDEX`(deploy.sh 默认清华源,见下)· `GIT_SHORT` ·
  `PIKS_VERSION`(默认 `v0.0.0`)。各镜像烘焙 `PIKS_GIT_SHORT` + `PIKS_VERSION` +
  **`PIKS_IMAGE_ROLE`**(`gateway`/`web`/`tools`/`research`,供自证是哪个镜像)。
  ⚠️ `ARG GIT_SHORT`/`ARG PIKS_VERSION` 必须声明在**首个 FROM 之前**(全局作用域)——
  在阶段内声明的 ARG 只对该阶段可见,后续阶段取不到它的默认值。
- ⚠️ **PyPI 源默认清华,不用 aliyun**(issue #57,2026-09-20):`deploy.sh` 的 `PYPI_INDEX`
  默认 `https://pypi.tuna.tsinghua.edu.cn/simple`。实测 aliyun 源**对本机**只有 ~20 KB/s
  (同机 curl 命中其 CDN 可达 4 MB/s,但 pip 的连接恒落坏节点;换 UA 无效),清华源同机同时刻
  **5.58 MB/s**(低 280 倍)。**这是取数链路的环境事实,非永久结论** —— 换线路后应重测。
- ⚠️ **`ARG GIT_SHORT`/`ENV PIKS_GIT_SHORT` 必须排在 `pip install` 之后**(research 阶段):
  它是**每次提交都变**的元数据,若排在依赖安装之前,则每次部署都让 pip 层缓存失效、重下
  全部依赖(实测把构建拖到数十分钟)。移到之后 → 只要 `requirements.txt` 不变,pip 层永久
  命中缓存(实测 `GIT_SHORT` 由 `test` 改 `zzz9` 重建,pip 层 `Using cache`、层 ID 不变)。
  其余三 stage 的 `ENV` 之后只有 `COPY`(无网络操作),故无此问题。
- ⚠️ legacy builder(本机 Docker 29,无 buildx)支持 `--target`,但**无 `--mount=type=cache`**:
  Go 阶段源码变动时仍会重编,本期只消除「前端改动击穿 Go 层」。**禁写 `# syntax=` 指令**
  (需 BuildKit,会硬失败)。

**`scripts/check-image-topology.sh`** 在 CI 断言这张表:四 target 存在、底座正确、只有
research 装 Python、tools 逐条 9 命令(拒绝 bulk COPY)、dist 只进 gateway、web 不含 nginx/dist。

### 9.2 运行时组成

`configs/docker-compose.prod.yml` 五服务:

| 服务 | 镜像 | 说明 |
|---|---|---|
| `postgres` | `postgres:16-alpine` | :5433 **loopback** |
| `gateway` | `piks-gateway` | **唯一对外**:`0.0.0.0:8090:80`;`depends_on web(service_healthy)` |
| `web` | `piks-web` | **不发布宿主端口**(私网内 `web:8090`);`piks_data:/data`;healthcheck 探 `/api/v1/dashboard` |
| `research` | `piks-research` | 常驻 worker;`stop_grace_period 7m`(> `TimeoutTotal`,不 SIGKILL 在跑的 run);与 web 共享 `piks_data` |
| `tools` | `piks-tools` | `profile=run`,user `1000:1000`,跑管线命令 |

环境:`TZ=Asia/Shanghai`;`SSL_CERT_DIR=/etc/ssl/certs:/certs`(自签 AI 网关 CA);
卷 `piks_data`(web=`/data/uploads`、research=`/data/research`,**子目录不重叠**) + `./certs:ro`。

> 安全性不退化:web 只是「Docker 私网 `piks` 内可路由」(gateway 经 `web:8090` 反代),
> **未发布宿主端口**,对外仍只有 gateway 一个入口。

### 9.3 nginx(`configs/nginx.conf`)

单 server :80;`client_max_body_size 10m`;gzip;`/` → `try_files … /index.html`(SPA fallback);
`/api/` → 反代 **`http://web:8090`**。

> ⚠️ **`resolver` + 变量式 `proxy_pass` 是必须的,不是优化**:nginx 对 `proxy_pass` 里的字面
> 主机名只在 worker 启动时解析一次并缓存到进程结束;web 每次重建都换 IP,于是 `deploy.sh`
> 每跑一次 `up -d web`,gateway 就会 502 到自己被重启。改用 Docker 内嵌 DNS(`127.0.0.11`)
> + `set $piks_web "http://web:8090"; proxy_pass $piks_web$request_uri;` 才会按 `valid=` 重解析。
> ⚠️ 变量式 `proxy_pass` **不会自动带原 URI**,必须显式拼 `$request_uri`,否则 `/api/v1/x` 会请求成 `/`。

### 9.4 部署流程(`scripts/deploy.sh`)

```
每镜像 VER 全局唯一(git describe --tags --exact-match || v0.0.0)
     版本 hash 由该镜像**自己的输入集**派生:
       前端 = 整个 frontend/ 的 tree;Go = `go list -deps` 的真实依赖闭包;
       四处均含整文件 `Dockerfile`
build --target <role>(tag 已存在则跳过)→ save|load(仅传 lab 上缺的 tag)
     → scp compose + scp lab 侧 scripts/ → up -d postgres → run --rm tools ./bin/migrate
     → up -d web research → up -d gateway → 落 stack 清单
```

- **按镜像判定重建/传输**:改前端只改 gateway 的版本 hash,web/tools/research 的 tag 不变
  → 既不重建也不传输。这正是拆分要换来的东西(代价见下)。
- ⚠️ **输入集必须覆盖该镜像构建真正读的每个文件**:少算 = 「改了文件、tag 不动、不重建不传输,
  生产跑旧像」的**静默错误**(比多算严重)。故 Go 用 `go list -deps` 取真实闭包而非手列目录
  (实施中实测漏过 `internal/model`);前端整树覆盖。
  ⚠️ **四处输入集都含 `Dockerfile` 整文件**(issue #55 补):此前 Dockerfile **不在任何输入集里** ——
  改了 Dockerfile 也不改 tag → `build_image` 判定「已存在、跳过构建」→ 修好的镜像既不重建也不传输,
  生产静默跑旧像,**修复本身失效**。取整文件 hash(改注释也触发重建)是**故意的**:方向安全
  (宁可多重建,不可漏),且 Dockerfile 极少改动。
- **干净树门控**:镜像从**工作树**构建却以**版本 tag** 命名,工作树脏时 tag 与内容不符。
  默认拒绝;确需调试用 `PIKS_ALLOW_DIRTY=1`(tag 附 `-dirty`)。
- **顺序是硬约束**:
  1. migrate **必须先于 web** —— `cmd/web/main.go` 启动即读 `app_config`,缺表会 fatal 崩溃循环;
  2. gateway **必须最后** —— 否则 nginx 会短暂反代半启动的 web(compose 的
     `depends_on: web(service_healthy)` 是第二道保险)。
- **未发版恒 `v0.0.0`**;正式发版才在 master 打语义 tag。每次部署**保留回滚镜像**
  `<name>:rollback-pre-<阶段>`(拆分前单镜像另存 `<name>:rollback-pre-<阶段>-single`)。
  ⚠️ **快照只打一次、存在即跳过**:回滚点是一次性的,无脑 `docker tag latest rollback-pre-X`
  会在第二次部署时把它静默改写成新镜像(标签与内容不符);`-single` 从已知拆分前 tag
  (`PIKS_PRE_SPLIT_REF`,默认 `piks-tools:v0.0.0-b996864`)取而非 `latest`。
- lab(192.168.0.202)**不保留代码仓库,镜像 = 唯一交付物**,编排全在 dev 侧。
- ⚠️ **`scripts/` 随部署同步**(issue #49 补):此前只 `scp` compose,lab 侧 `scripts/` **从不同步** ——
  实测 lab 的 `pipeline.sh` 长期停在 `-driver dongcai`(仓库早已 `-driver all` 六机构源),
  **编排与代码版本脱钩,6 源一条没采**。现 deploy.sh 一并 `scp` lab 上运行的脚本
  (`pipeline.sh`/`backup.sh`/`health.sh`/`setup.sh`,dev 侧 `check-*`/`fix-*` 不上 lab)并 `chmod +x`。
- **stack 清单**(`/home/rguo/piks/stack-manifest.json`):记录本次上线的四镜像 tag + commit,
  是「生产在跑什么」的单一答案。
- ⚠️ **拆分不减总量**:四镜像合计 `docker save` ~214MB(gateway 25 / web 8 / tools 42 /
  research 139),与旧单镜像 ~216MB 基本持平。**收益是升级半径,不是流量** —— 但按镜像传输后,
  改前端只传 **25MB**(gateway)而非整镜像 216MB;改 Go 只传 50MB(web+tools);改 Python 只传 139MB。
  如实如此,不夸大为「省流量」。

---

## 10. 强制约束速查(改代码前先看)

| # | 约束 | 落点 |
|---|---|---|
| 1 | 涨红跌绿(A 股) | `--red`/`--green` token,禁欧美配色 |
| 2 | 数字等宽、右对齐、tabular-nums | `.num` / `.num-t` |
| 3 | 图标用 Lucide 线性,禁 emoji | 全站;**唯一例外** `pages/chat.tsx:55` 错误气泡用了 ⚠️(待修) |
| 4 | 非对称栅格,禁 3 栏等宽 | `.two-col` = 7fr/5fr |
| 5 | 组件单文件 ≤150 行,JSX >80 行拆子组件 | 见 §11 违例清单 |
| 6 | 函数逻辑 >50 行抽 custom hook | `hooks/` |
| 7 | 筛选态反映到 URL query | 见 §11 违例清单 |
| 8 | 全局 ⌘K 命令面板 | `CommandPalette.tsx` + `AppShell.tsx` 监听 |
| 9 | 异步操作处理 loading/error/empty | `useData` + `States.tsx` |
| 10 | 禁紫粉渐变、playful 字体、AI 套话 | — |
| 11 | 圆角只用 3 档 token,禁魔法值 | 见 §11 违例清单 |
| 12 | **图谱隔离**:新增决策边同步 `graphRelFilter` | `internal/store/relationships.go` |
| 13 | `UpsertEntity` 空 `Status` = **保持既有** | 若当 `active` 会清空自选 |
| 14 | `to_id` 用主键 UUID,不用 `run_id` TEXT | 无 FK,接错静默悬空 |
| 15 | research 隔离:Go/前端不得引用 research 源码 | `check-research-isolation.sh` |
| 16 | 前端用户文案禁黑话(`quote-collector`/`crontab`/`幂等`/`reconcile`/`Fact ≠ Inference`…) | 用白话,或经 `lib/glossary.ts` 解释 |
| 17 | **深研执行只在 research 容器**:web 只建 pending 行 + NOTIFY | web 无 python3;新增 `os/exec` 须过 `check-research-isolation.sh` 白名单 |
| 18 | 四镜像内容边界(web 无 nginx/Python、tools 逐条 9 命令、dist 只进 gateway) | `check-image-topology.sh` |
| 19 | **API 数组空态编码为 `[]`,禁 `null`** | 零值 slice 会被编码成 `null` 致前端白屏(issue #59);指针字段的缺席仍用 `null` |

---

## 11. 现状偏差与技术债(代码 vs 文档)

> 本节是本次通读代码的审计结论。**修文档时以本节为准。**

### 11.1 已确认失效的文档陈述

| 位置 | 原文 | 代码事实 |
|---|---|---|
| `README.md` / `docs/项目详解.md` / `PIKS架构设计文档.md:27` | 「13 个前向迁移(0001~0013)」 | **16 个(0001~0016)**,新增 `0014_raw_document_extra`、`0015_research_run_queue`、`0016_raw_document_dedup`(均已修) |
| `README.md:25,34` / `docs/项目详解.md:51` | 「Web(PG 直渲 HTML)」「Go html/template」 | Go **不渲染任何 HTML**,纯 JSON API;界面是 React SPA |
| `docs/项目详解.md:58` | 「仍不做:SPA 框架」 | 前端**就是** SPA(Vite + React) |
| `PIKS架构设计文档.md:25` | 「schema 已演进至 11 个前向迁移」 | 15 个 |
| `frontend/README.md`(全文) | 描述 Next.js 14 / React Flow / TanStack Table / Framer Motion / `NEXT_PUBLIC_API_BASE_URL` / `/docs` 页 | **全部已删除**。实际是 Vite 5 + React Router + 自绘 SVG 图谱 + ECharts + 原生 table,**无** Next.js/React Flow/TanStack/Framer Motion |
| `docs/phase9/design/README.md:40` | P9-5 宏观研报「⏳ 草案待定稿」 | **已实现并合并**(commits `344df75`→`609fffa`,PR #28) |
| `research/README.md:28` | CLI `--profile complete-stock\|short-term\|prebuy\|index\|macro` | 实际 7 个:`complete-stock`/`company`/`stock`/`short-term`/`prebuy`/`industry`/`macro`;**无 `index`** |
| `research/src/quality_gate.py` docstring | 7 项机检 | 代码实际发 **6 项** |
| `docs/进度总表.md:92` | D-2 = 同仓双镜像 | **已改为四镜像**(2026-09-20 拆分,issue #47) |

### 11.2 代码内的规范违例(非文档问题,登记待修)

| 规则 | 违例 |
|---|---|
| 组件 ≤150 行 | `components/note/NoteForm.tsx`(200)、`components/layout/CommandPalette.tsx`(152);页面另有 `entities.tsx`(209)、`weekly.tsx`(190)、`trades.tsx`(157) |
| 圆角只用 3 token | **45 处** `rounded-[…]`;其中 39 处是字面 `rounded-[10px]`(= DEFAULT token,应用 `rounded`) |
| 筛选态入 URL | `pages/graph.tsx:21-23` 的 `type`/`focusId`/`kw` 用局部 `useState`,图谱筛选**不可分享** |
| 禁 emoji | `pages/chat.tsx:55` 错误气泡前缀 ⚠️(用户可见) |
| 三态区分 | `components/trades/PosReview.tsx`、`pages/reviews.tsx` 成功与失败信息同走 `text-faint`,**视觉不区分** |
| 组件内联色 | 多处 `style={{color:"var(--red)"}}` 绕过语义类(`TradeAddForm`/`WatchOverview`/`Sections`/`PositionTable` 等) |
| 无死代码检查 | `tsconfig.json` 未开 `noUnusedLocals` |

### 11.3 结构性观察

- **前端无代码分割**:`React.lazy` 零命中,全部路由静态 import,首屏 bundle 偏大。
- **118 个文件顶部 `"use client"` 是惰性残留**(纯 Vite 构建,无 RSC 运行时),可整批清理。
- **力导图 O(n²)**:无 Barnes–Hut,节点规模大时会退化。
- Number Lint 的 `report_fingerprint` 未写入 → gate 可复现性检查恒通过(§5.7)。

### 11.4 文档体系现状(应保留的约定)

| 文档 | 定位 |
|---|---|
| `CLAUDE.md`(根) | 前端重构规范 + 分支/发布纪律(**权威**) |
| `README.md` | 仓库门面 + 快速开始 |
| `PIKS架构设计文档.md` | v1.0 冻结蓝图(**不改正文**,只更新顶部现状偏差注记) |
| `docs/架构总览.md` | **本文**:以代码为准的完整现状架构 |
| `docs/数据源总览.md` | 全部取数链路清单(数据种类→来源→代码→频率);缺口登记与扩展入口 |
| `docs/项目详解.md` | 执行决策登记处(架构文档之外的决策) |
| `docs/进度总表.md` | 里程碑 + 子阶段明细 + 契约缺口 + 已知遗留 |
| `docs/phaseN/{design,stages}/` | 各阶段设计定稿 + 实现归档;已完成阶段冻结只读 |
| `research/README.md` | D-11 契约表(被 CI 脚本读取) |
| `frontend/README.md` | 前端说明(待重写) |

---

## 12. 边界(明确不做)

- **不自动交易 / 不荐股 / 不发买卖信号 / 不接资金 / 不做高频行情**。
  交易功能只做「记录 + 解读 + 诊断」——不连券商、不下单、不做信号。
- 与 steady **领域耦合**:不共享表、不互相调用,DB 级完全隔离(独立 Postgres 实例)。
- 不上:队列框架 / Elasticsearch / Neo4j / Kubernetes / 微服务 / 多 AI Agent / 十几个数据源。
  任务调度 = cron + PG 任务表。

---

*本文由通读全仓代码(Go 82 文件 / Python research 源码 / React 135 文件 / 13 迁移 / 全部脚本与编排)整理,事实以 2026-09-20 dev `84b3b49` 为准。*
