# PIKS 进度总表

> 全局里程碑跟踪,贯穿所有阶段。每完成一个交付物在此登记,作为续接的唯一依据(不靠记忆)。
> 目录约定:已完成阶段冻结只读;进行中阶段可写;阶段切换时重构 `docs/phaseN/`。
> **以代码为准的完整现状架构见 `docs/架构总览.md`**(三运行时 / 数据层 / API / 前端 / 部署 / 约束速查 / 文档偏差审计)。
> **数据源清册见 `docs/数据源总览.md`**(数据种类→来源→代码→频率;事件类单源缺口与扩展入口 issue #43)。

## 阶段总览

| 阶段 | 定位 | 状态 | 交付物 |
|---|---|---|---|
| 迭代 0 | 地基:最小闭环 | ✅ | 新闻→Event 抽取→Markdown→Obsidian 全链跑通(7 张表 + Go 骨架 + 采集/抽取/发布,mock 验收全过) |
| 迭代 1 | 可靠性 | ✅ | Event 去重聚类(仅 canonical 发布)、任务重试幂等、raw↔event 对账、增量发布、G1 东财探针+真实驱动 |
| 迭代 2 | 增值:市场情报 | ✅ | 行情数据→observations、市场状态 12 项、情绪模型、每日复盘聚合页(全链真实运行,AI 调用=0) |
| 迭代 3 | 增值:实体补全 | ✅ | entities 单表 + 实体构建(种子零 AI + affected 便宜档分类)+ 实体卡 + affected wikilink + hot_topics 补链 |
| 迭代 4 | 个人学习闭环 | ✅ | Belief / Case / Mistake 笔记类型 + 周报聚合(**设计已定稿 D24~D29;实现并入迭代 5:Web 内编辑 personal_notes 替代 Obsidian harvest,5-2 落地**) |
| 迭代 5 | Web 平台(替换 Obsidian/GitHub 界面层) | ✅ | PG 直渲 Web(看/写/理解/周报/AI 对话)+ vault/GitHub 停更;三阶段全部完成:5-1 只读、5-2 编辑+去界面层、5-3 AI 对话+截图(带引用问答、vision 识别,镜像部署 lab:8090) |
| 生产化 P3 | ✅ | ✅ | lab(192.168.0.202)部署落地:dev 本地编译+镜像传输、postgres+迁移+初始同步、crontab 日管线、vault 推送 GitHub 私有仓、每日备份;验收 §15 十项全过 |
| capacity P4 | ✅ | ✅ | 能力并入:research 个股深研并入 PIKS(`research/` 同仓多运行时 + `cmd/research-run` 编排 + `research_runs` 表 + 深研报告页)+ **Go 移除 vendor**(8.2MB/280 文件 → go.sum + 模块代理);设计定稿冻结(`design/research-merge.md`,D-2=双镜像 / D-11=research 独立迭代);**T1~T7 已实现并验收**(2026-09-12,dev-only,见 `stages/research-merge.md`) |
| 前端 IA P5 | ✅ | ✅ | 前端信息架构重组:从「按后端产物类型平铺」收敛为**个股轴心的个人投研平台**(首页=自选 `/`,`/stock/:code` 个股中心,市场数据迁 `/market`)+ **自选股以同花顺截图镜像同步**(复用交易截图两段式 `kind='watchlist'`);**Phase 1–4 已实现、验收并上生产**(2026-09-13,镜像 `c81c6d2`,migrate 0);已修 `UpsertEntity` 空 Status 地雷(硬验收:跑 entity-build 后 watch 仍在);实现记录见 `design/frontend-ia.md` §7 |
| 重构 P6 | ✅ | ✅ | 前端**决绝重构 + 投研闭环补齐**(用户新需求,2026-09-14):重建呈现层(设计系统 + IA + 页面)→ 专业、简约、清爽、**新手友好**(引导式首页 + 白话导航 + 隐藏管线内部);补齐 epic #1 四段闭环(决策记录 / 复盘→沉淀修复 / 研究覆盖率·自选富化 / 研究 delta),**零 schema**(复用 `relationships` 多态)。**设计已定稿**(`docs/phase6/design/ux-ia.md`)。**P6-1~P6-5 五阶段全部完成**(见 `stages/p6-1-foundation.md`、`p6-2-plain-nav.md`、`p6-3-guided-home.md`、`p6-4-decision-loop.md`、`p6-5-reflection-delta.md`)—— **已上生产**(镜像 `9454524`,2026-09-14,migrate 0;Playwright 17/17 渲染 0 console 错误) |
| 增强 P7 | ✅ | ✅ | 买入前**一键速评 + 量价形态分析**(用户新诉求,2026-09-14):「买入前查个股要尽可能多的分析 + 结论」;定位=**个股分析功能增强**(不一定要买)。一键现场重跑(新鲜度)+ 逐日**量价形态标签**(地量启动/换手峰值见顶/价跌量未缩…) + 「收盘价线 + 换手率柱」双轴图;并把**现成但未上屏**的 `risk.items`(风险红线)/`financial`(财务估值)补渲染。结论取自规则(评分卡 + 风险 veto),**AI 可选** → 不被 AI 网关阻塞。**零 schema**(逐日序列随 `metrics` 进 JSONB)。**设计已定稿**(`docs/phase7/design/ux-ia.md`);**P7-1/P7-2/P7-3 全部完成**(见 `stages/p7-prebuy-patterns.md`)—— **已上生产**(镜像 4e381ec,migrate 0) |
| 增强 P8 | ✅ | ✅ | 手机**截图投递页 `/m/upload`**(用户新诉求,2026-09-14):同花顺在手机上,原先须先传图到电脑再导入、「很麻烦」。做法=**手机优先的极简投递页**,不做全站移动适配 —— 手机连 WiFi 开 `/m/upload` 选类型 → 拍照/相册选图 → AI 识别 → **卡片式核对** → 确认入库。**零后端改动/零 schema/零迁移**(复用 `/api/v1/trades/import`+`/confirm`);抽 `useTradeImport` 共享状态机(`ImportFlow` 164→114 行);路由改**无路径 layout route**(AppShell 外,手机上不漏侧栏);客户端**降采样**(应对 5MB 上传上限);`globals.css` 仅加 `.m-upload` 作用域样式。**已上生产**(见 `docs/phase8/stages/m-upload.md`) |
| 研报 P9 | ✅ | ✅ | **研报体裁与专业版面**(用户新需求,2026-09-16):把「研报」从「个股深研 run 的仪表盘」拆出为**独立体裁**(`/reports`,单栏正文 + 左侧目录 + 三域标记 数据/计算/研判),三类研报(**行业 #12** / 公司 #11 / 宏观 #13)共用一套版面,差异由 `subject_type` + **章节清单**驱动;改 `markdown.py` **摘要前置**(现状摘要在文档末尾)、**免责置尾**;**零 schema**(章节清单走 `metrics.meta.section_manifest`)。**设计已定稿**(`docs/phase9/design/report-layout.md`,2026-09-16 过定稿门)。硬前置 = 主体轴泛化 #10(全链路假设 `code`=6 位股票码;实测 `NormalizeCode("sw801010")` 原样穿过、`IsStockCode` 判 false → 行业码零冲突)。⚠️ 行业估值**无历史序列**(实测 `index_value_hist_funddb` 本版不存在 / `stock_industry_pe_ratio_cninfo` 失效 / `stock_index_pe_lg` 仅宽基)→ 估值**只作横截面排名**,禁「估值历史分位」表述。**P9-1~P9-5 全部完成并上生产**:主体轴泛化(#10)+ `/reports` 独立版面 + 申万行业数据管线(#12)+ 公司研报/个股分析功能拆分(#11)+ **宏观研报(#13,`macro:cn_cpi` 一整条新数据管线)**,全部**零 schema**。相关 PR #15/#16/#17/#23/#28 |
| 容器 P10 | ✅ | ✅ | 容器按功能拆分:单镜像 → 四镜像(issue #47,2026-09-20 已上生产,栈 `v0.0.0-616f31b`);`docs/phase10/design/container-split.md` |
| 事件多源 T2(P11) | ✅ | ✅ | **跨源同事件判定 + 簇内来源可见**(epic #43 任务卡 T2 = issue #48,2026-09-20):归一化加固(抽取 `【】` 内嵌标题前缀)把跨源相似度还原 → 真实重复对 36→65 @0.7(**不改阈值**);簇内 ≥2 机构时下发 `cluster_sources`(机构 + 原文链接 + 上游一级源);**零 schema**。**已合并并上生产**(PR #52) |
| 事件多源 T3(P11) | ✅ | ✅ | **漏报 / 冲突检测**(epic #43 任务卡 T3 = issue #49,2026-09-20):① `source_count` 机构级计数让单源可见(标「单一来源」,中性**不判可疑**,绝不丢事件);② 纯规则数值冲突(`internal/cluster/conflicts.go`,门控 0.6)**留双方原文**、显性展示、**禁止静默择一**。门控按**生产 1410 条真实事实句**标定(随机误报 0.0750%,同标题 0 差异,对抗集 0/4);远改写召回边界如实登记留 #45。**零 schema**;顺修 `deploy.sh` 的 `scripts/` 同步(编排漂移根因)。**已合并并上生产**(PR #54) |
| 公告源 T4(P11) | ✅ | ⬜ | **公告独立源(巨潮资讯网)**(epic #43 任务卡 T4 = issue #50,2026-09-21,填 G3):新增 Go 驱动 `internal/collector/cninfo_announce.go`(`POST` 巨潮 `hisAnnouncement/query`,`pageSize` 上限 30、`minGap` 1s,单日 ~1200 条个股公告),落 `source_type='announcement'` / **`status='collected'`** —— **不进 LLM 抽取**(worker 只取 `raw`)、**不报对账异常**(reconcile 只看 `processed`),两处既有机制零改动;消息页新增**「公告」tab**(`GET /api/v1/announcements`),快讯投影显式排除 announcement。**只存标题 + 原文 PDF 外链,不存正文**(正文仅存在于 PDF;东财正文接口实测被 IP 级限流 300 连发 209 失败 / 连接复用 60/60 RST)。**含迁移 `0016`**(去重键按源是否带 `external_id` 分派为两条 partial unique index;公告标题会重名,实测 1377 行仅 1375 个不同标题,标题键会静默丢行;⚠️ **须与 `InsertRawDocument` 的无目标 `ON CONFLICT` 成对改**)。**已合并入 dev**(PR #63);**未上生产**。`docs/phase11/design/announcement-source.md` |
| 数据源分层 S1(公告分级,#68) | 🟡 | ⬜ | **A 层公告分级**(issue #68 S1,2026-09-21,承 #45 + epic #43 T4):公告实测 ~1200 条/交易日全平铺「看不过来」→ 新增 `internal/announce` **纯规则标题分级**(`must`/`important`/`routine`/`noise`),采集时落 `raw_documents.grade`(**迁移 `0017`**:列 + CHECK + 部分索引),前端公告 tab **默认只显必读+重要**、其余一键展开。判定次序 **否定式 → 中介机构衍生件 → 必读 → 重要 → 噪音 → 常规 → 默认常规**(前两步不可省:实测「最近五年**未被**处罚」含 must 词但语义相反;「重大资产重组…**核查意见**」是券商衍生件,一家就 4 份占满必读位)。校准(2026-09-18 单日 **1196** 条真实标题):**必读 14 / 重要 226 / 常规 678 / 噪音 278** → 必读+重要 **20.1%**、可折叠 **79.9%**(跨 5 交易日 79.9~87.5%);Go 规则与 Python 校准脚本**逐条一致**。⚠️ **只落标签、不进 LLM** —— 公告 `status='collected'` **逐字不变**(公告本就零 token 成本,分级**不产生成本收益**,价值纯在展示层);`grade` 空(非公告源/历史行)按**常规**显示,`gradeMatch` 把 NULL 并入 routine → **只折叠、不隐藏**(红线)。E2E(真驱动+真接口+真库):`new=151` → routine 107 / noise 24 / important 18 / must 2,必读桶(重大资产重组/预重整)与噪音桶(工商变更/独董/中介文件)语义逐条正确。设计:`docs/phase11/design/announcement-grading.md`。**未上生产** |
| 数据分层 C 层(P11) | ✅ | ⬜ | **快讯提频(盘中 3 分钟)+ 反封禁护栏**(issue #68 数据源分层的 **C 层 = 快讯改用法**,2026-09-21)。**S2 新浪去留**:用**生产聚类同一把尺子**(`NormalizeTitle`+`Jaccard`@0.7)实测新浪↔金十近同对仅 **11.2%/11.5% 且双向对称** → **保留新浪**(「转载≠独立源」是根因问题,交 **#45 P2 剥转载**;设计红线:**不得**以「数据无用」删源)。**S3 提频 + 护栏**:提频到**盘中每 3 分钟**轮询,**硬前置** per-host 三护栏(`internal/collector/limiter.go`:令牌桶 2req/s·突发3 / 空响应哨兵连续3次降速封顶×1/8 / 熔断连续4失败开路60s),**信号由驱动层 `Fetch` 出口喂入**(`observeFetch`),`dongcai` 自建 client 顺带归队 `httpSource`。**哨兵与熔断靠跨轮累积** → 采集**必须常驻**(仿 `research-worker`:ticker + `signal.NotifyContext` + 优雅退出)而非 cron 每 3 分钟拉起(后者护栏形同虚设 + 一天 ~4000 容器);新 `collector` 服务**复用 `piks-tools` 镜像**(仍四镜像),`-driver news` 只跑快讯 6 源(公告翻页重,留日管线)。连带:worker `-limit 300→800`;去掉「单轮 3 连败自动 `PauseSource`」(3 分钟就停源 + `PauseSource` **无人读取**的坏到两头)→ 改熔断承担 + **真正尊重 `sources.status='paused'`**(跳过+记 `skipped`)。**零 schema**。`docs/phase11/design/flash-cadence.md`;**PR #70 已合入 dev**(`02ccb88`),**未上生产** |
| 数据分层 D 层(P11) | ✅ | ⬜ | **热榜(从零新建)**(issue #68 数据源分层的 **D 层**,2026-09-22)。源与消费者**均需新建**(前置核查:热榜源与 `morning-brief` 在代码库中**均不存在**)。**2 源**:同花顺话题榜(15 条,含关联个股)+ 财联社首页热文(13 条,SSR 解析);常驻 `hot-topic` 服务盘中**每 30 分钟**,日管线补一发收盘态。🔴 **方案 A:两源各出各的、不合并、不加权、不排名** —— 实测 **615 对标题同题对 = 0**(J≥0.5 仍 0;粒度不同:题材/事件 vs 文章/复盘),混排必然误导。🔴 **不进 `raw_documents`/聚类/`events`**:另立 `HotTopicSource` 接口(**刻意不复用** `Driver`/`RawNews`)+ 独立表 `hot_topic_items`(**迁移 `0018`**)+ 独立进程 + 独立页 `/hot-topics` —— 把「**可被操纵的热度**」隔离成**结构性约束**(红线:只作展示,不作重要性判定,不与印证度 `source_count`/`cluster_sources` 合并)。⚠️ `rank` 取**上游数组下标 + 1**、跳行留空号、**不得重排**(实测财联社数组顺序 ≠ `readNum` 降序:榜位 7 = 507051 > 榜位 1 = 304251);SSR 漂移**报错不空成功**(#64 教训);`hot_value` **`NULL ≠ 0`**;护栏**复用 C 层** per-host 三护栏(同花顺话题榜走新 host `dq.10jqka.com.cn`,独占状态)。`docs/phase11/design/hot-topic.md` |
| 剥转载 P-1(P11) | ✅ | ⬜ | **转载 ≠ 独立源**(issue #83 分期 **P-1**,2026-09-23)。`raw_documents` 去重键含 `source_id` ⇒ 同一篇通稿被两家近逐字转载**落两行** ⇒ 按机构名去重的 `source_count` 被刷高 ⇒ 印证度三级(单一来源/多家印证/广泛报道)**全废**。落地:**读路径派生**(与 `cluster_sources`/`source_count`/`event_conflicts` 同范式,`internal/cluster/reprint.go`),新增 `independent_count`(**独立来源数**)+ 逐来源 `reprint` 标记;**不改 `source_count`(机构数)语义**。🔴 **两条实测反例定死形态**:① **不得用标题指纹**(同事件标题天然收敛,会把独立报道误判成转载,方向与目标相反);② **不得复用 `NormalizeTitle`**(内含 `stripBracketWrap` 抽【】内正文 → 裸标题与「【同标题】+正文」J=1.000,**实测 156 对**误判)→ 指纹取 **content 原文**(`NormalizeReprintContent` 只去电头/HTML 标签 + 折叠空白标点)。校准(lab 库 **398** 多机构簇 / **2735** 跨机构对):强双峰,门控 **0.85**(0.80 下沿全真转载,0.75~0.80 全改写);电头剥离覆盖 **18.3%** 成员行、只把真转载抬过门控;@0.85 **50.0%** 簇独立来源 < 机构数、**39.9%** 由「≥2 家」跌到「恰好 1 个独立来源」。前端三级标签改读 `independent_count` + 来源区逐条标「(转载)」。残余上限(改写型转载漏判)**如实登记**。**零 schema**。`docs/phase11/design/reprint-stripping.md` |
| 数据面地基 P-2(P11) | ✅ | ⬜ | **事件管线 P-2:迁移补列 + 打通已有消费端**(issue #83 分期 **P-2**,2026-09-23)。issue §三 P4 表列「迁移 8 项」,本篇**复核收窄为 4 列**。🔴 **刻意不做 3 项**(与 P-1 读路径的边界):`raw_documents.origin`(可从 `extra->>'source'` 读,建列 = 第二真源)、`raw_documents.is_reprint`(P-1 已派生;**更根本:转载是簇相对属性,列级布尔不良定义**)、`events.corroboration`(P-1 已派生 `independent_count`;**落库会与 `reexamine` 的 `MergeClusters` 并发漂移** ⇒ 簇成员数一变印证度就得重算 = 两处真源)。✅ **新增 4 列**(迁移 **`0021`**,**全部无 FK** —— 与决策边 `relationships` 同纪律):① `raw_documents.origin_kind`(`TEXT NOT NULL DEFAULT 'pipeline'`,**worker 的 `ListRawPendingStatus` 与 reconcile 的全部 raw 层查询加 `origin_kind='pipeline'`**;今日无 realtime 行,过滤是**契约非优化**,给 P-5 实时层立结构性隔离;⚠️ 去重键(0016)**不含本列**,P-5 须补「realtime→pipeline 提升」);② `event_clusters.canonical_event_id`(`ApplyClusters` 早已算出却丢弃,此处持久化;**回填比较器与抽出的 `canonicalIndex` 逐字一致** `created_at ASC`/同则 `confidence DESC`;**代表冻结** —— reexamine 不重选);③ `raw_documents.canonical_id`(**只建列**,分组填充属 P-8,本版全 NULL,**未实现**);④ `events.human_verdict`(**只建列**,取值域本版不定故**故意不加 CHECK**,**引擎永不碰**)。测试:`canonicalIndex` 单测 5 例 + `ApplyClusters` 落列集成测 + `origin_kind` 门控 / `0021` 回填 / `human_verdict` 存活三集成测,`go build/vet/test` 全绿。**含迁移 0021**。`docs/phase11/design/event-pipeline-p2.md`;`docs/phase11/design/README.md`;**未上生产** |
| 早/晚档窗口 P-3(P11) | ✅ | ⬜ | **早/晚档窗口(P1)+ 簇内代表选取(P6)**(issue #83 分期 **P-3**,2026-09-23,**依赖 P-2**)。**交付面 = 仅后端**(前端「簇合并视图」属 P-4,不做)。① **P1 早/晚档窗口**:`late` = 当日 09:15→18:30、`early` = 前一日 18:30→当日 09:15,**衔接无缝无叠**(单测钉死);🔴 **窗口锚 `events.created_at`(入库/抽取时刻)而非 `occurred_at`** —— 窗口理由是**阅读节奏**(非热度切口),锚「我们何时拿到它」;已知边界(抽取滞后推后窗口)如实登记。② **榜单接口 `GET /api/v1/board`**(`?stage=early\|late&date=`,`requireAuth`):窗口内**全集不截断**(不 LIMIT)、排除 `status='merged'`、按 `created_at` 倒序(**不排名**);每行 = **内嵌 `apiEventItem`**(与 `/api/v1/events` 单条同形)+ 榜单特有 `score`。③ **打分**:归一化加权 `score=Σwᵢ·normᵢ`,本版 `{CrossChannel:1, Entity:0, Watch:0}`,`max=0` 防除零;`score` **照算但不用作排序**(接口先立,将来调权重响应形状不变)。④ **P6 代表新规则**(`有链接 > 非转载 > 最早 > 高置信`;新增 `memberMeta{hasURL,isReprint}` + `store.ListEventDocMeta` 取 url/正文,**不用渠道数** —— 那是簇级属性):🔴 **只对新簇生效,存量不回填** —— 与 P-2「代表冻结」纪律一致,`canonicalIndex` 与迁移 `0021` 回填 SQL **有意分叉**(显式记录,不得静默);`reexamine` 的簇级 survivor 选举**不动**。⑤ **`canonicalTitle` 改无 LLM 规则**:取「与其他成员标题重合度最高」的**成员原文标题**(平票取最早;零 token、确定性、Fact 非 Inference);移除死字段 `PairVerdict.CanonicalTitle`。**零 schema、零迁移、零 LLM 增量**。`docs/phase11/design/event-pipeline-p3.md`;`docs/phase11/design/README.md`;`docs/phase11/design/event-pipeline-p2.md`;**未上生产** |
| 展示单元 P-4(P11) | ✅ | ⬜ | **展示单元(P8)+ 成本粗筛(P7)收口**(issue #83 分期 **P-4**,2026-09-23,**依赖 P-1/P-2/P-3**)。**整条 epic 第一次变成用户可见的东西**。① **P8 展示单元 = 簇**:数据层已合多家、展示层不得回退到 canonical 单条。事件投影新增 `cluster_title`(取 `event_clusters.title`)+ `cluster_facts`/`cluster_affected`(簇内**各成员** facts/affected **并集**,**仅成员>1 下发**,**字面去重不做模糊归并** —— 数字分歧留给 `event_conflicts`)。② 🔴 **来源走 raw 层全集**:`store.ListClusterRawSources` 从 `events.raw_document_id` 出发经 **`COALESCE(rd.canonical_id, rd.id)`** 取转载组代表、join 出该组**全部**行,补上「**报了这篇稿但未被抽成事件**」的机构(raw 层是全集、事件层是子集)。`canonical_id` 由迁移 **`0022`**(部分索引)+ 新命令 **`cmd/cluster-raw-link`**(第 **12** 个管线命令)回填:判据**复用** P-1 正文指纹 @0.85(`internal/cluster/reprint_raw.go`,**不另立第二把尺子**)、**代表冻结**(`SetRawCanonicalIDs` 只写 `WHERE canonical_id IS NULL`)。③ 🔴 **计数与转载标记仍走事件层**(`source_count`/`independent_count`/`reprint` 全由 P-1 实时指纹算,**与 P-3 响应口径逐字一致**;raw 层是回填快照可能漂移 ⇒「**宁可多列一个机构,不可多算一票**」)。⚠️ `canonical_id` 只能表达**转载组**,表达不了「一家发稿 A、另一家发稿 B」的非转载多源簇,如实登记。④ **P7 成本粗筛**(`internal/extract/gate.go` 纯函数):排序键 = 必送(`important`/`confirmed`)→ 分级(`level`)→ 热度(`reading_num`/`like_nums`)→ `retrieved_at`;被挡落 `status='deferred'`(**≠ `failed`**:正常分流,**不进对账**,`reconcile` 白名单不含它)+ `task_runs.meta` 记 `deferred`/`defer_reason`;🔴 **默认保守**(`-coarse-defer` 默认 false ⇒ 只排序**不筛**)、**不替代预算护栏**;`ListRawPendingStatus` 默认**不取** deferred(防死循环)、`-retry` 隐含放回 + 重加 1 天时间窗兜底。⑤ **前端**:新页 `/board`(早/晚档写 URL、**不排名**、显式标注「无热度排序」)、抽屉簇视图(`EventDetail` + 拆 `EventContent`)、来源按机构分组(`EventSources`:每机构全部 URL + 无外链如实标注 + 一级源链接归属一级源)、导航「发现」组 **13→14 项**。**一个迁移、零新表、零 LLM 增量**。`docs/phase11/design/event-pipeline-p4.md`;`docs/phase11/design/README.md`;`docs/phase11/design/event-pipeline-p2.md` §1.4/§4;`docs/phase11/design/event-pipeline-p3.md` §7;`docs/架构总览.md`;`docs/项目详解.md`;**未上生产** |
| 实时层+调度 P-5(P11) | ✅ | ⬜ | **实时层 + 三档调度 + 保留期**(issue #83 分期 **P-5** = **收口**,2026-09-23,依赖 P-1~P-4)。**零 schema/零迁移/零新服务/零 LLM 增量**。① **实时层 = raw 层滚动读**(**复用 C 层,不新增写入方**):`GET /api/v1/flashes?hours=N`(**ADDITIVE**,缺省逐字不变)按 `COALESCE(published_at,retrieved_at,created_at)` 取近 N 小时,前端「近 3 小时/全部」写 URL;🔴 **不合并/不排名/不做榜**;`origin_kind='realtime'` **保持 inert**。② 🔴 **榜单窗口锚改「原始到达时刻」**(`COALESCE(rd.published_at, rd.retrieved_at, e.created_at)`,取代 P-3 的 `e.created_at`)—— P-3 §1.2 已**预先授权**;理由:全链收盘后一次跑 ⇒ 锚 `created_at` 使**早榜结构性恒空**;后果:早/晚榜成员集合与 P-3 验收**不同**(批准的设计变更)。③ **保留期清理**:新命令 **`cmd/cleanup`**(第 **13** 个管线命令,`-days 28`/`-dry-run`)+ `scripts/cleanup.sh`(周日 + 距上次成功 ≥28d + **flock**);🔴 **只清「到期 + 无事件引用 + 非转载组代表」的行**(判据单趟 ⇒ 不留悬空 `canonical_id`);**已抽取行永久保留** ⇒ **「28 天」不是「全体 raw 的 28 天」**。④ **三档调度**:`pipeline.sh` 加 `STAGE=early|late`(**未知档 exit 2**),**按档**闸门/记账/flock;步骤分 `COMMON`(两档)+`EOD`(**仅 late**,收盘产物);crontab **2→4 条**;🔴 **realtime 档不在 cron**(常驻 collector 已覆盖)。⑤ **两处修正**:`deferred` 文档-代码不一致(修文档,**不隐藏**)、`ReconProcessedNoEvent` NULL 标题崩溃(`COALESCE`)。⑥ **三处 issue 事实前提按代码更正**。`docs/phase11/design/event-pipeline-p5.md`;`docs/phase11/design/README.md`;`event-pipeline-p3.md` §1.2/§7;`event-pipeline-p4.md` §7;`docs/架构总览.md`;`CLAUDE.md`;**未上生产** |

## 目录映射

| 目录 | 对应 | 状态 |
|---|---|---|
| `docs/phase1/` | 迭代 0 + 迭代 1(地基与可靠性,已完成冻结) | ✅ 冻结 |
| `docs/phase2/` | 迭代 2 + 迭代 3 + 迭代 4 + 迭代 5(增值 + Web 平台)+ G8/聚类质量/周报综述/交易/交易闭环 | ✅ 迭代 2/3/4 冻结;迭代 5 已归档(`stages/web-5.md` §5-1/5-2/5-3);后续 dev-only 功能均已归档(`stages/g8.md` `cluster-quality.md` `weekly-ai-summary.md` `trades.md` `trade-loop.md`) |
| `docs/phase3/` | 生产化 | ✅ 生产化已落地(设计定稿 + 实现验收,见 `stages/prod.md`) |
| `docs/phase4/` | 能力并入(research 深研并入 + 构建优化) | ✅ 已归档冻结(`design/research-merge.md` 设计冻结 + `stages/research-merge.md` 实现验收 T1~T7;dev-only,未部署 lab) |
| `docs/phase5/` | 前端 IA 重组(个股轴心 + 自选股) | ✅ 已实现并上生产(Phase 1–4);`design/frontend-ia.md` 含实现记录 §7;镜像 `c81c6d2`,migrate 0 |
| `docs/phase6/` | 前端决绝重构 + 投研闭环补齐(P6) | ✅ 已上生产(2026-09-14);P6-1~P6-5 全部完成(`stages/p6-1-foundation.md`、`p6-2-plain-nav.md`、`p6-3-guided-home.md`、`p6-4-decision-loop.md`、`p6-5-reflection-delta.md`) |
| `docs/phase7/` | 买入前速评 + 量价形态分析(P7) | ✅ 已上生产(2026-09-14,镜像 4e381ec);单阶段 `stages/p7-prebuy-patterns.md` |
| `docs/phase8/` | 手机截图投递页(P8) | ✅ 已上生产(2026-09-14);单阶段 `stages/m-upload.md` |
| `docs/phase9/` | 研报体裁与专业版面(P9) | ✅ 已定稿并实施完成(2026-09-17 上生产);`design/report-layout.md`、`design/company-analysis-split.md`(#11,已定稿并实施)。**编号说明**:dev 线现有 phase1–6,master 线现有 phase1–8(P7/P8 仅 master),取两线均未占用的 phase9 |
| `docs/phase10/` | 容器按功能拆分(P10,单镜像 → 四镜像) | ✅ 已上生产(2026-09-20,issue #47);`design/container-split.md` |
| `docs/phase11/` | 事件类多源交叉验证(epic #43 任务卡)+ 数据源分层(issue #68) | 🟡 进行中;T2/T3 **已合并并上生产**(issue #48 / #49,PR #52/#54),T4 **已合并入 dev**(issue #50,PR #63,**未上生产**),**A 层 S1 公告分级**(issue #68,PR #69 **已合入 dev**)、**C 层 S2/S3 快讯提频**(issue #68,PR #70 **已合入 dev**,dev-only)、**D 层热榜**(issue #68,从零新建,dev-only)、**P-1 剥转载**(issue #83,PR #92 已合入 dev,dev-only)、**P-2 数据面地基**(issue #83,迁移 0021,dev-only)、**P-3 早/晚档窗口+代表选取**(issue #83,dev-only)、**P-4 展示单元+成本粗筛**(issue #83,迁移 0022,dev-only)、**P-5 实时层+调度+保留期**(issue #83,收口,dev-only)同在 `docs/phase11/design/`;`design/event-cross-source.md`(T3 见 §8~§12)、`design/announcement-source.md`、`design/source-tiering.md`(分层草案)、`design/announcement-grading.md`(S1)、`design/flash-cadence.md`(C 层)、`design/hot-topic.md`(D 层)、`design/reprint-stripping.md`(P-1)、`design/event-pipeline-p2.md`(P-2)、`design/event-pipeline-p3.md`(P-3)、`design/event-pipeline-p4.md`(P-4)、`design/event-pipeline-p5.md`(P-5,收口)。**编号说明**:dev 线已有 phase1–10 |

## 子阶段明细

| 日期 | 子阶段 | 交付物 | 文档 | 备注 |
|---|---|---|---|---|
| 2026-08-26 | 迭代 0 设计 | 最小闭环设计定稿 | `docs/phase1/design/iter0-min-loop.md` | ✅ 已定稿冻结(D1~D7) |
| 2026-08-26 | 迭代 0 实现 | 全链 smoke test 通过(mock) | `docs/phase1/stages/iter0.md` | ✅ 验收 §8 全过;G1 东财 stub 待验证 |
| 2026-08-26 | vault 拆分 | Generated+Personal 双独立 git 仓库 | `项目详解.md` 技术栈表 | ✅ 架构 §18/§19 隔离落地 |
| 2026-08-26 | 迭代 1 设计 | 可靠性设计定稿 | `docs/phase1/design/iter1-reliability.md` | ✅ 定稿(D8~D11) |
| 2026-08-26 | 迭代 1 实现 | 可靠性验收 §5 六项全过 + 归零闭环 | `docs/phase1/stages/iter1.md` | ✅ G1 东财真实驱动就位 |
| 2026-08-26 | 真实 provider 验证 | 真实 AI 全链验证闭环 + 聚类缺陷修复 | `docs/phase1/stages/iter1.md` §5.1 | ✅ 修复 `/zen/go/` 路由;实体交集改包含匹配;4125 tokens 落库 |
| 2026-08-26 | 迭代 2 设计 | 市场情报设计定稿 | `docs/phase2/design/iter2-market-intel.md` | ✅ 定稿(D12~D17) |
| 2026-08-26 | 迭代 2 实现 | 行情采集 + 市场状态 + 每日复盘全链真实运行 | `docs/phase2/stages/iter2.md` | ✅ 验收 §5 八项全过;AI 调用=0;push2 指数/成交额/涨跌家数源被 WAF 限流,如实 pending |
| 2026-08-26 | 迭代 3 设计 | 实体补全设计定稿 | `docs/phase2/design/iter3-entities.md` | ✅ 定稿(D18~D21:单表 entities / 便宜档分类 / 仅叶子行业 / G3 延后) |
| 2026-08-27 | 迭代 3 实现 | 实体构建 + 实体卡 + affected wikilink + hot_topics 补链 | `docs/phase2/stages/iter3.md` | ✅ 验收 §5 七项全过;52 公司/32 行业/6 实体 affects;90 实体卡;重跑零提交 |
| 2026-08-27 | 生产化设计 | lab 部署设计定稿(单机 Docker + 用户 crontab + GitHub 分发) | `docs/phase3/design/prod-deploy.md` | ✅ 定稿(D-P1~P12,D-P10=方案 B);lab 免 sudo 约束 |
| 2026-08-27 | 生产化实现 | lab 部署落地 + 验收 §15 十项全过 | `docs/phase3/stages/prod.md` | ✅ dev 本地编译+镜像传输;首链 50 新闻→55 events→55 卡推 GitHub;幂等/备份/密钥/时区/调度/安全全过;待办:NTP(需 sudo)+ 首日 16:10 cron 实跑确认 |
| 2026-08-27 | 迭代 4 设计 | 个人学习闭环设计定稿 | `docs/phase2/design/iter4-learning-loop.md` | ✅ 定稿(D24~D29:单表 personal_notes / lab clone PIKS-Personal / 周报规则+高智档综述 / 02-Market/weekly / G3-5 延后 / Belief 状态只镜像) |
| 2026-08-27 | 迭代 5 设计 | Web 平台设计定稿(替换 Obsidian/GitHub,PG 直渲 + AI 对话 + 截图) | `docs/phase2/design/web-app.md` | ✅ 定稿(§9 阶段表:5-1 只读 / 5-2 编辑+去界面层 / 5-3 AI+截图;§3.3 缩放点选看内容;§3.4 时尚美观专业视觉规范) |
| 2026-08-27 | 迭代 5-1 | Web 只读平台:cmd/web + 8 页 + 原生 SVG 图谱(缩放/拖拽/点节点看内容) + 部署 lab:8090 | `docs/phase2/stages/web-5.md` | ✅ 验收 §2 七项全过;看板 113 事件/195 实体/181 关系/2 快照日(真实生产数据);图谱与视觉待浏览器目验;5-2/5-3 延后 |
| 2026-08-27 | 配置入库(5-2 前置) | 大模型配置改入库 `app_config` + `/settings` 页面可编辑;代码不再读 `PIKS_AI_*` 环境变量 | `migrations/0005_app_config.sql` | ✅ 迁移+种子+4 消费方(worker/cluster/entity-build/web)套用;lab 种子真实配置(Zen base/双模型/密钥掩码);页面实时编辑验收(预算 100→回 0、key 留空保持);.env 移除 5 项 AI 变量;deploy 顺序改 postgres→migrate→web |
| 2026-08-27 | 迭代 5-2 实现 | 笔记 Web 编辑(M1-M4)+ vault/GitHub 下线(M5) | `docs/phase2/stages/web-5.md` §迭代5-2 | ✅ 验收 §2 六项全过:新建/编辑笔记、关联事件/实体、事件卡「我的理解」(去重更新)、/weekly 周报聚合、vault/GitHub 停更(pipeline 无 publisher、跳过写盘+git、无新提交)、页面全 200;镜像 7102794 部署 lab;备份由用户自理 |
| 2026-08-27 | settings 模型下拉(5-3 前置) | /settings 三个模型下拉(extract/reasoning/vision)实时从 provider /models 拉取;新增 `ai_model_vision` 键(空=截图回退文本模型) | `migrations/0007_ai_model_vision.sql` | ✅ G7 探针实测 vision 模型接受 image_url;lab 配置 extract=deepseek-v4-flash、vision=deepseek-v4-flash-vision-exp |
| 2026-08-27 | 迭代 5-3 | AI 对话页 + 截图:问答带引用、截图上传识别、历史留存、配置即时生效 | `docs/phase2/stages/web-5.md` §迭代5-3 | ✅ 验收 §2 七项全过:问答带事件/实体引用可点、截图 vision 识别、G7 通过、历史回显、降级如实、附件持久化、每请求重读配置;镜像部署 lab;G7 缺口勾选 |
| 2026-08-28 | G8 语义检索 | /chat 检索升级:LLM 同义扩展(方案 B)。探针:Zen 无 embeddings 端点(404)→ 方案 A(Ollama)否决,选 query 端同义扩展;扩展召回「降准」→「下调存款准备金率」类事件 | `docs/phase2/design/g8-semantic-retrieval.md` + `docs/phase2/stages/g8.md` | ✅ 验收全过(扩展/不退化/加权排序/降级/标注);5 测试包通过;**只做 dev 不升级生产**(生产 /chat 维持关键词);已知权衡:扩展低权重噪音 + merged 事件状态过滤 |
| 2026-08-28 | 聚类质量修复 | 重审视 Pass:`cmd/cluster` 正常 pass 后追加既有 canonical 跨簇互检(候选池扩为活跃簇 canonical ∪ 未聚类),复用迭代 1 阈值 + LLM 确认,确认后并入既有簇。修复跨簇重复盲区(降准两簇合一)+ 新事件↔既有簇滞留 | `docs/phase2/design/cluster-quality.md` + `docs/phase2/stages/cluster-quality.md` | ✅ 验收全过(合并/Web 一张卡/无过度合并/幂等零消耗/预算跳过如实);7 测试包通过;**只做 dev 验证不部署 lab**(改动随未来镜像重建生效);已知权衡:survivor 标题不迁移、阈值不降 |
| 2026-08-28 | 周报 AI 综述(Web 适配 D26) | `/weekly` 新增 AI 综述段:手动按钮触发(POST action=generate)、高智档(reasoning)生成、落 `weekly_summaries` 表按 ISO 周缓存(GET 永不调 LLM)、预算护栏 + task_runs 记账、未配置/预算/无数据/失败如实降级 | `docs/phase2/design/weekly-ai-summary.md` + `docs/phase2/stages/weekly-ai-summary.md` | ✅ 验收全过(生成真实 Zen 综述严格基于当周数据/缓存命中零调用/重新生成覆盖不增行/未配置·预算·nodata 如实提示/记账 ai_tokens);dev 实测 `2026-W35` 563 tokens;`go build/vet/test` 全过;**只做 dev 不部署 lab**(随未来镜像重建生效) |
| 2026-08-28 | 交易功能(用户新需求) | `/trades` 每日自交易:同花顺「今日交易」/「持仓」截图 → 视觉抽取 → 预览编辑/勾选确认入库(migration 0010 `trades`+`positions`)、手动录入兜底、交易股票自动补实体(`detail.source='trade-import'`,已有复用)、AI 解读带引用(expandQuery + SearchKnowledgeExpanded + 笔记 + 防未来函数截断到 trade_date)、mistake 候选用户确认存笔记(iter4 单向 harvest)、预算护栏 + task_runs 记账、未配置/视觉未配置/预算耗尽/识别为空如实降级 | `docs/phase2/design/trades.md` + `docs/phase2/stages/trades.md` | ✅ 验收全过(截图导入 4 笔/持仓 3 行/手动/去重「已存在」/实体补全含已有复用/解读带引用+防未来函数实测未来事件零泄漏/无关联如实标注/mistake 存笔记幂等/降级如实/记账);`go build/vet/test` 全过;**只做 dev 不部署 lab**;验收修 2 bug(positions 反序列化丢 cost/mv、mistake 表单嵌套 range 模板崩溃) |
| 2026-08-28 | 交易闭环(承接交易功能) | 持仓 AI 诊断(`position_reviews` 按 snapshot_date 缓存,Go 算组合盈亏/集中度/联动 + KB 带引用 + 防未来函数截断到快照日 + risks 候选存笔记幂等 slug=`posrev-*`,复用 tradeReview 全管线零新 AI 能力)+ 本周交易/持仓进周报(页面 + AI 综述上下文,nodata 扩五类) | `docs/phase2/design/trade-loop.md` + `docs/phase2/stages/trade-loop.md` | ✅ 验收 design §4 十一项全过(诊断生成+缓存+GET 零调用/数字 Go 算/引用白名单/防未来实测未来事件零泄漏/risks 存笔记幂等/周报含交易持仓+综述引用/nodata 仅交易周实测/降级三式零调用/记账/prod 未动);`go build/vet/test` 全过;**只做 dev 不部署 lab**;验收修 1 真 bug(aggPositions 集中度占比恒 0,被 AI 如实指出)+ 1 质量缺口(flash 不填 refs,prompt 强化后引用可点) |
| 2026-08-29 | 前端 React 联调真实数据(产品化 1) | 后端只读 `/api/v1` REST **13 端点**(events/entities/relationships/market/snapshot/flashes/notes+notes/:id + dashboard/recon/reviews/trades/chat/settings/weekly,全部复用 store 查询,零 schema 变更)+ **React 前端 14 页全部接 `useData` 真实数据**(mock 仅降级兜底)+ CORS 中间件(`Access-Control-Allow-Origin: *`,浏览器 :3100→:8090)+「演示数据」徽章改条件显示(demo 信号计数,真实数据隐藏)+ Playwright 端到端脚本 `frontend/scripts/e2e_check.mjs` | `docs/phase2/stages/frontend-lian-tiao.md` | ✅ 前端 `npm run build` 16 路由全过;13 端点 curl 全 200 + 形状对齐 `types.ts`;`go build/vet` 全过;**浏览器 E2E 待 lab 依赖装好后跑**(scripts/e2e_check.mjs);React 前端为本仓库新增未发布,生产暂由 Go web(:8090)继续服务 |
| 2026-08-29 | 前端去 Next.js → **Vite SPA + nginx 网关**(产品化 2,用户要求) | frontend 保留、去掉 Next.js(9 处 `next/*` 耦合全改写:useUrlState 换 react-router `useSearchParams`、Link→react-router、TopNav/⌘K 交互页改原生 `<a>`)、14 页移 `src/pages/`(8 只读页注册 SPA 路由,5 交互页被 Go 遮蔽)、`api.ts` 改相对 `/api/v1`(同源走网关)、dev 用 vite proxy 复刻生产分流;**生产拓扑:nginx 单入口 :8090(SPA 静态 + 反代 `/api/*` 与 Go HTML 交互页),Go web 与 nginx 同容器只监听 `127.0.0.1:8090`**(用户明确「编辑页通过 nginx,go 监听 127.0.0.1」);镜像改 nginx:alpine 底座(多阶段:golang 编译 + node 构建前端 dist),deploy.sh 加同步 compose | `docs/phase2/stages/frontend-lian-tiao.md` §Next.js 下线 | ✅ `npm run build`/`tsc --noEmit`/SSR 渲染冒烟(4 路由)/vite dev 路径分流 curl(SPA·Go 编辑页·/api 代理)全过;`go build/vet` 全过;**已部署生产(镜像 2f65017,dev db81b12 合并 dev→master)**:`/` SPA index(深链 try_files 200)、`/api/v1` JSON、`/notes /settings /weekly /trades` Go 编辑页、详情页、`/static/` 样式全 200;容器内 nginx `0.0.0.0:80` + Go `127.0.0.1:8090` 拓扑确认;浏览器 E2E(scripts/e2e_check.mjs)仍待 chromium 系统依赖(libnspr4 缺,需 sudo 装后本地跑) |
| 2026-09-12 | 能力并入设计(用户新需求) | research 个股深研并入 PIKS:`research/` 同仓多运行时(不翻译为 Go)+ `cmd/research-run` 编排(状态机 pending→gathering→synthesizing→verifying→done/failed)+ 删其 SQLite 存储层(PG 唯一归档)+ LLM 合成复用 `ai.Provider`(extract 档)+ `synthesize` 的 Number Lint 作第二道防幻觉闸门 + `research_runs` 表(0012)+ 3 个 `/api/v1` 端点 + 深研报告页(Fact/Opinion 分区);**同时移除 Go vendor**(8.2MB/280 文件,1 直接依赖 → go.sum + `goproxy.cn`,Dockerfile 依赖层缓存);**research 可独立迭代**(D-11 硬约束:零共享状态 / 产物契约版本化 `contract` / 依赖与构建隔离 `piks-research` 独立镜像 / 接口冻结 + 最小版本 fixture 测试) | `docs/phase4/design/research-merge.md` | ✅ **设计已定稿冻结**(2026-09-12):D-2 = **方案 B 同仓双镜像**,D-11 = **research 单独迭代**;待实现 T1~T7;**只做 dev 验证不部署 lab**;steady 经评估**不并入**(两个完整服务栈,合并只增耦合) |
| 2026-09-12 | 能力并入实现与验收(P4,T1~T7) | Go 去 vendor + `research/` 迁入(删 SQLite storage,CLI 三命令改造)+ `research_runs` 表(0012)与存取层 + `internal/research` 编排(状态机 + 断点重跑 + 产物契约版本校验)+ `cmd/research-run` + Dockerfile `research` 独立 target(`piks-research` 镜像)+ 3 个 `/api/v1/research-runs` 端点 + `/research/:runId` 报告页(Fact/Opinion/机检三分区)+ 实体卡·持仓行「深研」入口 + 独立性 CI 检查脚本 + fixture 测试 | `docs/phase4/stages/research-merge.md` | ✅ **T1~T7 全过**(2026-09-12,dev-only):`go build/vet/test` 全过(含 fixture 状态机测试,不依赖 Python);`pytest research/tests` 71 项独立通过;`tsc --noEmit`+`npm run build` 过;Playwright 实测报告页四区 + 深研按钮闭环(console 无报错);契约升版 `contract:2` 如实拒绝、`contract:1` 正常 done;构建隔离实测日志无 golang/node;**prod 未动**(D-8) |
| 2026-09-13 | 前端 IA 重组设计(用户新需求) | 前端信息架构从「按后端产物类型平铺」重组为**个股轴心的个人投研平台**:首页=自选 `/watchlist`,`/stock/:code` 个股中心(以 code 为主键/entity 可选富化,聚合 6 节)+ **引入自选股**(复用 `entities.status` 三态 watch/active/archived,零迁移)+ **同花顺自选截图镜像同步**(复用交易截图两段式 `kind='watchlist'`,服务端 diff add/keep/remove)+ 市场数据退为「发现」组 + 对账降为设置子入口;聚合端点 `GET /api/v1/stock/:code`(照 `handleAPIDashboard` 形,新增 4 个 store getter);**⚠️ 前置必修地雷**:`UpsertEntity` 空 Status 当 active,每日 entity-build 会清空自选(§2.6.1) | `docs/phase5/design/frontend-ia.md` | 🟡 **设计草案**(2026-09-13,待定稿门):分 4 Phase ①个股中心(只读可回滚)②自选(修地雷+首页)③截图镜像同步④收口;**只做 dev 验证不部署 lab**;无代码改动、无 schema 变更 |
| 2026-09-13 | 前端 IA 重组实现与验收(P5,Phase 1–4) | 后端:`GET /api/v1/stock/:code`(api_stock.go)+ `GET /api/v1/watchlist`(api_watchlist.go)+ `?status=` 过滤 + store 4 getter(GetCompanyEntityByCode/ListTradesByCode/LatestPositionByCode/ListNotesReferencingEntity)+ **修 `UpsertEntity` 空 Status 地雷**(空=保持既有)+ 截图导入 `kind='watchlist'` 两段式(buildWatchPreview 服务端 diff add/keep/remove + confirm 幂等置 watch/archived);前端:自选首页 `/`(原看板迁 `/market`)+ `/stock/:code` 个股中心六区块 + 导航重排(自选/研究/发现/复盘/交易/系统)+ 全入口导流 + ImportFlow 拆 4 组件 | `docs/phase5/design/frontend-ia.md` §7 | ✅ **Phase 1–4 全过**(2026-09-13):`go build/vet/test` 过(含地雷回归 + 镜像 diff 集成测试);`tsc --noEmit`+`npm run build` 过;**Phase 2 硬验收**:置 watch → 跑真实 entity-build → 仍为 watch ✓;curl 端点形状对齐 StockHub;测试后 DB 已还原 |
| 2026-09-13 | 前端 IA 重组上生产(P5 部署) | dev `1cde70e`(含 b5baca4/efcfa9c research 两修)→ merge dev→master `82f3176` → 空提交 marker `c81c6d2`;`./scripts/deploy.sh`(build → save/load → compose 同步 → migrate → web up) | `docs/phase5/design/frontend-ia.md` §7 | ✅ **已部署 lab(镜像 `c81c6d2`,migrate 0)**:15 页全 200(含新 `/market` `/stock/600519` `/stock/300750` 深链);`/api/v1/watchlist`(`{items:[]}`)、`/api/v1/stock/:code`、`/api/v1/entities?status=watch` 全 200;`/api/v1/stock/600371`(万向德农)entity+industry+limit_ups 渲染正常;生产 bundle `index-CtxiwP8_.js` 含新 IA 文案;回滚镜像 `piks-tools:rollback-pre-ia`(旧 `511f951`)留存 lab。⚠️ 生产 `ai_model_vision` 空 → 截图导入暂不可用 |
| 2026-09-14 | P6-1 地基与诚实实现与验收 | 设计系统单系统化(`globals.css` 重写 + 圆角 3 token + 删 20 死类 + 修 8 处暗色渐变/色 + `.txt-faint` + 表格对齐提权 + 去 framer-motion)+ 5 项数据诚实/死重修复(reviews 出 mistakes、ladder 无值显 `—`、`DOC_TYPE_LABEL` 补全、Go 去 ~30 emoji、删 3 死依赖 + echarts 按需 + 拆超大文件)+ KPI 归「我」 | `docs/phase6/stages/p6-1-foundation.md` | ✅ **全过**:`go build/vet/test` + `tsc` + `npm run build`;bundle JS **1,633→948 kB**(−42%);新增 `reviews_test.go` 2 例做缺陷回归 |
| 2026-09-14 | P6-2 白话导航 + 隐藏管线 + 词汇表 + 文案实现与验收 | 侧栏 14→11 项按闭环分组去黑话(涨停股/消息/研究报告/问 AI/持仓诊断);实体库·图谱移入设置页「数据与运维」卡;快讯并入消息页双 tab(`?tab=`,保留 `/flashes` 深链)+ `lib/glossary.ts` 词汇表 + `<Term>` 行内提示 + `/help` 使用指南;全站 `.psub`/`.hint`/页脚/空态去管线黑话(含 `Pipeline` 重写:命令名→白话名,顺修重复 key);KPI 归「我的」;**修 e2e 冒烟脚本**(全 SPA 化 + 后端可达性探测 + 导航遍历) | `docs/phase6/stages/p6-2-plain-nav.md` | ✅ **全过**:`tsc`+`build`+`go test`;**e2e 冒烟 28 通过 / 0 失败**(本地 PG :5433 + Go :8090 + vite :3100),16 路由 + 侧栏 11 项遍历;P5 深链全达 |
| 2026-09-14 | P6-3 引导式首页 + 自选富化实现与验收 | 后端 `GET /api/v1/watchlist` 富化(零 schema):每项加 `latest_event`/`position_date`/`has_research`/`latest_research_asof`,顶层加覆盖率;新增 1 个批量 read `ListEventsByEntityIDs`;抽 `groupWatchEvents`/`watchNeedRank` 纯函数 + 2 例回归。前端首页改引导式(`pages/watchlist.tsx`→`pages/home.tsx`):页头 + 市场条 + **今天该看什么**(T1/T2) + **我的自选**(持有中/观察中,盈亏带「截至快照日」) + 快速开始 + 首次引导空态;`WatchGroups`/`TodayFocus`/`QuickStart` 新组件,`WatchTable` 加行徽标。截图导入失败如实报错 + 指路 `/settings` | `docs/phase6/stages/p6-3-guided-home.md` | ✅ **全过**:`go build/vet/test`(新增 watchlist 2 例)+ `tsc` + `build`;**e2e 冒烟 28/0**;`curl /watchlist` 富化形状实测(测后 DB 还原);首页有/无自选两态实测渲染,console 0 错误 |
| 2026-09-14 | P6-4 决策记录闭环(研究→决策→持仓)实现与验收 | 零 schema 决策边:复用多态 `relationships`(`from_type='trade'`, `rel_type='based_on'`, to 指向 research_run/event/personal_note 的 **UUID**)。后端 `POST /trades` 收 `based_on:{run_ids,event_ids,note_ids}` 逐条建边(失败如实报错不回滚);`apiTrade.based_on[]` + `/stock/:code` 顶层 `decisions`;store 新增 `InsertTradeReturningID`/`ListRelationshipsFrom(IDs)`/`ListResearchRunsByIDs`/`ListPersonalNotesByIDs`;`apiResearchRunSummary` 加 `id`(UUID,修接边悬空)。**同 commit 图谱隔离**:`ListAllRelationships` 加 `graphRelFilter` 排除决策边(否则漏进 `/graph` 成无名节点)。**顺修** `CreateRelationship` 的 `properties` NOT NULL 地雷(默认 `'{}'`)+ 集成回归测试。前端:`DecisionRefs`/`TradeBasedOnPicker`/`StockDecisions` 新组件 + `RefPicker` 复用,`TradeAddForm` 内嵌选择器、`TradeTable` 展开行显示、个股页首屏「当时在看什么」 | `docs/phase6/stages/p6-4-decision-loop.md` | ✅ **全过**:`go build/vet/test` + `tsc` + `build`;**`/graph` 9 节点/4 边、`/relationships` 64 前后不变**(DB 内 3 条决策边存在但被过滤);run/event/note 三类边读回实测(测后删除);**e2e 冒烟 28/0**;Playwright UI 实测选择器三组渲染 + 可勾选,console 0 错误 |
| 2026-09-14 | P6-5 复盘→沉淀修复 + 研究 delta + 收尾实现与验收 | 零 schema 沉淀回流:`tradeSaveMistakeCore`/`positionSaveRiskCore` 建笔记后补 `CreateRelationship(笔记→entity/event,'references')`(新 `linkSavedNote`,失败如实报错不回滚);**顺修** `/reviews` 点旧期诊断会静默存错条目(核心函数改收 `snapshot`,API 加 `?snapshot=YYYY-MM-DD`)。前端 `ReviewPointList`(逐条 title+content+存为笔记,带期精确到该期)`/reviews` 重写;研究 delta:`useResearchDelta` hook(`buildDelta` 纯函数 diff 两份 done 的 price/scorecard/risk)+ `StockResearchDelta` 四列表(涨红跌绿、回撤中性、缺失显 —)+ `RISK_LEVEL_LABEL`;个股页插入「研究变化」。收尾:`ForceGraph`→`useForceSim` hook(281→59 行);`weekly.tsx` 抽 `SummaryPanel`(219→176) | `docs/phase6/stages/p6-5-reflection-delta.md` | ✅ **全过**:`go build/vet/test` + `tsc` + `build`;**存风险→笔记回流个股页「我的笔记」且 `/notes`**(3 实体+2 事件边,测后删);幂等;/reviews 单笔 save-mistake 回流实测;delta 两个真实 run diff 实测(评分+2/期末价+0.44/区间+15.03%/回撤−1.90%/风险中→高,测后删);`/graph` 9/4 不变;**e2e 冒烟 28/0**;Playwright 实测 0 console 错误;历史 note 补 1 条 references 边回填 |
| 2026-09-14 | P7 买入前速评 + 量价形态分析实现与验收 | 后端新 `analysis/patterns.py`(规则判定量价形态 + 60 日序列,分位自适应阈值,每条带 evidence)+ `profiles/prebuy.yaml`(express,去 industry 提速,全 6 维评分卡)+ `json_report`/`engine`/`plan`/`profile` 接线(零 schema,序列入 metrics JSONB);合成可选 `Options.RequireSynthesis` + POST `quick`(无 AI 也出确定性结论)。前端 `EChart` 注册 LineChart + `VolumePriceChart` 双轴图 + `usePrebuy` hook + `StockPrebuy`/`PrebuyVerdict`/`RiskList`/`PatternTags` 组件 + `FINANCIAL_ROWS`/`VALUATION_ROWS`(风险与财务**首次上屏**)+ `types`/`glossary` 补全 + 个股页首屏接入 | `docs/phase7/stages/p7-prebuy-patterns.md` | ✅ **全过**:`go build/vet/test`(含 `TestOrchestratorQuickNoProvider` 集成)+ `pytest test_patterns` 10/10 + `tsc` + `build`;CLI 真跑 600519 出 60 点序列(labels 缩量续跌 / divergence 齐全);POST `{prebuy,quick:true}`→done 且 `metrics.patterns` 非空;**实测 LLM 网关 429 快速模式照常出确定性结论**(骨架报告 + 评分卡);Playwright 速评卡 9/9(风险红线上屏 / 双轴图 canvas / 原地重跑不跳页)、全站 e2e 28/0、0 console 错误;缺数据如实显 `—` |
| 2026-09-16 | P9 研报体裁与版面 | 设计定稿:`/reports` 独立入口 + 单栏正文/左目录/三域标记版面 + `section_manifest` 章节清单(零 schema)+ 摘要前置(改 `markdown.py` 装配顺序)+ 主体轴表示(`sw\d{6}`/`\d{6}`/`macro:<key>`)+ 估值口径红线 | `docs/phase9/design/report-layout.md` | ✅ **已定稿**(2026-09-16,用户过定稿门;已定:独立入口 / 单栏+目录 / 三域标记 / 先定版面再写代码);含实测数据源结论(申万行业**无估值历史序列** → 估值只作横截面排名);实施阶段 P9-1 主体轴泛化 → P9-2 版面 → P9-3 行业数据管线;未写代码 |
| 2026-09-17 | P9-4 公司研报与个股分析拆分(#11)实现与验收 | 按**功能**拆分(非改名/改档案):**公司研报**(`company` 档案,full 8 节,财务/估值/行业/风险)vs **个股分析**(`stock` 档案,express 7 节,行情/量价/换手/形态)。四阶段:P9-4a `analysis/risk.py` 让 price/volume **可选**(`symbol`/`as_of` 改显式传入),公司研报不采行情时规则 4–7(负债/净利/公告/估值)纯基本面独立运行、规则 1–3 整条跳过不猜不补;P9-4b `report/sections.py` 股价章触发去 `company` 耦合(原「含 company 即多一个量价章」,零回归);P9-4c `add_fundamental_evidence` 补基本面 Evidence(无 bars 时 `_refresh_evidence` 被挡,同 P9-3 坑)+ 两个新 profile;P9-4d 前端 `StockResearch` 固定两块 + `StockProfileBlock` 独立发起。**设计外发现**:机检 #4 `time_boundary` 原只认 `price.period_days`,公司研报两处皆无 → 补 `financial_snapshots[].report_date` 兜底(并加「无边界仍须 FAIL」反向守卫)。端到端实测 **机检 6/6 通过**、`data_completeness` 100%、评分卡「中性」(证明无假「结论受限」)、风险项纯基本面。回归:**pytest 164 passed**(146 + 新 18)、`go build/vet/test` 绿、独立性检查通过、`tsc` 零错、`vite build` 成功。**零 schema** | `docs/phase9/design/company-analysis-split.md` §10 | ✅ **已实现并合并**(2026-09-17;PR #23 已合入 dev;未上生产) |
| 2026-09-17 | 修复 #3 放量异常日逐日明细 | 个股分析页「放量异常日」原只显天数(如 `4`),看不到哪几天/放了多少/放大几倍。**根因比 issue 所述的更深一层**:issue 判断「数据算好了、单纯没渲染」只对 `abnormal_volume_dates` 成立 —— 逐日**量/额**不在 `metrics.volume`(只有 `avg_volume_20d`/`total_amount` 等汇总),**放大倍数**需该日自身的阈值 `max(前20日均量×2, 前5日峰值)`(依赖滚动窗口,无法从落库汇总还原),而 issue 又要求「前端不重算」。故**引擎补产** `abnormal_volume_detail` 逐条 `{date, volume, amount, threshold, ratio}`,前端只做展示格式化(新增 `AbnormalVolumeDetail` 可展开逐日表,接入 Fact 区)。字段置 dataclass 末位给默认值,旧夹具不受影响。**零 schema**(`asdict` + `default=str` 自动落库)。未夹带:已核实 `price.py` 的 `limit_up_days`/`limit_down_days` **只有计数无日期数组**(与本次不同类,需另开 issue) | PR #24 | ✅ **已实现并合并**(2026-09-17;`pytest` 169 passed = 146 + 18(P9-4) + 5(本次);端到端验证落库回读与「阈值取该日自身」;`tsc`/`vite build`/`go build·vet` 绿;未上生产) |
| 2026-09-17 | P9-5 宏观研报(#13)实现与上生产 | 主体从「个股/行业」扩到**宏观维度**(`macro:<key>`,如 `macro:cn_cpi`)——**新建一整条数据管线**(非加 profile):P9-5a 宏观主体解析 + 指标 provider / P9-5b `analysis/macro.py` 四维分析 + 报告装配 / P9-5c GDP 季频·累计口径收口 + 冒号主体码全链路实测 / P9-5d 前端宏观触发 UI(`MacroDimensionPicker`)+ 封面「数据截止 / 报告生成时间」双行区分。关键实测:issue 建议的 `*_yearly` 接口**陈旧一年**不可用 → 改 NBS 口径源;CPI/PPI「当月」是**指数**非百分比;GDP 是**累计口径**;四源全部**新→旧序**;**无发布日**可写(写即编造)。**零 schema**(`macro:` 形态 Go 侧已定型) | `docs/phase9/design/macro-research.md` | ✅ **四阶段全部完成并上生产**(PR #28 已合,镜像 `e5896f3`);CPI/PPI/M2/GDP 四维全通;后续两修:`7365aff`(封面数据源不再套用个股默认值)、`c0feee9`(代码掩码不吞 6 位小数的整数部分) |
| 2026-09-18 | 修复 #4 换手率口径 + #26 资金面(龙虎榜)链路 | **#4**:换手率口径实测收口 —— 与同花顺一致,修正误导性措辞(明确标注「仅流通口径,拿不到同花顺自由流通口径」)。**#26**:资金面(龙虎榜)链路接线 + 修 3 处假数字缺陷 | PR #33 / PR #34 | ✅ 均已合并(2026-09-18;`a024beb` / `0094bdb`) |
| 2026-09-18 | 修复 #6 实体名去空格 + #19 账户资金汇总 | **#6**:实体名去「字间空格」归一 + 交易导入按去空白匹配去重。**#19**:持仓截图导入采集账户资金汇总(总资产/总市值/浮动盈亏/当日参考盈亏)→ migration **0013 `account_snapshots`** + `AccountCard` 组件 | PR #35 / PR #36 | ✅ 均已合并(2026-09-18;`c0c1293` / `f10ac94`);0013 是**第 13 个**迁移 |
| 2026-09-20 | 修复 #37 消息排序 + #38 来源外链 | **#37**:消息排序缺失 —— 重要消息默认时间升序(最新沉底),两个 tab 均无排序控件 → 补排序控件 + 默认倒序。**#38**:事件/快讯来源不可点击 —— 详情已画外链图标却是假链接,快讯原文 URL 未下发 → 后端下发原文 URL + 前端真实外链 | PR #39 / PR #40 | ✅ 均已合并(2026-09-20;`e06a637` / `cf5bac0`) |
| 2026-09-20 | **容器拆分 #47:单镜像 → 四镜像** | `piks-tools`(934MB,同背 nginx + Go web + 12 CLI + Python 深研)拆为 `piks-gateway`(纯 nginx)/ `piks-web`(纯 Go API)/ `piks-tools`(9 管线命令)/ `piks-research`(Python + 深研队列 worker);单 Dockerfile 多 `--target`。**唯一运行时行为改动**:UI 深研从「web 进程内 `os/exec python3`」改为 **DB 队列**(migration 0015 `quick`/`days` 两列 + `FOR UPDATE SKIP LOCKED` 认领 + `LISTEN/NOTIFY` 唤醒 + 租约回收孤儿)+ `cmd/research-worker` 常驻。deploy.sh 重写为**按镜像自身输入的依赖闭包 hash 派生版本**(前端取整个 `frontend/` tree,Go 取 `go list -deps` 闭包 —— 手列清单会静默漏,实测漏过 `internal/model`;工具链缺失回退整树 = 安全方向)+ 干净树门控 + 迁移/启动顺序硬约束 + **回滚快照一次性**(存在即跳过,`-single` 从已知拆分前 tag 取而非 `latest` —— 实施中 `latest` 曾在第二次部署静默改写成新像);nginx 改 `resolver` + 变量式 `proxy_pass`(否则 web 每次重建后 gateway 永久 502) | issue #47;设计 `docs/phase10/design/container-split.md` | ✅ **已上生产**(2026-09-20,栈 `v0.0.0-616f31b`:gateway `c4377ad` / web `bb80388` / tools `602d148` / research `2426e05`);13 页全 200、深研 pending→done 33s、上传 10MB 边界(2MB 通 / 11MB 413)、**resolver 实测**(重建 web 后 gateway 未重启仍 200);回滚镜像 `rollback-pre-split`(+ `-single` 保留旧单镜像) |
| 2026-09-20 | **跨源同事件判定 #48(T2)** | `NormalizeTitle` 前置 `stripBracketWrap`:抽取 `【】` 内嵌标题前缀(≥6 字才当标题,否则视为栏目标签),把因「东财/新浪带正文段」而稀释到 0.165 的**同一事件**相似度**还原**(不改阈值 0.7)。`GenCandidates` 拆 `autoGroups`/`llmPairs` 两纯函数便于离线取证。**簇内来源可见**:`store.ListClusterSources`(含 `merged` 成员的机构名 + 原文链接 + 上游一级源,按机构去重)+ `/api/v1/events` 仅跨源簇(≥2 机构)下发 `cluster_sources` + 前端详情/表格展示「N 家印证」。**零 schema** | issue #48;设计 `docs/phase11/design/event-cross-source.md` | ⬜ **已合并并上生产**(PR #52 已合,2026-09-20 随四镜像栈上生产)。校准(163 条 6 源真实标题):重复对 **36→65** @0.7,新增 29 对逐条核对**均为同一事件**;同股不同批次证书判别边界 0.515(距 0.7 留 0.185 余量);8 单测 + 真库集成测 + API 投影测过;⚠️ **LLM 确认分支因 AI 网关 `CreditsError` 未取真值**(不影响结论,多数跨源对走零 token 的高置信直合) |
| 2026-09-20 | **漏报 / 冲突检测 #49(T3)** | `internal/cluster/conflicts.go`(新):单位白名单锚定数字 + 骨架 Jaccard(只删数字**不删汉字**)+ 门控 **0.6** + 两侧各恰 1 个不同值才报,**输出双方原文**。`source_count`(机构级计数,未聚类=1;⚠️ `cluster_sources` 是 `omitempty` **反推不出**单源,会与未聚类混)。`ListClusterMembersWithFacts`(故意**不**按机构去重)+ `toEventItem` 4 参 + `event_conflicts,omitempty` 默认附带。前端:`EventConflicts`/`EventSources` 新组件 + 详情 chip「单一来源」(`.st-dim`)+ 表格对称徽标 + `glossary` 两词条。`deploy.sh` 补 lab 侧 `scripts/` 同步(**编排漂移根因**:lab `pipeline.sh` 长期停在 `-driver dongcai`,6 源一条没采)。**零 schema** | issue #49;设计 `docs/phase11/design/event-cross-source.md` §8~§12 | ✅ **已合并并上生产**(PR #54 已合,2026-09-20 随四镜像栈上生产)。门控按**生产 1410 条真实事实句**标定:随机句对误报 **0.0750% @0.6**(无门控 5.65%)、8 组同标题 39 句对 **0 差异**、对抗集 **0/4**、真阳性 3/5。已知召回边界(远改写 J≈0.47~0.50 落误报带内,规则不可分)**钉测**留给 #45。⚠️ **生产无跨机构簇**,**663/669 = 99.1%** 事件会标「单一来源」—— 标签按设计正确但属噪音,**UI 价值取决于 lab 多源采集落地**(本次未执行 lab,用户指示待 issue 处理完)。上游缺陷 `cmd/cluster` 默认 `-limit 100` 静默漏聚类 **另开 issue #53** |
| 2026-09-20 | **gateway 冷启动挂死 #55(P0)** | `nginx:alpine` 的 entrypoint 脚本 `10-listen-on-ipv6-by-default.sh` 在 Alpine 分支跑 `apk manifest nginx` —— 该命令在**冷容器**里访问 apk 仓库(`dl-cdn.alpinelinux.org`,Fastly),该域名从 lab 解析不稳 → 脚本卡死 → entrypoint 阻塞 → **nginx 永不启动**,而容器状态仍显示 `Started`(**静默挂死**,只有探端口才发现)。该脚本的唯一作用是把**打包自带**的 `default.conf` 校验 checksum 后自动补 `listen [::]:80;`,而我们的 `default.conf` 是自写的 `configs/nginx.conf`(已显式 `listen 80;`)→ 校验必然走「differs from packaged」分支,纯白跑却给启动挂了外网依赖。`Dockerfile` gateway 阶段 `RUN rm -f` 删掉该脚本(零功能损失)。**同时修 `deploy.sh` 的 hash 输入集缺 `Dockerfile`** —— 否则改了 Dockerfile 也不改 tag,`build_image` 跳过构建,**修复本身静默失效** | issue #55 | ✅ **已合并并上生产**(PR #56 已合;**2026-09-20 晚随四镜像栈上生产**,gateway PID1 实为 `nginx: master`)。实测:新容器以真实 entrypoint 启动后 `nginx: master` = 1,容器内 `wget http://web:8090/api/v1/dashboard` 返回真实 JSON;冷容器里 `apk manifest nginx` 实测 **10.7s** 才返回(DNS transient error)。(修复生效前生产曾靠临时手动 `docker exec -d piks-gateway nginx` 续命;**现已随四镜像栈部署,该临时手段已撤**) |
| 2026-09-20 | **部署构建慢 #57** | 两个叠加问题:① `deploy.sh` 默认 PyPI 源 aliyun **对本机仅 ~20 KB/s**(同机 curl 命中其 CDN 可达 4 MB/s,pip 连接恒落坏节点;换 UA 无效),清华源同机同时刻 **5.58 MB/s**(低 280 倍)→ 默认改清华;② `Dockerfile` research 阶段 `ARG GIT_SHORT`/`ENV PIKS_GIT_SHORT` 排在 `pip install` **之前**,而 GIT_SHORT 每次提交都变 → 每次部署都让 pip 层缓存失效、重下全部依赖 → 移到 pip 之后。逐条排除:非「没走国内源」(日志明写 aliyun)、非 DNS 劫持(解析到国内 IP)、非代理(BuildKit 实测不注入)、非 Docker NAT(容器/宿主延迟均 ~13ms)、非整体出网慢(容器内清华源秒下) | issue #57 | ✅ **已合并并上生产**(PR #58 已合,2026-09-20 晚随四镜像栈上生产)。验证:清华源构建成功(依赖装齐 akshare 1.18.96/pandas 3.0.6);`GIT_SHORT` 由 `test` 改 `zzz9` 重建 → pip 层 `Using cache`、层 ID 不变(缓存修复确证);`bash -n` + `check-image-topology.sh` 通过。⚠️ **属环境事实非永久结论**,换线路需重测 |
| 2026-09-21 | **空库白屏 #59(空态契约)** | `/api/v1/dashboard` 在无市场快照时返回 `"market":{"indices":null,…}`、`"snap_history":null` —— **Go 零值 slice 经 `encoding/json` 编码成 `null`**,而前端 `types.ts` 把 `indices`/`ladder`/`snap_history`/`top_events`/`task_runs` 声明为**非可选数组**并直接 `.map()`(`WatchOverview.tsx:29`)→ 空库整页白屏,连带 `/market`、`/ladder`、`/stock/:code` 同类崩。**2026-09-20 清库把它从「恒不成立的分支」逼成现实**(此前生产总有一两天快照,`len(snaps)>0` 恒真)。**修法(后端修,用户定)**:① `apiDashboard` 四个数组字段构造时空 slice;② 无快照时 `Market` 用新的 `emptyMarketSnapshot()` (字段齐全、三数组为 `[]`);③ 同类 sweep —— `toEventItem` 的 `facts`、`toEntity` 的 `aliases`(`json.Unmarshal` 留 nil 的坑);④ **全端点空库实测** `/dashboard` `/events` `/entities` `/relationships` `/flashes` `/notes` `/recon` `/reviews` `/weekly` `/chat` `/research-runs` `/watchlist` `/stock/:code` `/trades` **零 `null`**(指针语义的 `account`/`entity` 除外,那是有意的缺席)。**前端不动**(`?? []` 防御列为独立加固项,不在本 issue 范围) | issue #59;`docs/架构总览.md` §6.2 新增「数组空态编码」约定 | ⬜ **已实现待审核**(PR 未合,dev-only)。回归测试 `internal/web/api_dashboard_empty_test.go` 断言**真实 JSON 编码结果**(非 Go 层长度 —— 缺陷正在编码这一步):空态三数组恒 `[]`、dashboard 四数组无 `null`、并**反证**零值 slice 确会变 `null` 以钉住机制。另有真库集成测试 `internal/web/api_dashboard_integration_test.go`:在**临时库**跑真实 handler(缺陷在「`len(snaps)==0` 这条从未被走到的分支」上,单测照不到),**已验证回退修复后该测如实失败**(6 处 null 全部报出)。端到端另做:建空库 `piks_verify59`(migrate 15)+ 起 web + 真实 curl 全端点零 `null`;`react-dom/server` 临时脚手架渲染空库 market 形状通过、喂 `indices:null` 则抛异常(反证白屏根因,脚手架验证后已删)。`go build/vet/test` 全绿 |
| 2026-09-21 | **事件类型枚举漂移 #61** | 消息页事件类型**前后端枚举不一致**:后端权威枚举(`internal/extract` 的 `allowedEventTypes` / JSON Schema `enum` / `0001_init.sql`)是**9 值**(policy/earnings/industry/accident/international/tech/macro/company/other),而前端自存一份**8 值**表(`lib/constants.ts` 的 `EVENT_TYPES`、`lib/format.ts` 的 `EVENT_TYPE_LABEL`、`EventTable.tsx` 的 `TYPE_TAG`)—— 其中 **6 个 key 全仓零出现**(product_launch/supply_agreement/industry_event/investment/sales_data/rumor)是前端臆造的。后果:① 后端 8 个真实类型有 7 个在前端表里查不到 → 表格/详情/研报/笔记关联处**露英文原值**;② 类型下拉 8 项里 **6 项永远筛不出东西**,而 industry/accident/international/tech/macro/company 六类**根本无法筛**。**两步修**:(1) **数据层** —— 建立唯一真源 `internal/model/eventtype.go`(`EventTypes []{Key,Label}` + `EventTypeKeys`/`EventTypeCSV`/`IsEventType`/`EventTypeFallback`),`extract` 的校验集与 JSON Schema **由它派生**(连兜底桶 `other` 也取自真源,不再字面量手抄);(2) **单一真源架构** —— 新增 `GET /api/v1/event-types` 下发 `[{key,label}]`,前端 `lib/eventTypes.tsx` 的 `EventTypesProvider`(App 根部挂一次)+ `useEventTypes()` 消费,同时**删除**三处本地表。关键取舍:**只下发 key 不够** —— 前端仍会留一张 label 表,后端加类型照样回落英文,漂移只修一半,故 key **与** label 一并下发;配色**不新增色**,按后端下发顺序循环复用既有 5 个 tag 色(`t-mix`/`t-idx`/`t-ev`/`t-bond`/`t-gray`),后端加类型前端零改动。顺修 `weekly/Sections.tsx` 事件类型**裸渲染 `{e.event_type}`**(绕过所有映射层,直接显英文 key) | issue #61;新增守卫 `scripts/check-event-type-parity.sh`;设计 `docs/架构总览.md` §5.2/§6.2/§7.3 与不变量 #20 | ⬜ **已实现待审核**(PR 未合,dev-only)。守卫断言四项(真源自身合法 / `extract` 零 key 字面量 / 前端无本地枚举表 / 路由在位)并**已做反证**:6 处故意植入的错误(漏 label、重复 key、手抄 CSV enum、删真源引用、前端重建映射表、臆造 key、删路由)**逐条实测被拦**。⚠️ 前端判定**不能只看 key 字面量** —— `industry`/`macro`/`company` 与实体类型、研报 profile 等**正交枚举**撞名(那几处本就该在前端硬编码),故按「key 与其**事件 label** 同行」判定。`go build/vet/test`、`tsc --noEmit`、`vite build`、既有两守卫均绿;**未做真库比对**(dev 侧连不到 lab 的 PG,本机 5432 不可达) | `cmd/cluster` 默认 `-limit 100` + `ListUnclusteredEvents` 按 `created_at ASC` 只取最旧 100 条 → 超出部分**不报错、不告警、不进日志**,`task_runs` 仍记成功;积压时新事件**永远轮不到**,并被 `source_count` 语义误标成「单一来源」(把「管线没跑到」显成「只有一家在报」)。**生产实测坐实**:首 100 条候选 `autoGroups=0/llmPairs=20`(几乎全是实体分支噪音),而全量 190 条才出 `autoGroups=6/llmPairs=199` —— 旧窗口**一条真重复都碰不到**。**修法(零 schema)**:`-limit` 默认改 **0=不限**(取全部);显式传 `-limit>0` 时用新 `store.UnclusteredEventsTruncated` 探测并把 `limit`/`truncated` 记进 `task_runs.meta` + 打 WARN。⚠️ 同时发现**重审视 Pass 也补不上**(`reexamine.go` 对「全未聚类分量」`survivor==-1` 直接 skip,假设正常 pass 已处理)。**生产量化**(190 条未聚类 + 真实 LLM 确认 `deepseek/deepseek-v4.1-flash`):去掉 limit 后 **13 簇 / 14 条被合并 / 163 仍单条**(vs 生产现状 6 簇 / 13 条) | issue #53;设计 `docs/phase11/design/event-cross-source.md` §13 | ⬜ **已实现待审核**(PR 未合,dev-only)。`go build/vet/test` 全绿 + 新增真库集成测 `TestUnclusteredEventsTruncated`;**生产存量 190 条待补跑一次 `cluster` 才生效** |
| 2026-09-22 | **/events 路由绑错组件 #73** | 侧栏「消息」指向 `/events`,但该路由的 element 是 `pages/events.tsx`(纯「重要消息」列表、**无 tab 条**),而三 tab 容器页是 `pages/messages.tsx`(挂在 `/flashes`)→ **侧栏永远落到没有 tab 的页面,公告 tab 从任何导航入口都不可达** → **A 层公告分级(#68 S1)在生产零可见度**(后端/接口全对,前端进不去)。引入点 `d0acf5c`(P8 `/m/upload`,2026-09-14)——主题与 `/events` 无关却顺手翻转了该行,早于 #68 上线,故与 A/C 层升级**无关**。**修法(一行路由)**:`/events` element 改回 `<Messages />` + 删因此闲置的 `import Events`;⚠️ `pages/events.tsx` **不删** —— 它是 containers 里「重要消息」tab 的渲染体(`messages.tsx` 内部 import),本修复只是把路由指回容器页。附带收益:`/events/:id` 详情返回后落到**带 tab 条**的消息页 | issue #73;`docs/架构总览.md` §7.1 路由表 2 行 + 脚注 | ⬜ **已实现待审核**(PR 未合,dev-only)。`tsc --noEmit` + `vite build` 绿;**把断言补进既有冒烟脚本** `frontend/scripts/e2e_check.mjs`(断言**渲染后的 DOM**:`/events` tab 条 = 重要消息/快讯/公告、点「公告」触发 `/api/v1/announcements`),冒烟 **32 通过 / 0 失败**。**已做反证**:把 `/events` 改回绑 `Events`(错误注入先回读确认落盘)→ 新断言**如实失败**(`tabs=[全部状态,已确认,待复核]`),而既有 `nav/events` 断言**仍 PASS**(`spa=true` —— 正是它拦不住本 bug 的原因),还原后转绿。**验收判据教训**(记入本行):SPA 路由正确性**不能靠 HTTP 200 或 bundle grep** —— `try_files` 对任何路径都返 index.html,200 无鉴别力,既有导航断言只查 `#root` 挂载同样无鉴别力;「某接口浏览器请求数 = 0」才是 UI 不可达的强信号 |
| 2026-09-21 | **A 层公告分级(#68 S1)** | 公告 ~1200 条/交易日全平铺「看不过来」→ 新增 `internal/announce` **纯规则标题分级**(`must`/`important`/`routine`/`noise`),采集时落 `raw_documents.grade`(**迁移 `0017`**:列 + CHECK + 部分索引);判定次序 **否定式 → 中介机构衍生件 → 必读 → 重要 → 噪音 → 常规 → 默认常规**(前两步不可省:「最近五年**未被**处罚」含 must 词语义相反;「重大资产重组…**核查意见**」是券商衍生件,一家 4 份占满必读位)。前端公告 tab **默认只显必读+重要**,`GradeFilterBar` 给「查看全部(另有 N 条)」;`AnnounceRow` 仅 must/important 挂 chip;`gradeMatch` 把 `grade=NULL` 并入 routine → **只折叠、不隐藏**(红线);筛选条如实标「按标题规则自动判断,非官方认定」。⚠️ **只落标签、不进 LLM** —— `status='collected'` 逐字不变,公告仍零 token(分级**不产生成本收益**,价值纯展示层)。`cmd/collector/main.go` / `internal/store/raw_documents.go` / `internal/model` / `internal/web/api_v1.go` + 前端 `constants.ts`/`types.ts`/3 组件/公告页 | issue #68(S1);设计 `docs/phase11/design/announcement-grading.md`;上游 `docs/phase11/design/source-tiering.md` §3 | ⬜ **已实现待审核**(PR 未合,dev-only)。校准(**真实**单日 1196 条标题):**必读 14 / 重要 226 / 常规 678 / 噪音 278** → 可折叠 **79.9%**(跨 5 交易日 79.9~87.5%);Go 规则与 Python 校准**逐条一致**;单测钉四档 + 三处易错点 + 占比区间(75~90%);API 单测含「未分级不隐藏」反向用例;真库集成测往返;迁移全量套用 17 个 + CHECK 拒非法值。**E2E**(真驱动 `cninfo-announce` + 真接口 + scratch 库):`new=151` → routine 107 / noise 24 / important 18 / must 2,`collected` 151 全保持;必读桶(广和通重大资产重组 / *ST发展预重整)与噪音桶(工商变更/独董离任/保荐代表人变更/律所法律意见)语义逐条正确;插 `grade=NULL` 历史行后 `grade=routine` 由 107→**108**(不消失)而默认视图**仍 20**(不混入)。`tsc --noEmit` + `vite build` 绿,已抽 `AnnounceSearch` 破循环依赖 |
| 2026-09-21 | **entity-build 每轮必崩 #71** | `events.affected` 存在 JSON `null` 行 → `internal/store/relationships.go` 的 `ListAffectedTermEvents` 用 `jsonb_array_elements_text(e.affected)`,遇标量报 **SQLSTATE 22023「cannot extract elements from a scalar」**,`entity-build` **整条命令崩**(实测当日 **18/18 全失败,且历史从未成功**;不是跳过坏行,是命令级失败)。**根因比表面深一层**:写入侧 `internal/extract/extract.go` 的 `mustJSON` 是裸 `json.Marshal`,而 **`json.Marshal(nil 切片)` 返回字面量 `null` 且 `err==nil`** → 回退分支不触发;`internal/store/events.go` 的守卫只判 `len(affected)==0`(而 `len("null")==4`)**看不穿、放行入库** —— 故写入路径**仍在漏**(非单纯历史脏数据)。⚠️ 该守卫自 events 表**首个提交 `186b6f2`** 就存在 —— 「先有守卫、仍漏数据」证明守卫**无效**,不是回归。**修法三层**:① 写入侧 `mustJSONArray`(nil/空 → `[]`)+ `store.emptyToArrayIfScalar`(按 **JSON 语义值**判定,非长度);② 消费侧加 `jsonb_typeof(...)='array'` **纵深防御**(再有标量也只跳过、不崩命令);③ 历史脏行由幂等脚本 `scripts/fix-events-affected-null.sql` 清理(**不新增迁移编号** —— 数据修复不污染迁移序列)。`Facts` 与 `Affected` 共用同一 helper,当前生产 `facts` 993/993 全数组纯属侥幸,**同源同类一并修正** | issue #71;`docs/架构总览.md` 不变量 #23 + §6.2 | ⬜ **已实现待审核**(PR 未合,dev-only)。**真库复现坐实**:`jsonb_typeof` 分布 = `array` 992 / `null` **1**;加守卫返回 1902 行,不加即复现 22023;触发行 `ec37858b-…`(2026-09-20 15:32 落库)。新增单测 2 个(钉死 nil→`[]` 非 `null`、9 种标量/畸形输入的归一)+ **gated 真库集成测**(插 `affected='null'::jsonb` 后 `ListAffectedTermEvents` 不得报错,且好行仍被收割);**已做反证** —— 移除守卫后该集成测**如实失败**并报出与生产同一条 22023,还原后转绿;修复脚本在临时库验证 `UPDATE 1` → 二次执行 `UPDATE 0,0`(幂等)。`go build/vet/test ./...` 全绿(10 包 ok) |
| 2026-09-22 | **冷构建慢:镜像源换 USTC** | 用户反馈「Python 太慢」。**逐层拆解冷构建**(`--no-cache --progress=plain`)定位到**不是 Python、也不是构建本身,而是镜像源选错**:① **pip 侧** —— 完整装 `research/requirements.txt` 逐源实测:aliyun **202.7s** / 清华 18.2s / pypi.org 9.8s / **USTC 8.8s**(aliyun 慢 23 倍,#57 时代结论仍成立;**但清华也不是最优,USTC 再快 ~2 倍**#57 当时未测 USTC);单包下载复现同序(aliyun 32.8s vs USTC 1.8s)。② **apt 侧** —— `Dockerfile` research 阶段 `ARG APT_MIRROR` **硬编码 aliyun**(#57 只改了 pip 侧,**漏了这里**),容器内 `apt-get update` 三轮稳定复现:aliyun **~18.6s** / 清华 ~6.6s / deb.debian.org 2.2s / **USTC ~1.8s**(慢 10 倍)。**修法**:`deploy.sh` 的 `PYPI_INDEX` 默认 清华→**USTC**;`Dockerfile` 的 `APT_MIRROR` 默认 aliyun→**USTC**(USTC 的 `/debian-security` 路径与 deb.debian.org 一致,sed 替换无需额外处理)。**端到端冷构建实测:157.9s → 37.7s(快 4.2 倍)** | 承接 #57(该 issue 已合,本次为其未覆盖的 apt 侧 + 更优源);`docs/架构总览.md` 构建章节 | ✅ **已实测**。⚠️ **澄清范围**:research 日常部署(仅改 Go/前端)**全层 CACHED,~0.8s** —— 慢**只发生在冷构建**(requirements.txt 变更/换构建机/清缓存),已如实记录。⚠️ **属环境事实非永久结论**,换构建机/换线路需重测;`PIKS_PYPI_INDEX` / `APT_MIRROR` 仍可覆盖 |
| 2026-09-21 | **apk 层缓存击穿致构建挂死 #71** | 升级时构建在 `alpine` 阶段 `apk add` 处 **exit 137(SIGKILL)** —— 非 OOM(空闲 13Gi)、非代码错。两层叠加:① **缓存被击穿** —— `web`/`tools` 阶段把 `ARG GIT_SHORT`/`ARG PIKS_VERSION` 排在 `RUN apk add` **之前**(`Dockerfile:86-91`/`104-109`),而 GIT_SHORT **每次提交都变** → apk 层缓存每次升级都失效、每次重装(对照:`research` 阶段早已在 #57 把 ARG/ENV 挪到 `pip install` **之后**;`gateway` 用 `nginx:alpine` 不再跑 apk,故不受影响);② **Alpine CDN 不可靠** —— `dl-cdn.alpinelinux.org` 从构建机实测 463KB 索引 **~11KB/s 且会中途挂死**。**修法**:apk 源换 `mirrors.aliyun.com`(实测**容器内可达**;dl-cdn 与清华在容器内**均不可达**)+ 把版本 ARG/ENV 移到 `apk add` **之后**(照 `research`/#57 成例)。`APK_MIRROR` 可 `--build-arg` 覆盖 | 承接 #71;`Dockerfile` web/tools 阶段 | ⬜ **已实现待审核**(PR 未合)。**属环境事实非永久结论** —— 换构建机/换线路需重测;另注:容器内**够不到宿主 10808 代理**(该代理在 Windows 侧),故容器内取数只能走直连镜像源 |
| 2026-09-22 | **D 层热榜(#68 从零新建)** | 热榜源与消费者**均需新建**(前置核查:热榜源与 `morning-brief` 在代码库中**均不存在**)。**2 源**:同花顺话题榜(15 条,`dq.10jqka.com.cn`,含关联个股)+ 财联社首页热文(13 条,SSR 解析);常驻 `hot-topic` 服务盘中**每 30 分钟**,日管线补一发收盘态。**实现**:新 `HotTopicSource` 接口(`internal/collector/hottopic.go`,**刻意不复用** `Driver`/`RawNews`,防「塞进事件链路」)+ 独立表 `hot_topic_items`(**迁移 0018**)+ `cmd/hot-topic` 常驻 + `internal/store/hot_topics.go` + `GET /api/v1/hot-topics` + 独立页 `/hot-topics`(导航「发现」组,12→13 项)。🔴 **方案 A:两源各出各的、不合并、不加权、不排名** —— 实测 **615 对标题同题对 = 0**(J≥0.5 仍 0;粒度不同:题材/事件 vs 文章/复盘),混排必然误导,且 API **按源分列出参**使前端结构上无法跨源混排。⚠️ `rank` 取**上游数组下标 + 1**、跳行留空号、**不得重排**(实测财联社数组顺序 ≠ `readNum` 降序:榜位 7 = 507051 > 榜位 1 = 304251,故 `rank` 是事实、不可按热度重排);SSR 结构漂移**报错不空成功**(#64 教训);`hot_value` **`NULL ≠ 0`**;护栏**复用 C 层** per-host 三护栏(同花顺话题榜走新 host,独占状态)。`extra` 落库留档但**不下发**(无消费方)。展开:compose 新增 `hot-topic` 服务(仍复用 `piks-tools` 镜像,四镜像不变)、`deploy.sh` rollout 排在 **migrate 之后**(依赖 0018 表)、`pipeline.sh` 补收盘 one-shot、`TAG_TOOLS` 依赖闭包并集已含 hot-topic | issue #68(D 层);设计 `docs/phase11/design/hot-topic.md`;上游 `docs/phase11/design/source-tiering.md` §2/§6 | ⬜ **已实现待审核**(PR 未合,dev-only)。`go build/vet/test` 全绿(含真库集成测);`tsc --noEmit` + `vite build` 绿;E2E 冒烟已接入。**方向性偏离已如实登记**(design §7):采集频率一天两次→**盘中每 30 分钟**、`morning-brief` 消费者→**独立页**(该消费者本不存在) |
| 2026-09-22 | **公网暴露 `piks.5home.online`(#78)** | PIKS 全栈**仍跑 lab**,新增**阿里云宿主边缘 Nginx**(443,SNI 分流)+ **frp 隧道**回源:`公网 → 阿里云 Nginx → 127.0.0.1:17010(frp stcp visitor)→ frps:17000 → lab frpc → lab 127.0.0.1:8090(piks-gateway)`。**stcp + secretKey**:visitor 只绑宿主 loopback,回源口**公网不可达**(实测 connection refused)→ Nginx 是唯一入口。边缘配置在阿里云 `/root/host-infra`(**非本仓库、不入 git**):新增 vhost 模板 `piks.conf.tmpl`(`__PIKS_DOMAIN__`)+ `edge.env` 的 `PIKS_DOMAIN` + `deploy.sh` 渲染规则;证书复用通配符 `*.5home.online`。⚠️ 模板**不声明 `default_server`**(归 ai-gateway,否则 `nginx -t` 报重);本 vhost 显式 `client_max_body_size 32m`(公共 snippet 不设,默认 1m 会让截图上传 413)。**本仓交付物 = 文档**(架构总览 §9.5 + README) | issue #78;`docs/架构总览.md` §9.5 | ✅ **已上线并端到端验证**(2026-09-22):`https://piks.5home.online/` + `/api/v1/dashboard` 均 **200**;`http://47.116.65.140:17010` 连接被拒(loopback-only);证书 SAN `*.5home.online`。⚠️ **当前无鉴权(裸奔,用户已当面接受此临时风险)** —— `internal/web/server.go` 全路由无 auth,公网可读写全部数据 + 触发 LLM 计费;缺口跟踪 **#78**,补鉴权前须确认 `ai_daily_token_budget` 闸生效(依赖 #75) |
| 2026-09-22 | **管线无退避重跑(#76)** | 实测 `pipeline-2026-09-21.log`:**31 个 tick / 34 轮完整链**,`entity-build` **34/34 FAIL**(#71 的 null 崩溃当时未部署修复),`.done` 只在**全链 ok** 时生成 ⇒ stamp 永不生成 ⇒ 每 15 分钟重跑整条链(**同时反复触发全量 `cluster`,是 #75 token 爆点的放大器**);另有单步无超时 —— `cluster` 一次挂 **2h25m** 堵死串行链,后续步骤顺延到凌晨。修 4 项:① **`flock -n` 单实例锁**(消灭并发管线);② **单步超时** `timeout --signal=TERM --kill-after=30s ${PIKS_STEP_TIMEOUT:-1800}s`;③ **步骤台账** `.done`(全链)→ `pipeline-$TODAY.done.d/<step>.done`(每步),下一 tick **只跑未完成步骤**;④ **失败分型 + 退避** —— `deterministic`(SQLSTATE/constraint)/`quota`(429/额度)**立即放弃**记 `== ABANDON`,瞬断/超时计次至 `PIKS_MAX_RETRY`(默认 5)当日放弃。`health.sh` 同步改按台账判定(含旧格式 stamp 过渡兼容) | issue #76;`docs/架构总览.md` §8.1;`docs/数据源总览.md` §3;`docs/phase3/design/prod-deploy.md` §9.1 增补;`configs/.env.prod.example` | ⬜ **已实现待审核**(PR 未合)。**沙箱演练实测四项验收全过**(替身 docker,仓库脚本仅换 `C=` 一行):① 确定性失败 → 首次即 ABANDON、后续 tick **零重跑**(「无待跑步骤」);② 单步假装挂 3600s + 超时 4s → 整轮 **5s** 走完,后续步骤照常 ok;③ 并发第二实例被 `flock` 挡下并记 cron.log;④ 429 类首轮 ABANDON、瞬断类计次 5 次后 ABANDON,其余步骤全程只跑一次。`bash -n` 两脚本全过。⚠️ `scripts/` 由 `deploy.sh` 同步到 lab,**随下次部署生效** |
| 2026-09-22 | **cluster 不收敛 + 预算无闸 (#75)** | 生产单日 `cmd/cluster` 实耗 ≈ **2350 万 token**(账面 1173 万),把自建网关打成持续 429 并扩散到 `worker` 抽取。三环根因:① 候选池 = `cluster_id IS NULL` 而 `BuildComponents` 只留 `len(m)>=2` ⇒ **从未匹配上对端的事件永不拿 cluster_id**、每轮重跑(生产 `unclustered=1915/4083`);② `ai_daily_token_budget=0` 而守卫是 `if >0` ⇒ **0 = 无护栏**(不是「不限预算」);③ 重审视池无界(全部活跃簇,每日 +180)。修:迁移 **0019** 加扫描水位 `events.cluster_scanned_at`(扫完无对端即落戳)+ **成对**把「窗口内已标记事件」并入重审视池(否则永久漏召回)+ 重审视**时间窗** `-window-days`(默认 7,按 `MAX(member.created_at)` 口径)+ 修 2 个潜伏 bug(空池早退杀死重审视;更早的单成员簇吃掉多源簇致 LLM 标题丢失)+ **补 4 处账本盲区**(重审视 token 因 map 值拷贝漏计、entity-build `_ = tokens` 丢弃、chat/expand 连 task_runs 行都不写、重试时**报错也累加** token)+ 429 可辨识(`ai.APIError{Status}.Retryable()`)+ 退避 + 日界统一北京午夜(`config.BeijingMidnight`) | issue #75;`docs/phase2/design/cluster-quality.md` §3.4/§6.1;`docs/架构总览.md` §8.1 | ⬜ **已实现待审核**(PR 未合,dev-only)。✅ dev 实测收敛:**连续两次 `cluster` 第二次 `events=3→0`**(池排空);集成测试过(标记后不进池但仍在 `ScannedEventsSince`、窗口过滤、已归簇不被标记);`go build/vet/test` 全过;新增 `-window-days` / `-dry-run` flag。⚠️ **未部署 lab**(随镜像重建生效);`ai_daily_token_budget` 仍为 **0** ⇒ 护栏**未开**,须由用户经 `/settings` 手填(建议 1000000);**LLM 判定分支准确率至今零实测**(校准期网关欠费)⇒ 任何「改判定策略/减量」改动须先补真值 |

| 2026-09-23 | **剥转载 P-1(issue #83)** | 事件管线(issue #83)的**根因层**:印证度三级(单一来源/多家印证/广泛报道)整个建立在**机构数** `source_count` 上,而 `raw_documents` 去重键含 `source_id` ⇒ **同一篇通稿被两家近逐字转载会落两行** ⇒ 机构数被刷高 ⇒ 计数不实、分级全废。**修在读路径**(与 `cluster_sources`/`source_count`/`event_conflicts` 同一既有范式,**零 schema / 零迁移 / 零管线步骤**):新 `internal/cluster/reprint.go` 纯函数 —— 对簇内各机构**代表正文**(`ListClusterSources` 增读 `rd.content`)算字符二元组 Jaccard,**≥ 0.85** 视为同一篇稿、并查集合并为一源;`toEventItem` 回填 `independent_count`(组数)与逐来源 `reprint` 布尔。**`source_count`(机构数)语义不变** —— 印证度三级**改看 `independent_count`**。前端:`EventTable`/`EventDetail` 三级标签 + `EventSources` 逐条标「(转载)」+ `constants.ts` 三级 `corroborationLabel()` + 两条词条。🔴 **两条实测反例定死实现形态**:① **不得用标题指纹**(同事件标题天然收敛,会把独立报道误判成转载、方向与目标相反);② **不得复用 `NormalizeTitle`**(内含 `stripBracketWrap` 抽【】内正文 → 裸标题与「【同标题】+正文」J=1.000,**实测 156 对**误判)→ 指纹取 **content 原文**,新增 `NormalizeReprintContent`(只去电头/HTML 标签,`reprintDatelineRe` 含「【标题】后紧跟电头」形态) | issue #83(分期 P-1);设计 `docs/phase11/design/reprint-stripping.md`;`docs/phase11/design/README.md`;`docs/架构总览.md` §6.2/§8.1;`docs/数据源总览.md` §5 | ⬜ **已实现待审核**(PR 未合,dev-only)。**校准先行(lab 生产库 2026-09-20~21,398 多机构簇 / 1369 成员 / 2735 跨机构对)**:正文指纹**强双峰**(p75=0.333、p90=1.000),门控 **0.85** 落在「下沿 0.80 全真转载、0.75~0.80 全改写」的内侧(0.84~0.88 区间逐条人工核对**全部为真转载**);电头剥离覆盖 **18.3%** 成员行、**只把真转载抬过门控**(≥0.85:397→453,混合区不变);@0.85 **50.0%** 簇独立来源 < 机构数、**39.9%** 由「≥2 家」跌到「恰好 1 个独立来源」。单测 7 个(真转载 6 例 / 改写 4 例**逐字取自实测语料**,含「裸标题 vs【同标题】正文」误判回归)+ `toEventItem` 投影测(硬验收:机构数 3 / 独立来源数 2;纯转载簇 2/1;无正文不误伤;未聚类=1);`go build/vet/test ./...` 全绿;`tsc --noEmit` + `vite build` 绿。⚠️ 残余精度上限(改写型转载漏判 → 仍高估)如实登记。**未上生产** |
| 2026-09-23 | **数据面地基 P-2(issue #83)** | 事件管线(issue #83)分期 **P-2** —— **数据面地基**(与 P-1 的读路径是**不同层面**)。issue §三 P4 表列「迁移 8 项」,本篇**复核收窄为 4 列**(1 项已落地:extra/0014;3 项**刻意不做**)。🔴 **不做 `raw_documents.origin`**(可从 `extra->>'source'` 读,建列 = 第二真源)、`raw_documents.is_reprint`(P-1 已读路径派生;**更根本:转载是簇相对属性**,需 canonicalIdx 才能定「谁转载谁」,**列级布尔本就不良定义**)、`events.corroboration`(P-1 已派生 `independent_count`;**落库会与 `reexamine` 的 `MergeClusters` 并发漂移** —— 簇成员数一变印证度就得重算 ⇒ 两处真源)。✅ **迁移 `0021` 新增 4 列**(幂等 `ADD COLUMN IF NOT EXISTS` + 逐列 `COMMENT`,**全部无 FK** —— 与决策边 `relationships` 同纪律):① `raw_documents.origin_kind`(`TEXT NOT NULL DEFAULT 'pipeline'` + CHECK,PG11+ 快速默认不重写表;**worker 的 `ListRawPendingStatus` 两处 WHERE + reconcile raw 层三处检查 + `ListReconDaily` 三处子查询**一律加 `origin_kind='pipeline'`;今日**无 realtime 行,过滤是契约非优化**,给 P-5 实时层立结构性隔离;⚠️ 去重键(0016)**不含本列**,P-5 须补「realtime→pipeline 提升」否则实时行抢吃正式行去重槽);② `event_clusters.canonical_event_id`(`ApplyClusters` 早已算出 canonical 却丢弃,此处持久化;**存量回填**取「最早非 merged 成员」,**比较器与抽出的纯函数 `canonicalIndex` 逐字一致** `created_at ASC`/同则 `confidence DESC`,用 `status <> 'merged'` 与 `countActiveMembers` 同口径;**代表冻结** —— reexamine 不重选,回填只对存量算一次);③ `raw_documents.canonical_id`(**只建列**,分组填充属 P-8,本版全 NULL,**未实现**);④ `events.human_verdict`(**只建列**,取值域本版不定故**故意不加 CHECK**,**引擎永不碰** —— 幂等重跑只动 status/cluster_id/updated_at)。模型 +4 字段(`RowToStructByName` **严格**模式 ⇒ `eventCols`/`rawDocCols` 两个共享常量同步)**打通已有消费端** | issue #83(分期 P-2);设计 `docs/phase11/design/event-pipeline-p2.md`;`docs/phase11/design/README.md`;`docs/phase11/design/reprint-stripping.md` §4/§7;`docs/进度总表.md`;`docs/架构总览.md` §1/§4.1/§6.2/§11.1;`README.md`;`docs/项目详解.md`;`PIKS架构设计文档.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。测试:`canonicalIndex` 单测 5 例(最早当选 / 同时间取高置信 / 同时间同置信取首个**确定性** / 单成员 / 最早压过高置信)+ `ApplyClusters` 落列集成测(`TestApplyClustersWritesCanonicalEventID`)+ `origin_kind` 门控 / `0021` 回填(最早胜 / 平局取高置信 / 全员 merged→NULL / 空→NULL / **幂等重跑**)/ `human_verdict` 跨 `SetEventCluster`+`MergeClusters` 存活三集成测;`go build/vet/test ./...` 全绿;`PIKS_TEST_INTEGRATION=1` 对本地 5433 实跑 `store`/`cluster`/`web` 三包全 `ok`。⚠️ **无 realtime 行 ⇒ 多源/起源隔离未做线上实测**(无法构造 prod 证据),以集成测试为准,**如实登记未线上验证**。**含迁移 `0021`**,**不含部署**。**未上生产** |
| 2026-09-22 | **自选自动同步(同花顺,issue #87)** | 用户诉求「自选/持仓/买卖截图导入太麻烦,要自动化」;本期**只做自选**。改「截图 → 视觉识别 → 人工确认」为**服务器每日 3 次自动拉取**同花顺「我的自选」(09:00/12:55/18:00,北京时间)。**数据源实测**(2 个无签名 GET):名单 `t.10jqka.com.cn/newcircle/group/getSelfStockWithMarket/` + 加入价/日 `ugc.10jqka.com.cn/selfstock_detail`(base64 XML),移动端 UA,无滑块/无指纹。**架构**:自选「成员资格」真源**仍是 `entities.status='watch'`(不迁表)**;新增表 `watchlist_entries`(**迁移 0020**)**只补加入价/日** —— 因 `entities.detail` 每轮被 entity-build 的 `UpsertEntity` **无条件覆盖**(本表存在的唯一理由,已钉集成回归 `TestWatchlistEntrySurvivesEntityBuild`)。新 `internal/ths`(协议层)+ `internal/watchsync`(策略层 **不 import web**)+ `cmd/watch-sync` 常驻(复用 tools 镜像,仍四镜像;重启去重靠 `task_runs.meta.slot`)。🔴 **code→name 三链回退**(本地 entities → 同花顺 realhead → deferred,**宁缺毋假绝不拿 code 当名建实体**);🔴 **失败不静默**(空名单/过滤归零/无凭据/登录失败一律 failed,`selfstock_detail` 失败降级须可见;#64 教训);🔴 **只读**(绝不向同花顺写);`NULL ≠ 0`。**凭据门控(#78 止血)**:`ths_account`/`ths_password`/`ths_cookie` 入 `app_config`,`POST /settings` **默认拒写**(需 `PIKS_ALLOW_THS_CRED_UI=1`),`/settings/form` 只回掩码 —— 防公网攻击者覆盖凭据锁死你 | issue #87;设计 `docs/phase11/design/watchlist-sync.md`;`docs/数据源总览.md` §1/§2.4/§3/§4.2;`docs/架构总览.md` §6.4/§8.1/§9.3/§9.5/不变量 #27-29;`CLAUDE.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。`go build/vet/test` 全绿;**真库集成测 10 个全过**(另新增 XML GB2312 回归)(含红线回归:NUL 往返、幂等、移出不删行、价/日不被 entity-build 覆盖、deferred、fail-closed、detail 降级、slot 去重);`tsc --noEmit` + 镜像拓扑守卫(11 命令)绿。✅ **账密登录端到端实测通过**(2026-09-22 探针:四步全通 → `✓ userid=<真实 id 略>`,43 项 / 38 A股 / 带价带日各 43 / 取名 38-38);**cookie 注入与账密两路均实测可用**(实现上注入优先)。⚠️ 排障坑:`code=-3 用户名或密码错误` **不区分账号/密码/不存在**(假账号回一模一样的 `-3`),登录失败无法从文案判断原因。🔴 **修掉一个只有真打上游才暴露的坑**:上游 XML 一律声明 `encoding="GB2312"`,Go 默认 XML 只认 UTF-8 → `CharsetReader is nil`,verify2 **第一步**即倒(fixture 当初用 UTF-8 故单测全绿却线上必挂);已统一 `newXMLDecoder()`(GB18030)并钉 `TestXMLCharsetReader`。⚠️ **未上生产** |
| 2026-09-23 | **早/晚档窗口 P-3(issue #83)** | 事件管线(issue #83)分期 **P-3** —— **早/晚档窗口(P1)+ 簇内代表选取(P6)**,依赖 **P-2**(已合 dev)。**交付面 = 仅后端**(前端「簇合并视图」展示单元属 P-4,不做)。① **P1 窗口**:新 `internal/web/board.go` 纯函数 `boardWindow(date, stage)` —— `late` = 当日 **09:15→18:30**、`early` = 前一日 **18:30→当日 09:15**,**衔接无缝无叠**(单测 `TestBoardWindow` 钉死 `early(d).end==late(d).start`、`late(d).end==early(d+1).start`);默认档 `boardStageNow` 按北京时刻(`>=18:30` → late)。🔴 **窗口锚 `events.created_at`(入库/抽取时刻)而非 `occurred_at`** —— 窗口理由是**阅读节奏**(早上看隔夜+盘前/晚上看全天,issue 明写**非热度切口**)⇒ 锚「我们何时拿到它」;已知边界(抽取滞后把事件推入更晚窗口,因管线收盘后批量跑)如实登记。② **窗口查询** `store.ListEventsInWindow(start,end)`:半开 `[start,end)`、**排除 `status='merged'`**、按 `created_at` 倒序、**不 LIMIT**(issue「窗口内全集不截断」,日量约百条可控),返回 `EventForAPI`(复用事件流投影)。③ **榜单接口 `GET /api/v1/board`**(`?stage&date`,`requireAuth`):响应 `stage/date/window_start/window_end/count/items[]`,每行 = **内嵌 `apiEventItem`**(与 `/api/v1/events` 单条同形,前端复用)+ 榜单特有 `score`;印证度**标签**仍由前端从 `independent_count` 派生(后端只给客观计数,P-1 约定)。④ **打分**:`boardScore(sig,max,w)=Σwᵢ·normᵢ`,`normᵢ=sigᵢ/maxᵢ`,`max=0` 防除零;本版 `boardWeights={CrossChannel:1,Entity:0,Watch:0}`(Entity/Watch 本版恒 0,接口先立);🔴 **`score` 照算随行下发但\*\*不用于排序\*\***(issue P1「作印证度标签,不排名」)。⑤ **P6 代表新规则**:比较器 `有直接链接 > 非转载 > 最早创建 > 更高置信`;`canonicalIndex` 增 `meta []memberMeta{hasURL,isReprint}` 平行参数,`isReprint` 由分量内**正文**经 P-1 指纹派生(`ReprintFlags(contents,-1)`,原发取组内最小下标);新增 `store.ListEventDocMeta`(按 event-id 集合取 `(url,content)`,**只对多成员分量取**控制正文取数面 —— `ApplyClusters` 拿到的 `[]model.Event` 只有 `RawDocumentID` 外键)。🔴 **不用渠道数选代表**(那是**簇级**属性,P6 红线)。⑥ 🔴 **存量 / 新簇口径有意分叉**:**只对新簇生效,不回填存量** `canonical_event_id` —— 保 P-2「代表冻结」纪律与 `based_on` 决策边不漂移;**`canonicalIndex` 与迁移 `0021` 回填 SQL 不再逐字一致**(批准的设计决定,改注释 + 改 P-2 文档 §1.3 **显式记录,不得静默**);`reexamine` 的簇级 `pickSurvivorIndex` **不动**。⑦ **`canonicalTitle` 改无 LLM 规则**(issue P6 规则 3 升为主规则):对每个成员算「与其他成员标题 `NormalizeTitle`+`Bigrams` Jaccard 之和」,取**重合度最高**者的**成员原文标题**(平票取最早,再平取先到;全 0 退化最早)—— 零 token、确定性、Fact 非 Inference;移除死字段 `PairVerdict.CanonicalTitle`(与 prompt JSON、`mock.go` 一并删)。**零 schema / 零迁移 / 零 LLM 增量** | issue #83(分期 P-3);设计 `docs/phase11/design/event-pipeline-p3.md`;`docs/phase11/design/README.md`;`docs/phase11/design/event-pipeline-p2.md` §1.3/§2.3/§4;`docs/进度总表.md`;`docs/架构总览.md` §6.2/§11;`docs/项目详解.md`;`README.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。测试:单测 `board_test.go`(`boardWindow` 早/晚边界 + **无缝无叠** / `boardStageNow` / `boardScore` 归一化与 w=0 项与 max=0 防除零)+ `canonical_test.go` 重写(`canonicalIndex` 新规则 7 例 + `canonicalTitle` 重合度高者胜/单成员/无共性退化最早)+ 集成 `pipeline_p3_integration_test.go`(`ListEventsInWindow` 半开边界/排除 merged/倒序/投影齐备 + `ListEventDocMeta` url/content 与零值/空入参)+ `canonical_integration_test.go` **新增** `TestApplyClustersRepresentativePrefersURL`(**有链接的更晚成员胜出**、更早无链接者被标 merged);`go build/vet/test ./...` 全绿;`PIKS_TEST_INTEGRATION=1` 对本地 5433 实跑 `store`/`cluster`/`web` 三包全 `ok`;**接口实跑**(本地 `cmd/web` + 登录)`GET /api/v1/board?stage=early&date=…` 返回窗口内事件、边界正确、merged 被排除、`independent_count`/`score` 齐备、按 `created_at` 倒序。⚠️ **未做线上实测**(dev 无新簇、无 realtime 行);**无前端消费端**(board 仅后端就绪,`tsc --noEmit` 未触发)。⚠️ 既有 flaky `internal/web/TestTokenSignVerify`(issue #78,~6% 概率误报:43 字符 RawURLEncoding 末位仅 4 bit 有效,篡改末位有时解码出相同字节)**与 P-3 无关**,应另开 issue 修。**未上生产** | 用户报「消息页过滤、筛选都不能用」。**根因(实测反证)**:`useUrlState.setParam` 每次 `new URLSearchParams(searchParams.toString())` —— 从**闭包里过期的 `searchParams`** 重建;而 `usePagedQuery` 的 `setFilter`/`setSize` 在**同一处理器内连调两次** `setParam`(写入筛选值 + 清 `page`)⇒ 两次共享同一份旧值、**第 2 次覆盖第 1 次**,最终只导航「只有 page 变化」的 URL,**筛选值静默丢弃**。浏览器/Playwright 反证:点快讯来源 chip「东方财富」→ `?tab=flash`(source 丢失);对照组(单次 setParam 的 tab 切换)正常 —— 差异只在调用次数,机制坐实。波及 **全部用 `usePagedQuery` 的页**:`events`/`flashes`/`announcements`/`notes`/`entities`/`trades`/`reports`/`RunHistoryTable`。**② 抽取态筛选是死选项**(叠加缺陷):生产 `events.status` = `extracted` 2642 / `merged` 1444,**无 `verified`/`published`**;旧映射 `confirmed→verified\|published` ⇒ 「已确认」**恒空**,「待复核」= 全量、无鉴别力。**③ 搜索口径不符占位符**:`handleAPIEvents` 只 `strSub(Title,Summary)` **不搜 `affected` 实体**(按公司名搜不到);公告占位符写「标题/代码」却只搜标题。**④ 公告缺完整筛选/排序**。**修法**:① `useUrlState` 增 `setParams(obj)` 原子多键 + 改**函数式 `setSearchParams((prev)=>…)`**(根治,非「小心别连调」);`setFilter`/`setSize` 各一次调用。② 重定义在产两态 `extracted`(已抽取)/`merged`(已被合并),`ListEventsForAPI` **放行 merged**、`eventStatusFront`/`eventStatusOK` 同口径精确匹配,删死选项(旧 `verified/published` 行并入 extracted);⚠️ **放行只开给事件列表** —— 看板「高置信事件 Top N」**无**知识态筛选,含 merged 会混入重复条目,故拆出 `ListTopEventsForDashboard`(排除 merged),两条查询共用 `eventsListQuery(includeMerged, sort)`。③ 事件 `strSub` 补 `affectedTerms(ev.Affected)`;公告补 `sec_code`/`sec_name` 并改占位符(快讯 `Title` 本就是 `COALESCE(title,content)` = 渲染正文,补注释说明)。④ e2e 补**筛选回归断言** | issue #80;`docs/架构总览.md` 不变量 #31 + §6.2 API 表 3 行;`docs/项目详解.md` 架构表 | ⬜ **已实现待审核**(PR 未合,dev-only)。`go build/vet/test ./...` 全绿;新增 `internal/web/event_status_issue80_test.go`(状态映射配对 + **死选项守卫** + `affectedTerms` 解析失败不误匹配,3 个全过)+ 真库集成测 `TestListEventsForAPIIncludesMerged`(自建隔离数据,`PIKS_TEST_INTEGRATION=1` 对本地 5433 实跑,断言 merged 出现在列表、extracted 不被过度收窄)。⚠️ **反证已做两道**(错误注入先回读确认落盘再信输出):把 `eventStatusFront`/`eventStatusOK` 还原成**旧实现** → 单测如实失败(6 处);把 `ListEventsForAPI` 的 `WHERE … 'merged'` 摘回旧 3 值 → 集成测如实失败。`tsc --noEmit` + `vite build` 绿。⚠️ **未跑真浏览器 e2e**:本机无可用带数据后端(本地 `piksp4` 栈 events 空表、lab :8090 跑的是**旧代码**无法验证新前端),新断言已写、语法过,但**筛选端到端实测未完成**,须在能连真实后端的机器补跑(如实登记,不冒充已验证) | `cmd/collector` 的 `runOne` 无论 `failCount` 多少都 `FinishTaskRun(...,"success",...)` + 返回 `nil` ⇒ ① 进程 exit 0 → `pipeline.sh` 写 `.done` 戳、当日不再重试;② `task_runs.status='success'` → 前端绿色「已完成」而实际 0 条入库;③ `meta.failed` **无任何读取方**(全仓只 `TokensSince` 读 `meta->>'ai_tokens'`)。真实复现:迁移 0016 期间插入侧 `ON CONFLICT` 与新 partial index 不匹配 → `new=0 dup=0 failed=123`,而记账/退出码全报成功。**修法**:`runOne` 按结局分开 —— **全失败** ⇒ `status='failed'` + 返回 error(退出码非零,管线**不写 done 戳**当日可重试);**部分失败** ⇒ 新状态 `partial`(不是 `success`),不拖垮整源(#43:免费源无 SLA);`dup` 不计失败;抽 `classifyInsert` 钉住「err 优先于 ok」判定次序。**② 前端能看见**:`toTaskRun` 补 `partial` 分支(**旧 switch 未匹配即落 `running`,「进行中」是另一种误导**)+ `apiTaskRun.Failed`(非可选数字,读 `meta.failed`)→ `Pipeline.tsx` 显示「N 条未入库」 | issue #64;`docs/数据源总览.md` §2.1;`docs/架构总览.md` §6.3/§8.1/不变量 #30 | ⬜ **已实现待审核**(PR 未合,dev-only)。**真库端到端三场景实测**(临时库 `piks_verify64`,migrate 20):① 正常 → `new=3 dup=0 failed=0` / `success` / exit 0;② 注入系统性失败(`ALTER TABLE … RENAME COLUMN content_hash`,正是 0016 事故形态)→ `new=0 failed=3` / `status='failed'` / **exit 1**;③ 注入单行 CHECK 约束 → `new=2 failed=1` / `status='partial'` / exit 0。真实 `GET /api/v1/dashboard` 回读 `task_runs` 三条映射正确(`ok`/`failed`/`partial`,含 `failed` 计数)。新增单测 `TestClassifyInsert`(含 err 优先反证)+ `api_taskrun_test.go`(状态映射 8 例 + `failed` 字段恒在);`go build/vet/test ./...` 全绿;`tsc --noEmit` + `vite build` 绿。**顺带核查其余 `success` 调用点**:`entity-build`/`worker`/`cluster`/`quote-collector`/`trade-import`/`trade-review`/`chat` **单操作形态,失败即 fail/error** 无此病;`reconcile` 有异常也记 `success` 但**属设计**(异常入 `meta.by_category` 且 `/recon` 上屏,非采集丢数),不在本 issue 范围 |
| 2026-09-23 | **展示单元 P-4(issue #83)** | 事件管线(issue #83)分期 **P-4** —— **展示单元(P8)+ 成本粗筛(P7)**,依赖 **P-3**(已合 dev)。**整条 epic 第一次变成用户可见**(P-1~P-3 只有后端消费端,`/api/v1/board` 已就绪但前端 `ENDPOINTS` 无 board)。**四个用户锁定决定**:① 一个 PR 交付 P8+P7;② P8 来源取数 = **本期实现 raw 层全集**(打通 `raw_documents.canonical_id` 分组与读路径,不妥协);③ P8 前端落点 = **新增榜单页 + 抽屉改簇视图**;④ P7 = 被筛掉的 raw 落 `status='deferred'` **不 delete、不静默丢**。① **迁移 `0022`**(`raw_documents.canonical_id` 回填 + partial index `idx_raw_docs_canonical`):依据 = P-1 正文指纹(`GroupReprints`,阈值 `0.85` **不另立**),**组内不同机构数 ≥2 才成组**(同机构两支同稿无意义);**代表 = 组内最早 `retrieved_at`,同则 `id`(确定性)**;**只填仍为 `NULL` 的行**(代表冻结,已非 NULL 不重选 —— 防「今天 A 代表、明天 B 代表」)。纯函数在 `internal/cluster/reprint_raw.go`,回填器 = **`cmd/cluster-raw-link`**(`cmd/` 第 **12** 个管线命令,raw 层与事件层解耦,独立命令而非并入 `cmd/cluster`),接入 `scripts/pipeline.sh` 的 `worker` 之前。② **P8 后端**:`apiEventItem` 增 `cluster_title`(展示单元标题 = `event_clusters.title`,新 `store.ListClusterTitles`)+ **`cluster_facts`/`cluster_affected`**(仅在 `len(members)>1` 时下发 —— canonical 单条只是子集,补 **簇内成员并集**,字面去重不模糊归并);`cluster_sources` 改由 **`store.ListClusterRawSources`** 供数(走 **raw 层全集**:成员 raw 所属转载组代表 → 该组全部机构),**`url` → `urls []string` 为 ADDITIVE 非破坏**(保留 `url = urls[0]`);🔴 **计数口径不变**:`source_count`/`independent_count`/`reprint` 仍是**事件层**(P-1 `ReprintFlags`/`IndependentCount`,与 P-3 响应一致),**raw 层只加宽机构+URL 列表**。③ **P8 前端**:新页 `/board`(`useUrlState` 写 `?stage=&date=`,复用 `EventTable`/`EventDetail`,**显式标注「本版无热度排序,只有印证度标签」**)+ 详情抽屉**簇视图**(`EventContent`/`EventDetail` 取 `cluster_title ?? title`、facts/affected 并集)+ **`EventSources` 按机构分组**(每机构列出**全部** URL;**无 URL 源如实写「该源无外链」绝不拼假 URL**;🔴 **金十按代码更正** —— 金十快讯**无逐条原文 URL**,`url` 实为**一级源**(新华社等)链接 ⇒ 渠道名与一级源名**分区标注**、**链接归属一级源**);`navItems.ts` 发现组首位增「榜单」(**13 → 14 项**)。④ **P7 成本粗筛**(判据落纯函数 `internal/extract/gate.go`):`important/confirmed=1` 必送 → `level=A` 优先 → 其余按 `reading_num` DESC → **跨层按 `retrieved_at` 时间窗截断**;被筛掉的 `MarkRawDeferred` 落 `status='deferred'` + `task_runs.meta` 记 `deferred`/`defer_reason`;**默认保守**(`-coarse-defer` 默认 false ⇒ 只排序不筛),**不得**替代 `ai_daily_token_budget` 护栏;`deferred` **不进对账**(非异常,与 `failed` 不同)、**不在快讯流显示**(抽取层概念,展示层不感知,仅任务台账可见)→ ⚠️ **P-5 更正**:此项**与代码不符** —— `ListRawDocumentsWithSource` 无 status 过滤,`deferred` 行**仍在快讯流显示**;P-5 修文档向代码靠拢(**不隐藏**),见 `event-pipeline-p5.md` §3.1。**一处迁移 / 零新表 / 零 LLM 增量** | issue #83(分期 P-4,epic 保持 OPEN);设计 `docs/phase11/design/event-pipeline-p4.md`;`docs/phase11/design/README.md`;`docs/phase11/design/event-pipeline-p2.md` §1.2/§4;`docs/phase11/design/event-pipeline-p3.md` §7;`docs/架构总览.md` §4.1/§6.2/§6.3/§7.1/§7.2/不变量 #35;`docs/进度总表.md`;`docs/项目详解.md`;`docs/数据源总览.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。测试:`internal/cluster/reprint_raw_test.go`(单测:≥2 机构成组 / 同机构两支**不**成组 / 代表取最早同则 id / 单成员不成组 / 与 P-1 `GroupReprints` 判据一致)+ `internal/store/pipeline_p4_integration_test.go`(`canonical_id` 回填幂等 + **只填 NULL** + 窗口取数;**`ListClusterRawSources` 走 raw 全集** —— 构造「某机构 raw 未抽成事件」仍出现在来源里(issue 硬约束验收)/ 同机构多 URL 只计一票 / 无 URL 源带出)+ `internal/store/pipeline_p2_guard_integration_test.go`(**`origin_kind` 门控反证**,含「**先验注入**」纪律:删 `raw_documents.go` 的谓词 → 回读确认落盘 → 测试**必须变红** → 改回)+ `internal/extract/gate_test.go`(粗筛:`important/confirmed` 必送 / level 优先 / `reading_num` 排序 / 时间窗截断 / 空 extra 不 panic / 保守默认)+ `internal/web/board_test.go` 更新(`cluster_title` 下发;`cluster_sources` 走 raw 层后 `independent_count` 与 P-3 口径**一致**);前端 `tsc --noEmit` + `vite build` 绿(`/board` 筛选写 URL、三态、无 URL 源「该源无外链」渲染、抽屉簇视图标题/并集)。🔴 **三处 issue 事实前提已按代码更正,不得照字面实现**:① issue「P8 链接取 raw 层全集」当前**不可验** —— dev `processed 且无事件 = 0` ⇒ 本期是**结构上更正确的取法**,**不是修好了某个丢失**(文档不得写成「修掉 N 条」);② issue P7「预算静默降级」缺口**已关闭**(#75/PR #84 已写 `budget_exhausted`/`guard_disabled`),P7 剩余范围只有粗筛;③ 金十两层描述与代码**相反**(见上)。⚠️ **未做线上实测**(lab 无多源簇、无 realtime 行);**raw 层与事件层两处转载判据可能因回填水位漂移**(`cluster-raw-link` 须在采集后跑),已显式登记 + 测试断言一致性。**未上生产** |
| 2026-09-22 | **collector 落库全失败仍报绿 (#64)** | `cmd/collector` 的 `runOne` 无论 `failCount` 多少都 `FinishTaskRun(...,"success",...)` + 返回 `nil` ⇒ ① 进程 exit 0 → `pipeline.sh` 写 `.done` 戳、当日不再重试;② `task_runs.status='success'` → 前端绿色「已完成」而实际 0 条入库;③ `meta.failed` **无任何读取方**(全仓只 `TokensSince` 读 `meta->>'ai_tokens'`)。真实复现:迁移 0016 期间插入侧 `ON CONFLICT` 与新 partial index 不匹配 → `new=0 dup=0 failed=123`,而记账/退出码全报成功。**修法**:`runOne` 按结局分开 —— **全失败** ⇒ `status='failed'` + 返回 error(退出码非零,管线**不写 done 戳**当日可重试);**部分失败** ⇒ 新状态 `partial`(不是 `success`),不拖垮整源(#43:免费源无 SLA);`dup` 不计失败;抽 `classifyInsert` 钉住「err 优先于 ok」判定次序。**② 前端能看见**:`toTaskRun` 补 `partial` 分支(**旧 switch 未匹配即落 `running`,「进行中」是另一种误导**)+ `apiTaskRun.Failed`(非可选数字,读 `meta.failed`)→ `Pipeline.tsx` 显示「N 条未入库」 | issue #64;`docs/数据源总览.md` §2.1;`docs/架构总览.md` §6.3/§8.1/不变量 #30 | ⬜ **已实现待审核**(PR 未合,dev-only)。**真库端到端三场景实测**(临时库 `piks_verify64`,migrate 20):① 正常 → `new=3 dup=0 failed=0` / `success` / exit 0;② 注入系统性失败(`ALTER TABLE … RENAME COLUMN content_hash`,正是 0016 事故形态)→ `new=0 failed=3` / `status='failed'` / **exit 1**;③ 注入单行 CHECK 约束 → `new=2 failed=1` / `status='partial'` / exit 0。真实 `GET /api/v1/dashboard` 回读 `task_runs` 三条映射正确(`ok`/`failed`/`partial`,含 `failed` 计数)。新增单测 `TestClassifyInsert`(含 err 优先反证)+ `api_taskrun_test.go`(状态映射 8 例 + `failed` 字段恒在);`go build/vet/test ./...` 全绿;`tsc --noEmit` + `vite build` 绿。**顺带核查其余 `success` 调用点**:`entity-build`/`worker`/`cluster`/`quote-collector`/`trade-import`/`trade-review`/`chat` **单操作形态,失败即 fail/error** 无此病;`reconcile` 有异常也记 `success` 但**属设计**(异常入 `meta.by_category` 且 `/recon` 上屏,非采集丢数),不在本 issue 范围 |
| 2026-09-23 | **访问控制:应用层鉴权 + 预算护栏 (#78)** | PIKS 公网暴露(§9.5)后**全链路无鉴权**:全部 `/api/*` 裸路由 + CORS `*`,公网可读写全部数据 + 触发 LLM 计费。**核实比 issue 更宽**:① lab gateway 发布 `0.0.0.0:8090`,**局域网可绕过边缘直连**;② `ai_daily_token_budget` 生产为 **0** = 护栏关闭(#75 语义),且 **`/chat`、`/research-runs` 处理器里压根没有预算检查**(只有 weekly/trades 有)→ 可无限烧 token。**落地(A:应用层)**:新 `internal/web/auth.go` —— 单密码登录(bcrypt)→ **HMAC-SHA256 签名会话 token**(payload `exp\|nonce`,`HttpOnly; Secure; SameSite=Lax` cookie,亦接受 `Authorization: Bearer`,含可选预共享 `PIKS_AUTH_TOKEN`);**60 分钟滑动续期**(剩余 <30min 才重签,响应头 `X-Auth-Token` 供 Bearer 客户端读回);**除 `GET /api/v1/healthz` 与 `POST /api/v1/auth/login` 外全部 `/api/*` 经 `requireAuth`**(白名单式,新增接口默认受保护);`PIKS_AUTH_PASSWORD_HASH`/`PIKS_AUTH_SECRET` 缺失 → `NewServer` 报错 **web 拒启动(fail-closed)**;新增 `cmd/hashpw` 本地生成哈希。**两处必验的坑**:① compose web healthcheck 打 `/api/v1/dashboard` → 上鉴权后 **401 → web 永不 healthy → gateway `depends_on` 卡死 → 部署挂** ⇒ 新增**免鉴权 `/api/v1/healthz`** + healthcheck 改指它(+ `deploy.sh:228` 核对 curl 同步);② CORS `Access-Control-Allow-Origin: *` 与带凭据跨域**天然冲突**、同源单入口下已多余 ⇒ **去掉改同源**。**同 PR 堵烧钱洞**:`/chat`、`/research-runs` 补 `budgetExhausted`(复用既有口径,`0` 仍 = 护栏关闭语义不变)+ per-IP 限流(LLM 30/min、登录 5/min)。**前端**:`api.ts` 加 `credentials:"include"` + **401 收敛跳 `/login?next=`**;`App.tsx` 加 `AuthGate`(首屏 `GET /auth/me`,门在所有路由外) + 无壳 `/login` 页;`SideNav` 底部「退出登录」。**零 DB schema** | issue #78;设计 `docs/phase12/design/access-control.md`;`docs/架构总览.md` §9.5 + CORS 段;README 生产部署/安全红线;`configs/.env.prod.example` + `docker-compose.prod.yml` + `deploy.sh` | 📝 **设计定稿 + 实现待审核**(本 PR)。`go build/vet/test ./...` 全绿(新增 `auth_test.go` 8 用例:fail-closed / 签名篡改 / 过期 / 滑动续期窗口 / 预共享 / 中间件放行 / 登录发 cookie / 登出清 cookie / 登录限流);`tsc --noEmit` + `vite build` 绿。⚠️ **未部署 lab**(随镜像重建生效);**部署前须先在 lab `.env` 写入两个必填密钥**,否则 web 起不来;生产预算仍 `0` 须由用户经 `/settings` 改非 0 |
| 2026-09-23 | **实时层 + 三档调度 + 保留期(P-5,issue #83 收口)** | 事件管线(issue #83)分期 **P-5 = 最后一个分期**,依赖 P-1~P-4(均已合 dev、未上生产)。**四个用户锁定决定**:① 实时层 = **复用 C 层 + 只做读接口**(不新增写入方);② 调度 = **重试窗口**(非 issue 字面单发);③ 榜单窗口锚 = **改锚原始到达时刻**;④ `deferred` 可见性 = **修文档向代码靠拢**。① **实时层 = raw 层滚动读**:`store.ListRawDocumentsWithSource` 增可选 `since time.Time`(零值=不限),`GET /api/v1/flashes?hours=N`(**ADDITIVE 非破坏**,缺省逐字不变)按 `COALESCE(rd.published_at, rd.retrieved_at, rd.created_at) >= $2` 取近 N 小时;前端 `flashes.tsx` 增「近 3 小时/全部」切换(写 URL query);🔴 **不做同事件合并/不排名/不做榜**(P3 红线),`origin_kind='realtime'` **保持 inert**(不启用写入方)。② 🔴 **榜单窗口锚改「原始到达时刻」**:`store.ListEventsInWindow` 的 `WHERE`/`ORDER BY` 由 `e.created_at` 改 `COALESCE(rd.published_at, rd.retrieved_at, e.created_at)`,`e.created_at` 仅作 `raw_document_id IS NULL` 兜底 —— P-3 §1.2 已**预先授权重评**;理由:全链收盘后一次跑 ⇒ 锚 `created_at` 使**早榜结构性恒空**;后果:早/晚榜成员集合与 P-3 验收**不同**(批准的设计变更,已登记);`board.go` 仅更新文件头注释(纯窗口/响应形状不变)。③ **保留期清理**:新命令 **`cmd/cleanup`**(第 **13** 个管线命令,`-days 28`/`-dry-run`,`-days<=0` 拒绝)+ `scripts/cleanup.sh`(**flock** + 周日闸 + 距上次成功 ≥28d,时间戳仅成功时更新,失败如实记日志);🔴 **只清「到期 + 无事件引用 + 非转载组代表」的行**(`store.cleanupWhere` 常量,候选与实删同判据);**判据单趟** ⇒ 任何时刻不留悬空 `canonical_id`(整组同时到期则渐进清理,非漏删);**已抽取行永久保留**(事件溯源 + FK)⇒ **「28 天」不是「全体 raw 的 28 天」**。**接线三处**:Dockerfile tools 段 + `check-image-topology.sh`(12→13)+ `deploy.sh` 的 `go_deps_hash_all`(同批补上 P-4 漏列的 `cluster-raw-link` —— 静默地雷)。④ **三档调度**:`scripts/pipeline.sh` 加 `STAGE=early|late`(**未知档 exit 2**),**按档**闸门(`early` 0915-1159 / `late` 1830-2259)、记账(`-$STAGE` 后缀)、flock(两档互不阻塞);步骤分 `COMMON`(两档:`migrate`/`collector -driver all`/`cluster-raw-link`/`worker -limit 800`/`cluster`)+ `EOD`(**仅 late**:`collector cninfo-announce`/`hot-topic`/`quote-collector`/`entity-build`/`watch-sync -once`/`market-state`/`daily-review`/`reconcile`)—— 🔴 收盘产物**不能进 COMMON**(09:15 跑会为尚未发生的当日出报告);`scripts/setup.sh` crontab **2→4 条**(early `*/10 9-11 1-5` / late `*/10 18-22 1-5` / cleanup `0 2 * * 7` / backup),逐条 `grep -v -F` 去重;🔴 **realtime 档不在 cron**(常驻 collector 已覆盖,不加新服务)。⑤ **两处修正**:`deferred` 在快讯流的**文档-代码不一致**(修文档向代码靠拢,**不隐藏**,红线)、`ReconProcessedNoEvent` 的 **NULL 标题崩溃**(`COALESCE(r.title,'')`,金十无标题致对账 500)。⑥ **三处 issue 事实前提按代码更正**(实时层字面已被 C 层取代 / 窗口锚使早榜恒空 / realtime 档不在 cron)。**零 schema、零迁移、零新服务、零 LLM 增量** | issue #83(分期 P-5,epic 保持 OPEN);设计 `docs/phase11/design/event-pipeline-p5.md`;`docs/phase11/design/README.md`;`event-pipeline-p3.md` §1.2/§7;`event-pipeline-p4.md` §7;`docs/架构总览.md`;`docs/项目详解.md`;`CLAUDE.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。测试:`internal/store/pipeline_p5_integration_test.go`(清理安全边界:有事件引用不删 / **被存活成员指向的代表不删** / 未到期不删 / dry-run 候选数==实删数 / 幂等)+ `pipeline_p3_integration_test.go` 更新(**锚原始到达** `anchorProof` 用例;`docID` 助手改收 anchor 参数)+ `internal/web` 回归(`hours` 缺省不变)。**实跑(dev 5433)**:植入 40 天前无引用 raw → `-dry-run` 候选 1 → 真跑删 1 → 重跑 0 → 探针消失,`task_runs` 记账核对;`pipeline.sh early|late` 桩干跑:early=5 步、late=13 步,按档台账/锁分列,档外/周末/未知档正确退出;`cleanup.sh` 两闸跳过并记日志。`go build/vet/test` 全绿;`tsc --noEmit`;拓扑检查 **13** 命令。⚠️ **未上生产**;已知 flaky `TestTokenSignVerify`(issue #95)与本期无关 |
| 2026-09-23 | **`TestTokenSignVerify` 偶发误报 (#95)** | 测试自身缺陷,非产品逻辑:`TestTokenSignVerify`(#78/PR #91 引入)约 **6%** 概率误报「篡改签名仍通过」,使 `internal/web` 偶发红、CI 门控不可信。**根因**:会话签名 32 字节经 `base64.RawURLEncoding` 编码为 **43 字符**,**末位字符仅承载高 4 bit**,低 2 bit 是丢弃的填充位;旧测试 `tok[:len(tok)-1]+"X"`,当原末位字符低 2 bit 与 `X`(下标 23,低 2 bit=`11`)相同时,解码出的 32 字节**完全相同** ⇒ 篡改在字节层面是 **no-op**,签名当然仍通过。命中概率 4/64 = **6.25%**。**实测坐实**:探针跑旧写法 2000 次 = **126/2000 = 6.30%**。**修法**:改**非末位**字符(`flipChar(sig,0)` —— 首字符承载 6 个有效 bit,必然改变解码字节)+ 篡改后**先断言「解码字节确实变了」再验签**(测试自身失效时立刻失败,不再静默假绿)。**验证**:`go test ./internal/web/ -run TestTokenSignVerify -count=200` 全过(反证探针实测旧写法 6.30%);`go build ./...` + `go vet ./internal/web/` + 包测全绿。🔴 **只改测试,零产品代码改动** | issue #95;`docs/进度总表.md` | ⬜ **已实现待审核**(PR 未合,dev-only)。issue「约 6%」经探针确认为 **6.30%** |

## 决策登记(并入相关,2026-09-12)

| 决策 | 结论 | 依据 |
|---|---|---|
| steady 是否并入 PIKS | ❌ 不并入 | 两个完整 Go+Python 服务栈(含模拟交易系统);数据仅覆盖 hs300+zz500 约 800 只,与 PIKS 涨停池/中小盘事件流交集窄;合并只增部署耦合。数据交集待 SQL 验证后再谈窄点集成 |
| research 是否并入 PIKS | ✅ 并入 | Python 6562 行 / 5 个纯数据依赖 / **无自有 LLM runtime**(`synthesize` 从文件或 stdin 读 LLM 输出,接口即为外部 LLM 设计)/ SQLite 层 327 行可整体删除;翻译为 Go 是纯亏损 |
| research 的 LLM 归属 | PIKS `ai.Provider` 提供(extract 档优先,回退 reasoning) | 一套配置、一处成本、复用预算护栏与 `task_runs`;`synthesize` 的 Number Lint 构成第二道防幻觉闸门 |
| `/chat` 是否改 tool-loop | ❌ 不改 | 保持现有「检索→引用」纪律(chat.go 引用白名单);深研作为**可引用对象**(`[R:run_id]`)而非对话内可调用动作 |
| Go vendor | ❌ 移除 | 仅为 1 个直接依赖(pgx)背 280 文件/8.2MB;go.sum 逐包校验完整性不弱于 vendor;dev 机已配 `goproxy.cn` |
| Python 部署形态(D-2) | ⚠️ **已两度变更:单镜像 → 四镜像** | ① 2026-09-12:原定同仓双镜像 → 改**单镜像** `piks-tools`(底座 `python:3.12-slim`,含 nginx + Go bins + React dist + research 运行时),理由:Go 编排在 web 进程内 `os/exec python3`,UI 深研与 CLI 共用同一容器。② **2026-09-20(issue #47):再拆为四镜像** —— 单镜像的升级半径不可分(改前端整镜像重建、`COPY . .` 击穿 Go 层全量重编实测 27s),且「改 Python 需重启 web」。拆分代价是 web 从此无 python3,故深研触发必须离开 web 进程 → DB 队列 + research 容器常驻 worker。详见 `docs/phase10/design/container-split.md` |
| research 独立迭代(D-11) | ✅ **支持(硬约束)** | **代码级独立性不变**:Go/前端零接触 research 源码,由 `scripts/check-research-isolation.sh` 在 CI 校验(含新增规则:web 容器无 python3,`exec.Command` 收敛到白名单)。⚠️ 已知收窄:四镜像下 research target 仍由共享的 Go 编译阶段派生(worker/research-run 是 Go 二进制),故改 `internal/research` 会重建 research 镜像;但**不重跑 node**、gateway/web 不动 |
| research 的 SQLite 存储层 | ❌ 删除 | 327 行仅被 cli.py(3 处)与 engine.py(2 处)引用;归档交 PG,消灭双写隐患 |

## 契约缺口进度(数据源契约)

| 缺口 | 归属 | 状态 | 落地日期 |
|---|---|---|---|
| G1 新闻快讯接入(东财) | 迭代 1 | ✅ 探针 + 真实驱动(getFastNewsZhibo,实况采集 50 条) | 2026-08-26 |
| G2 行情/涨停数据(东财 push2ex/push2) | 迭代 2 | ✅ 涨停/跌停/炸板池 + 指数;⚠️ 指数/成交额/涨跌家数(push2)受 WAF 限流,如实 pending 待源核验 | 2026-08-26 |
| G3 公告 | 迭代 4+ / issue #50 | ✅ **已接入巨潮公告为独立采集源**(issue #50,2026-09-21,epic #43 T4):Go 驱动进日管线,落 `raw_documents`(`source_type='announcement'` / `status='collected'`),**不进 LLM**(官方披露无真假问题),消息页「公告」tab 呈现。⚠️ **只存标题 + 原文 PDF 外链,不存正文** —— issue #50 验收**已正式改判为单源接入**(原第 2 条「巨潮/东财双源可比对」已从 issue 正文移除);正文抓取实测被上游限流否决。**已合并入 dev**(PR #63);未上生产。**公告分级**(issue #68 S1,2026-09-21):标题规则打 `grade` 标签 + 前端默认折叠,解决「全平铺看不过来」;**只落标签、不改 `status`** | 2026-09-21 |
| G4 财务数据 | 迭代 4+ | ⬜ D23 明确不在迭代 3 | |
| G5 宏观/政策 | 迭代 4+ | ⬜ D23 明确不在迭代 3 | |
| G6 个人知识回写 harvest | 迭代 5-2 | ✅ Web 内编辑 personal_notes(替代 Obsidian→PG 收割器),笔记可关联事件/实体,事件卡「我的理解」落库,周报聚合 | 2026-08-27 |
| G7 AI 对话页 + 截图上传 | 迭代 5-3 | ✅ 完成:探针实测 Zen `deepseek-v4-flash-vision-exp` 接受 image_url;截图识别+问答带引用;未配置视觉模型时如实降级 | 2026-08-27 |
| G8 语义检索 | 迭代 5+ | ✅ 完成(dev-only 不升级生产):探针 Zen 无 embeddings 端点(404)→ 方案 B LLM 同义扩展;「降准」→「下调存款准备金率」语义召回;失败降级纯关键词 + 如实标注 | 2026-08-28 |
| G9 事件类多源(≥3 独立机构) | issue #43 T1→T4 | ✅ **完成 T1 多源采集**:快讯接 **6 个独立机构源**(东方财富/金十数据/财联社/新浪财经/同花顺/富途资讯),全部自建纯 HTTP 驱动读原始 JSON(akshare 会丢 url/热度字段);`sources.name` 改**机构名**;上游原始字段落 `raw_documents.extra`。✅ **T2 同事件判定已合并并上生产**(issue #48,PR #52,2026-09-20):归一化加固还原跨源相似度(**不降阈值 0.7**)+ 簇内 ≥2 机构下发 `cluster_sources`(机构/原文链接/上游一级源)。✅ **T3 漏报/冲突检测已合并并上生产**(issue #49,PR #54):`source_count` 单源标注 + 数值冲突留双方原文(门控 0.6,生产标定);详见 `docs/phase11/design/event-cross-source.md`。✅ **T4 公告独立源已合并入 dev**(issue #50,PR #63,2026-09-21):巨潮公告接入日管线,**只存标题 + 外链**;**未上生产** | 2026-09-21 |

## 执行决策登记

> 完整登记见 `项目详解.md`。三个关键决策(回写分域双源 / V1 只接新闻源 / 因果链为模板)已在其中定稿。
> 迭代 1 落地后增补:
> - **OpenCode Zen 必须走 `/zen/go/` 路由**(2026-08-26 实测):`/zen/v1` 对当前账号报 CreditsError,`/zen/go/v1/chat/completions`(OpenAI 兼容)与 `/zen/go/v1/messages`(Anthropic,Claude Code 所用)均有效。PIKS 走 OpenAI 兼容路径,base URL 带 `/go`。
> - **聚类实体交集改为包含匹配**:真实抽取实体措辞有方差("银行" vs "银行板块"),精确匹配漏聚;pre-filter 只求召回,精确判定交 LLM(commit `835a8e8`)。
> - **迭代 2(2026-08-26)新增**:
>   - **东财 push2 主机有 IP 级 WAF 限流**:池端点(push2ex)稳定,指数(push2 stock/get)偶通,ulist.np/clist/f6 持续 SSL RST。策略 = 池数据必须、其余尽力而为,失败如实 pending(宁缺毋假,§5.8)。
>   - **复盘页幂等坑**:行业 top-N 曾只按 count 排序(Go map 随机序破坏重跑零提交),加 name 次级排序修复(commit `2daf48e`)。
>   - **情绪阈值初版,~20 交易日真实数据后校准**(架构 §9.8)。
> - **迭代 3(2026-08-26)新增**:实体层走**单表 `entities`(type 判别 + detail JSONB)**,复用 relationships 多态(不新建关系表);实体构建 = 种子源零 AI(zt_pool → Company/Industry)+ 事件 affected 未命中词便宜档批量分类一次(拿不准 → `unknown` 不猜);行业仅叶子(不建 parent 链);G3 巨潮/财务/宏观明确延后。
> - **聚类质量(2026-08-28)新增**:
>   - **重审视 Pass 不降阈值**:跨簇重复根因是结构性盲区(候选池缺既有 canonical),非阈值过紧;重审视复用迭代 1 三关(同类型 + Jaccard≥0.7 或实体交集+3天 + LLM 确认),只扩候选池不降门槛,防过度合并。
>   - **survivor 恒为既有簇,标题不动**:absorbed 簇全体成员 `cluster_id` 迁入 survivor、原代表标 merged、簇行标 merged(事务);canonical 跨簇重选/标题迁移列后续可选(避免卡面重写与 git 噪音)。
>   - **预算护栏顺延**:重审视沿用正常 pass 剩余额度,耗尽则跳过并如实记 `reexam_skipped_budget`(保护行为,非降级缺陷)。
> - **迭代 3(2026-08-27 落地)新增**:
>   - **实体构建两阶段写入防 churn**:内存收集贡献(别名并集 + 关系)再一次性 upsert,避免「种子写别名 → 收割改回」的来回写。
>   - **affected 词 → aliases 补全 + 发布器 TermResolver**:命中实体(含后缀剥离)→ `[[entity-xxx|原词]]`;未命中保持纯文本(诚实不假造)。
>   - **发布器全量候选重渲染**:实体层更新后已发布事件卡自动升级 wikilink;幂等靠 md5 跳过 + 仅首发布打标记(重跑 git 零提交、DB 零 churn)。
>   - **空别名序列化 `[]` 非 `null`**(json.Marshal(nil)→null 的坑,真机修复归零)。
>   - **AI 调用收敛**:唯一一次便宜档分类(首跑 7 词),此后全匹配 AI 归零;护栏耗尽 → 未匹配词全部 unknown。

## 已知遗留(登记,续接先看这里)

> 截至 2026-08-27(迭代 5 收官)统一登记。各阶段归档里的「遗留」以本节为权威汇总,避免续接时重复盘点。

### 数据源缺口(设计明确延后,想做才做)

| 缺口 | 状态 | 说明 |
|---|---|---|
| G3 公告 | ✅ 已接入(巨潮) | D21 原延后;issue #50 已接入巨潮为**独立采集源**(标题级,不进 LLM);⚠️ 原验收的「双源可比对」**已于 2026-09-21 从 issue 正文改判移除**(改为单源接入),故公告仍**只有 1 个机构源**,跨源漏报无法发现 |
| G4 财务数据 | ⬜ 延后 | 不在迭代 3 |
| G5 宏观/政策 | ⬜ 延后 | 不在迭代 3 |
| G2 残留:push2 指数/成交额/涨跌家数 | ⚠️ pending | 东财 WAF 限流,宁缺毋假,待源核验 |
| **事件类单源(快讯/公告)** | ⬜ 部分解决 | 快讯已接 6 机构源(T1);**公告已接 1 机构源**(巨潮,issue #50/T4,2026-09-21),但**仍只有 1 家**,跨源漏报无从发现(issue #50 的「双源比对」验收已于 2026-09-21 改判移除)。候选渠道已实测盘点(财联社/新浪/同花顺/央视 ≥3 独立机构)→ **issue #43**;清册见 `docs/数据源总览.md`。**同事件判定(T2)已落地**(issue #48):多源同事件聚一簇 + 簇内可见各源来源;**漏报/冲突检测(T3)已落地**(issue #49):`source_count` 单源标注 + 数值冲突留双方原文。⚠️ **生产仍只采到 1 源**(lab `pipeline.sh` 编排漂移,根因已修、lab 未执行,见 `docs/数据源总览.md` §5.1) |
| **新浪「转载≠独立源」**(C 层 S2) | ✅ 已解决(dev-only) | 担心新浪作为聚合平台多转载金十 → 污染「独立信源数」。C 层以生产聚类同尺(@0.7)实测新浪↔金十近同对仅 **11.2%/11.5% 且双向对称** → **不删新浪**(设计红线;实测见 `docs/phase11/design/flash-cadence.md` §2)。**根因已由 #83 P-1 剥转载解决**(2026-09-23):簇内各机构正文指纹近逐字(≥0.85)判为转载、并成一个独立来源,`independent_count` 即「几家在**各自**报」。**未上生产** |
| **快讯盘中不采 / 无高频调度** | ✅ 已解决(dev-only) | C 层新增常驻 `collector` 服务(`-interval 3m`,盘中每 3 分钟)+ per-host 三护栏。**未上生产**(待 PR 人工审核) |
| **交易日历未做**(C 层) | ⬜ 延后 | 常驻循环只判「工作日 + 时段闸」,节假日空转(无新数据、护栏正常)。与 #45 P5「交易日历暂不做」一致 |

### 质量技术债

- **G8 语义检索(embedding)**:✅ **2026-08-28 完成(方案 B:LLM 同义扩展,dev-only 不升级生产)**。探针:Zen 无 embeddings 端点(`/embeddings` 404),方案 A(Ollama)评估后否决;实现 = 问题经 extract 档模型扩展同义改写词,并入 n-gram 检索(原文权重 1.0/扩展 0.5),失败自动降级纯关键词 + 页面标注。验收:「降准」→ 召回「下调金融机构存款准备金率」(全文无「降准」)。详见 `stages/g8.md`。⚠️ 真向量检索仍待「有 embedding 源」前提;当前点名场景已解决。
- **聚类 LLM 判定偏保守**(2026-08-27 运行 `llm_pairs=9 merged=0`):✅ 2026-08-28 重审视 Pass 落地,跨簇漏聚结构性修复(**不降阈值**,仍三关);阈值偏保守本身仍为独立质量话题(需真实重复样本统计)。⚠️ **2026-09-22 #75 补**:候选池**不收敛**是该症状的另一半 —— 存量未配对事件每轮重扫、被反复送检,`llm_pairs` 里混着大量「判过又重判」的对。已由扫描水位 + 重审视时间窗收口(§3.4);**LLM 判定分支准确率至今零实测**(校准期网关欠费),故「改判定策略/砍分支」类减量须先补真值。
- **跨簇重复盲区**:`ListUnclusteredEvents` 只聚未聚类事件,已聚 canonical 不被重新审视 → ✅ **2026-08-28 重审视 Pass 落地**(`cmd/cluster` 正常 pass 后追加):候选池扩为「活跃簇 canonical ∪ 未聚类事件」,复用迭代 1 阈值 + LLM 确认,确认后并入既有簇(`MergeClusters` 整体迁入);dev 实测降准两簇合一、Web 一张卡、幂等零消耗;详见 `design/cluster-quality.md` + `stages/cluster-quality.md`。⚠️ **2026-09-22 #75 补**:该 Pass 的池**无界**(全部活跃簇代表,每日递增)且**每轮从零重算**,是单日 2350 万 token 的第二大来源;已由 `-window-days` 时间窗 + 「窗口内已标记事件」并入收口。**残留未解**:「判过**不同**的对每轮重生」这一层时间窗只解池边界、不解该层,需落判过台账(`cluster_pair_verdicts`),已归新 issue。

### 可选增强(设计已标可选/备选,未实现)

- **对话多会话**(web-app §4.4 可选):当前单会话 + 清空按钮,无会话列表页。
- **交易功能延伸**(design/trades.md §6 边界):✅ 2026-08-28 录入+解读+持仓已落地(dev-only);✅ 2026-08-28 交易闭环已落地(dev-only,`design/trade-loop.md` + `stages/trade-loop.md`):持仓 AI 诊断(成本/盈亏对照 + 集中度 + 交易联动带引用)+ 本周交易/持仓进周报页面与 AI 综述。后续可选:持仓收益测算、买卖配对(T+1)回测、多笔批量复盘、自动/定时诊断。
- **截图 OCR 降级**(web-app §4.3 备选):G7 视觉模型实测通过后未走此路,逻辑未实现。
- **周报 AI 综述**(iter4 D26 可选):✅ **2026-08-28 完成(Web 形态落地,dev-only 不部署 lab)**。`/weekly` 新增「AI 综述」段:手动按钮触发(POST action=generate)、高智档(`ai_model_reasoning`,未配置回退 extract)生成、落 `weekly_summaries` 表按 ISO 周缓存(GET 永不调 LLM,缓存命中零成本)、预算护栏 + task_runs 记账(`weekly-summary` 含 ai_tokens)、未配置/预算/无数据/失败如实降级;详见 `design/weekly-ai-summary.md` + `stages/weekly-ai-summary.md`。后续可选:自动触发、历史综述列表、跨周对比。

### 运维

- **lab NTP 需 sudo**:系统时钟同步确认修复需 sudo(用户/lab 管理员执行;模型无 sudo 不代跑)。
- **failed_raw 滞留**:lab 生产 1 条(`352413df`,validation: empty events array),旧采集失败,可清理。
- **异地备份**:用户自理(本地 `backup.sh` 已有)。
- **公网暴露无鉴权(#78)**:`piks.5home.online` 已公网可达但**全链路无 auth**(详见 `docs/架构总览.md` §9.5)。用户明示「先裸奔、自己担」,待补鉴权(边缘 Basic Auth / mTLS / 应用层登录);补齐前须确认 `ai_daily_token_budget` 闸生效(依赖 #75)。
- **阿里云边缘配置不在本仓库**:`/root/host-infra`(vhost 模板 / `edge.env` / `deploy.sh`)**未纳入 git**,仅存于阿里云宿主机;重建/迁移该宿主需另行备份此目录。

### 已过时(不再需要做)

- **vault 数据卫生**(清测试卡/合并跨簇重复):迭代 5-2 已下线 vault/GitHub 界面层,清理对象失去意义。
- **迭代 4 设计的 `cmd/harvest`、`cmd/weekly-report`**:已被 Web 内编辑 personal_notes + `/weekly` 替代。
